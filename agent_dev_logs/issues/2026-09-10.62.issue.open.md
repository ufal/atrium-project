# [\#62 Issue](https://github.com/ufal/atrium-project/issues/62) `open`: Build, release, run - retire the E2E entrypoint overrides
**Labels**: `enhancement`, `development`


#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) opened issue at [2026-09-10 12:18](https://github.com/ufal/atrium-project/issues/62):

The clearest possible build/release/run violation: a release artifact that re-resolves its own pinned dependency at run time.

`.github/workflows/e2e-pipeline-smoke.yml:274`, inside Stage 2, running **against the already-published image**:

```
--entrypoint /bin/sh
ghcr.io/ufal/atrium-alto-postprocess:<tag>
-c "pip install https://github.com/cneud/alto-tools/archive/refs/heads/master.zip && export PATH=… && python run_pipeline.py …"
```

`atrium-alto-postprocess/setup/requirements.txt:8` pins `alto-tools @ git+https://github.com/cneud/alto-tools.git@1f4f01e5f6ac3562740e39948442973b9cb94be4`, and `Dockerfile:38` installs that file at build time. So the E2E lane does not merely duplicate the build — it **overrides a commit pin with a moving `refs/heads/master`**, in the artifact we ship, at the moment we test it. Whatever that lane proves, it is not a property of the published image.

Worth root-causing before deleting: the build-time install runs as root (`USER atrium` only at `Dockerfile:79`), so the console script lands in `/usr/local/bin`, while the runtime patch installs `--user` into `/home/atrium/.local/bin` and re-exports `PATH` to reach it. The workaround may simply be stale. **Step one is `docker run --entrypoint alto-tools ghcr.io/ufal/atrium-alto-postprocess:<tag> --version` against a published tag** — the answer decides whether this is a two-line deletion or a real packaging bug.

stop re-resolving alto-tools inside the published image at run time

### Also in scope: retire the entrypoint overrides

The same workflow replaces the entrypoint on **every** stage — `--entrypoint /bin/sh` (`:272`), `--entrypoint python` (`:289`, `:311`, `:336`) — so no Dockerfile `ENTRYPOINT` is exercised anywhere in that lane. #55's `docker-build-smoke` (`load: true` + `probe-targets`) proved the mechanism for starting these containers as built. Remove each override where the real entrypoint can now run the stage; where it genuinely cannot, say why in a comment rather than leaving a silent override.

### Rider: close the remaining unpinned refs (factor II)

Factor II is otherwise green (B1 closed — `uvicorn` in all five `service/requirements.txt`). Three unpinned resolutions remain, same class of problem:

- `atrium-nlp-enrich/requirements_flexiconv.txt:1` — `flexiconv @ git+https://github.com/ufal/flexiconv.git`, no ref
- `atrium-llm-enrich/requirements_flexiconv.txt:1` — same
- `atrium-alto-postprocess/Dockerfile:42` — `git clone --depth 1 FreeOCR-AI/layoutreader`, no commit

### Acceptance

- E2E Stage 2 runs with **no `pip install`** in its command and **no `--entrypoint` override**, and passes.
- `grep -c "pip install" .github/workflows/e2e-pipeline-smoke.yml` returns 1 (the `tools/e2e/requirements.txt` line at `:102`, which runs on the runner, not in the image).
- Every `git+` requirement in the ecosystem carries an explicit ref, and the layoutreader clone a commit.
- The vendored extraction/statistics code is referenced in the alto-postprocess README with its upstream origin and licence.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-10 13:03](https://github.com/ufal/atrium-project/issues/62#issuecomment-5619112183):

Sized wrong — it's much smaller. See `agent_dev_logs/digests/62.digest.md`.

**One acceptance bullet is unsatisfiable here.** "Every `git+` requirement in the ecosystem carries an explicit ref" covers `flexiconv` in nlp-enrich and llm-enrich, which can't be fixed from this repo and have no issue in either. Either open two one-liners or strike the bullet.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-11 13:47](https://github.com/ufal/atrium-project/issues/62#issuecomment-5635363354):

#### Summary of recent changes by Opus 5

Landed on `test`: hub 598621e (+ records 49cc1b2), nlp-enrich `843a7ab`, llm-enrich `d645e75`.

Smaller than filed — Stage 2 was already fixed by 6ba4807 + alto `6ddabf7`, and the other three overrides were **exact no-ops**, each reproducing the entrypoint it replaced. Deleted them plus the duplicated script argument each one hid (Stage 5 keeps `openrouter_client.py` — that image's entrypoint is the bare interpreter). Verified against the **release** tags, not `test`, since the lane defaults to `latest`: `v0.10.5` / `v0.20.1` / `v0.6.3` all carry the entrypoints relied on.

New `tests/test_e2e_entrypoints.py` blocks a silent override returning — an override must be declared with a reason, and no `docker run` may install at run time. Confirmed red against the old Stage 2 shim before being trusted. `flexiconv` pinned `@v0.3.10` in both repos, so factor II is green.

Acceptance: overrides 0 ✅ · `pip install` 1 ✅ · every `git+` pinned ✅ · **the lane running green with real entrypoints** ⏳ — needs a dispatch at `latest` and `test`; the duplicated-argument trap is invisible until then.

Successor finding, wants its own issue: `-w` points every stage at a fresh checkout rather than the image's `/app`, so a change that never reached the image still passes E2E — and that is *why* these overrides looked harmless for years. alto#50 is 🟢 and can be closed.


-------------------------------------------------------------------------------



[Export of Github issue for [ufal/atrium-project](https://github.com/ufal/atrium-project). Generated on 2026.09.11 at 16:41:40.]
