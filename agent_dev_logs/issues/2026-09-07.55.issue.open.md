# [\#55 Issue](https://github.com/ufal/atrium-project/issues/55) `open`: Add `HEALTHCHECK` and `SIGTERM` handling to all five tool-repo services
**Labels**: `enhancement`, `development`


#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) opened issue at [2026-09-07 11:34](https://github.com/ufal/atrium-project/issues/55):

Zero Dockerfiles declare `HEALTHCHECK`; no service handles `SIGTERM`. The `/health` endpoints we built are invisible to a Kubernetes liveness probe, and a rolling restart kills in-flight work.

This is the concrete, actionable subset of #53's 12-factor checklist — days of work, and the difference between images ARÚP/ARÚB can run unattended vs. must babysit.

**Acceptance**: ARÚP/ARÚB confirm the images run cleanly on their Kubernetes.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-07 18:28](https://github.com/ufal/atrium-project/issues/55#issuecomment-5574433020):

Implementation notes from the first pass — three findings that change this issue's scope.

**1. Kubernetes does not read Docker `HEALTHCHECK`.** The kubelet uses `livenessProbe` /
`readinessProbe` / `startupProbe` from the pod spec and ignores the image's directive
entirely. `HEALTHCHECK` is still worth adding (docker/compose/podman read it, it documents
the contract in-image, and CI can now assert it), but it cannot by itself make `/health`
visible to a K8s probe. The partner-facing half is a probe contract plus a reference
manifest: `docs/k8s_deployment.md` + `docs/templates/k8s/atrium-service.deployment.yaml`.

**2. Four of the five services had no runnable API image.** Only nlp-enrich published one.
alto-postprocess, translator and page-classification reached FastAPI *only* through a
`docker-compose entrypoint:` override on the batch image; llm-enrich's `api` stage was
declared but excluded from `build-targets` (roadmap B5). Since the handoff boundary is "a
Docker image, run at ARÚP/ARÚB's own Kubernetes" (#40, 2026-08-02), there was nothing to
pull — the acceptance criterion was unreachable by configuration. All four now publish an
`api` target.

**3. Two prerequisites had to land first.** Three services called blocking inference inline
in `async def` handlers; uvicorn's SIGTERM handler is an event-loop callback, so it could
not run until the request returned and `--timeout-graceful-shutdown` had nothing to
measure. And nlp-enrich's shallow `/health` ran `run_pipeline.py --dry-run` as a subprocess
on *every* probe, before checking `?deep=`, unbounded — the worst possible target for a
30s-interval HEALTHCHECK.

**On "a rolling restart kills in-flight work"** — this was literally true of `POST /jobs`:
it returned `queued` and left the job in a bare `asyncio.create_task(...)` with the result
discarded, so uvicorn saw zero in-flight requests and exited mid-job. Neither
`--timeout-graceful-shutdown` nor in-flight-request middleware can reach that. Jobs now
register with `ServiceState.track()`, which the drain awaits — verified against a real
uvicorn subprocess sent a real SIGTERM.

**Design note:** readiness is a **separate `/ready`** route; shallow `/health` stays 200
even while draining. A liveness probe that fails during a graceful shutdown gets the pod
SIGKILLed before the drain finishes — this issue's own failure mode, one layer up.

**Scope/estimate:** this came to 53 files across six repos, past "days of work" and past
the Q3 milestone's 2026-09-30 due date. Suggest moving the milestone rather than missing it.

Also: roadmap **B8** needed no fix — `service/text_api.py:475` already env-gates `reload`,
default false. Six documents described it as open; corrected in `docker_gha_roadmap.md` §8.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-07 18:32](https://github.com/ufal/atrium-project/issues/55#issuecomment-5574463555):

#55's acceptance criterion is "ARÚP/ARÚB confirm the images run cleanly on their
Kubernetes", and it had no tracked vehicle: `digests/55.digest.md` pointed at #56 and a
"workplan §7", but #56 is DOG/Switchboard registration and no workplan document exists in
this repo.

Procedure: `docs/k8s_acceptance_runbook.md`. Two columns per repo must hold:

(1) the probes behave (startupProbe passes cold without flapping; livenessProbe does not fire during a routine rolling restart; readinessProbe reports not-ready within seconds)
(2) `kubectl rollout restart` drops no in-flight request.

Cannot be run by this repo's CI by design — the boundary is a published image run on their own cluster.

- [ ] page-classification
- [ ] alto-postprocess
- [ ] translator
- [ ] nlp-enrich
 - [ ] llm-enrich

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-09 14:48](https://github.com/ufal/atrium-project/issues/55#issuecomment-5603838959):

### Note from Sonnet 5

Re-verified this against the actual repo state (not just the plan notes): everything above is real and already merged — hub `a9fc268`, alto `1bd64d8`, translator `e731e55`, pc `f1df2c5`, llm-enrich `4747647`, nlp-enrich `3458831`+`3fa9934`. No unpushed branch, no open PR — it landed directly on each repo's default branch, and CI (`Docker Build & Publish`, `Paradata Canonical Drift`, `API Meta-Contract`, `Skill Branch Validation`) has been green on every push since, including the container smoke test noted above as unrun.

One trivial loose end: a stale, self-contradictory comment left in `atrium-llm-enrich/.github/workflows/docker.yml`'s `build-targets` line — no behavior impact.

Nothing left on our side.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-15 06:26](https://github.com/ufal/atrium-project/issues/55#issuecomment-5675765392):

### Status by agent pass — 2026-09-15

Re-verified independently; **everything on our side still holds, and nothing here is closable by us.**

Confirmed in the trees:
- `HEALTHCHECK` + `STOPSIGNAL SIGTERM` in all five `api` stages (alto `Dockerfile:112`/`:116-117`, nlp `:59`/`:84-85`, llm `:145`/`:169-170`, pc `:112`/`:135-136`, translator `:94`/`:118-119`), all five HEALTHCHECK lines byte-identical.
- `service/healthcheck.py` md5 `dc986ad6e6e6b1888d83bf18426cf300` and `service/atrium_service.py` md5 `d6a3b61fcc6cb6c4578193b0a57d5e99` — identical across the hub canonical and all five repos, and both now enforced as `MANIFEST.json` rows through the rewritten para-drift loop.
- `/ready` registered at `docs/templates/shared/atrium_service.py:248`, wired in all five services; the drain contract has a test in all five `tests/test_api_contract.py`.

**One thing changed shape since this issue was written.** The literal `--timeout-graceful-shutdown` flag no longer exists in any Dockerfile — #58 moved the budget to `ENV GRACEFUL_SHUTDOWN_S=20` (alto `Dockerfile:113`, nlp `:74`, llm `:159`, pc `:125`, translator `:108`), consumed as `timeout_graceful_shutdown=int(os.getenv("GRACEFUL_SHUTDOWN_S", "20"))` in each `__main__`. Behaviour is preserved and now tunable, but any future check grepping Dockerfiles for the flag will find only comments. `pc/tests/test_service_runtime_deps.py:267` already accepts either spelling.

**Still open, and still not ours**: the five acceptance checkboxes are all unticked. This issue closes on ARÚP/ARÚB's word.

**One loose end that has now survived two audits** and is a one-line deletion: `atrium-llm-enrich/.github/workflows/docker.yml:71-72` still says the `api` stage is "Still declared-but-unpublished" directly above `:73`, which builds it.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-16 15:56](https://github.com/ufal/atrium-project/issues/55#issuecomment-5700481183):

### Status summary by Opus 5

Our side is complete and unchanged: `HEALTHCHECK` + `STOPSIGNAL SIGTERM` in all five `api`
stages, `/health` and `/ready` wired, the drain contract tested in all five
`tests/test_api_contract.py`, and `docs/k8s_deployment.md` +
`docs/templates/k8s/atrium-service.deployment.yaml` + `docs/k8s_acceptance_runbook.md` published.

The loose end flagged on 09-15 is gone — `atrium-llm-enrich/.github/workflows/docker.yml` no
longer claims the `api` stage is unpublished.

One thing to carry into the acceptance round: the graceful-shutdown budget is now
`ENV GRACEFUL_SHUTDOWN_S=20` (#58), not the old `--timeout-graceful-shutdown` flag. Behaviour is
the same and now tunable per deployment.

### Remaining — external only

ARÚP/ARÚB run `docs/k8s_acceptance_runbook.md` against the published images and tick the five
boxes. Nothing left on our side; this issue closes on their word.


-------------------------------------------------------------------------------



[Export of Github issue for [ufal/atrium-project](https://github.com/ufal/atrium-project). Generated on 2026.09.19 at 14:36:40.]
