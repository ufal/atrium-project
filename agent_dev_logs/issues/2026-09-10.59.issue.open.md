# [\#59 Issue](https://github.com/ufal/atrium-project/issues/59) `open`: CODEBASE: bring agent skill's SHARED_FILES up to the para-drift guard set
**Labels**: `enhancement`, `development`, `github_actions`


#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) opened issue at [2026-09-10 12:08](https://github.com/ufal/atrium-project/issues/59):

What is left is the **second** guardrail. `tools/skill_drift_check.py:98-104` declares:

```python
SHARED_FILES = (
    "atrium_paradata.py",
    "atrium_document.py",
    "atrium_document.schema.json",
    "para_licenses.py",
    "service/atrium_service.py",
)
```

Five files. `para-drift.reusable.yml` guards **thirteen** — the five above plus `tests/test_para_licenses.py`, `tests/test_document_originators.py`, `service/healthcheck.py`, `atrium_vocab.py`, `atrium_vocab.schema.json`, `tests/test_atrium_vocab.py`, `atrium_rocrate.py`, `tests/test_atrium_rocrate.py`, `check_version.py`. So the skill-side drift check reports parity over a set that has not grown since before #51, #54 and #55 landed their canonical files.

This is the exact pattern `docs/docker_gha_roadmap.md` §2.2 names as why drift returns: the guardrail cannot see what it is meant to hold, and reports green over the part it can.

### Scope

1. Extend `SHARED_FILES` to the full para-drift set, keeping the `service/`-prefix and `port_closure` logic at `tools/skill_drift_check.py:322-328` working for the new entries.
2. Derive the list from one place if cheap — three files (`para-drift.reusable.yml`, `scripts/revendor_shared.sh`, `tools/skill_drift_check.py`) now enumerate the same canonical set by hand, and each addition since #51 has updated some but not all of them. A shared manifest turns the next omission into a diff instead of a silent gap.
3. If a single manifest is too much for this issue, add the cross-reference comment each file's neighbour already carries, so the next canonical file has three visible obligations rather than one.

### Acceptance

- `tools/skill_drift_check.py` reports a finding when **any** para-drift-guarded file is out of parity in one tool repo. Verify by **breaking it**, not by reading the source — modify `atrium_rocrate.py` in one tool repo and confirm both guards go red. This ecosystem has twice been caught by a green check that was measuring something else.
- Adding a fourteenth canonical file requires editing one list, or fails loudly until all three are updated.




-------------------------------------------------------------------------------



[Export of Github issue for [ufal/atrium-project](https://github.com/ufal/atrium-project). Generated on 2026.09.11 at 16:41:40.]
