# [\#53 Issue](https://github.com/ufal/atrium-project/issues/53) `open`: Follow best practices in all repos - `12 Factor` rules
**Labels**: `enhancement`, `development`


#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) opened issue at [2026-09-06 12:36](https://github.com/ufal/atrium-project/issues/53):

### The Twelve Factors

[I. Codebase](https://12factor.net/codebase)
 - One codebase tracked in revision control, many deploys

[II. Dependencies](https://12factor.net/dependencies)
 - Explicitly declare and isolate dependencies

[III. Config](https://12factor.net/config)
 - Store config in the environment

[IV. Backing services](https://12factor.net/backing-services)
 - Treat backing services as attached resources

[V. Build, release, run](https://12factor.net/build-release-run)
 - Strictly separate build and run stages

[VI. Processes](https://12factor.net/processes)
 - Execute the app as one or more stateless processes

[VII. Port binding](https://12factor.net/port-binding)
 - Export services via port binding

[VIII. Concurrency](https://12factor.net/concurrency)
 - Scale out via the process model

[IX. Disposability](https://12factor.net/disposability)
 - Maximize robustness with fast startup and graceful shutdown

[X. Dev/prod parity](https://12factor.net/dev-prod-parity)
 - Keep development, staging, and production as similar as possible

[XI. Logs](https://12factor.net/logs)
 - Treat logs as event streams

[XII. Admin processes](https://12factor.net/admin-processes)
 - Run admin/management tasks as one-off processes


Follow these principles if possible in repos:

- https://github.com/ufal/atrium-page-classification
- https://github.com/ufal/atrium-translator
- https://github.com/ufal/atrium-alto-postprocess
- https://github.com/ufal/atrium-nlp-enrich
- https://github.com/ufal/atrium-llm-enrich
- https://github.com/ufal/atrium-project



#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-09-09 17:36](https://github.com/ufal/atrium-project/issues/53#issuecomment-5606131359):

### Current state — by Opus 5

| Factor | | Measured |
|---|---|---|
| I. Codebase | 🟡 | 11 canonical files held byte-identical by `para-drift` (re-verified by hash). But `atrium_rocrate.py` (landed 09-09) is vendored 5/5 and declared in `scripts/revendor_shared.sh` — whose `SELFTESTS` comment says it "mirrors the `--selftest` steps in para-drift.reusable.yml" — and `.github/` contains **zero** occurrences of "rocrate". |
| II. Dependencies | 🟢 | **B1 closed ecosystem-wide** — `uvicorn` now in all five `service/requirements.txt`. Left: `flexiconv @ git+…` unpinned (nlp, llm), layoutreader clone unpinned (`alto/Dockerfile:42`). |
| III. Config | 🟡 | 178 env-read sites, ~40 service vars — but **one** `.env.example` in the ecosystem, and `docs/k8s_deployment.md` contains the string "env" zero times. |
| IV. Backing services | 🟡 | Two asymmetries inside the same repos: `call_udpipe.py:73` posts to a module constant with no `--url`/env, while `call_nametag.py:203` has both; translator's LINDAT URLs are class constants while its LLM backend is fully env-driven. |
| V. Build/release/run | 🔴 | **B9 unchanged.** `e2e-pipeline-smoke.yml:274` still `pip install`s `alto-tools` from `refs/heads/master.zip` *inside the published image at run time*, against the commit pin. Entrypoint still overridden on every stage. |
| VI. Processes | 🟡 | 4 of 5 properly stateless (`TemporaryDirectory` per request). nlp-enrich `/jobs` holds a process-local `_jobs` dict (`service/jobs.py:19`) + container-local workspaces. |
| VII. Port binding | 🔴 | See below. |
| VIII. Concurrency | 🟡 | Replica model is right; VI makes `replicas > 1` wrong for one service, and nothing says so. |
| IX. Disposability | 🟢 | **Closed by #55**, merged on all six default branches. Only the external ARÚP/ARÚB acceptance round remains. |
| X. Dev/prod parity | 🟡 | All images 3.11; `translator/scheduled-smoke.yml:51` is the only 3.12 lane. B7 (`.yml` vs `.yaml` compose header) still live in nlp-enrich. |
| XI. Logs | 🟡 | alto is the model (`text_inference.py:51-55`). `pc/service/inference.py:22` calls `logging.basicConfig()` at import in a library module; `nlp/service/enrichment.py:234` writes logs to a file. |
| XII. Admin processes | 🟢 | One-off tasks run from the same image and codebase. |

### Three things that need a decision

**1. Factor VII contradicts our own handover document.** `docs/templates/k8s/atrium-service.deployment.yaml:54-56` — the manifest ARÚP/ARÚB deploy from — sets `env: PORT=8000`. Only alto honours it (`Dockerfile:107` → `text_api.py:549-550`). The other four bake `--port 8000` into an **exec-form** `ENTRYPOINT` array (nlp `:62`, translator `:67`, llm `:147`, pc `:113`), where no shell exists to expand `$PORT`. Either make them read it, or drop it from the manifest — but not both as-is, ahead of the acceptance round in `docs/k8s_acceptance_runbook.md`.

**TODO**: make all repos follow alto-postprocess example

**2. B9 is the last live §2.1 breakage.** A release artifact that re-resolves its own pinned dependency at run time. #55's `docker-build-smoke` (`load: true` + `probe-targets`) now provides the mechanism to retire the entrypoint overrides too.

**TODO**: retire the entypoint, if needed, just copy the required files and only the used functionality + reference it in docs clearly

**3. `atrium_rocrate.py` is a canonical shared file with no guard, on day one.** Adding the `para-drift` parity + selftest steps costs minutes now and a reconciliation later. This is the exact "guardrail can't see what it's meant to hold" pattern `docker_gha_roadmap.md` §2.2 names.

**TODO**: add drift check

### Next steps

V and VII are the next two candidates to be worked on as a separate issue with specific acceptance criteria

Follow decisions above


-------------------------------------------------------------------------------



[Export of Github issue for [ufal/atrium-project](https://github.com/ufal/atrium-project). Generated on 2026.09.10 at 14:27:36.]
