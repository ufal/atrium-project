# 📓 Digest — [\#60](https://github.com/ufal/atrium-project/issues/60) `open`: CONFIG: publish the env-var contract — `.env.example` per repo and an env section in `k8s_deployment.md`

> _Condensed from [`../issues/2026-09-10.60.issue.open.md`](../issues/2026-09-10.60.issue.open.md) — **4 comments** (2026-09-10 13:04 ordering constraints · 2026-09-11 16:55 four re-measured findings · 2026-09-14 08:22 "contract delivered, CI red" · 2026-09-14 11:37 "five residual fixes"). Carved out of [\#53](https://github.com/ufal/atrium-project/issues/53), factor III, plan item F._
> _Verification passes: **2026-09-10** (first inventory) · **2026-09-11** (re-measured after #58, #61, #63 landed) · **2026-09-14 morning** (the "red in CI" edition) · **2026-09-14, this refresh** (re-measured against hub `f675943`, alto `f6d9196`, nlp `bfabfca`, llm `5ac82a5`, pc `f11936f`, translator `d0c1572` — in all six the default branch equals `test` equals the measured tree)._

**Labels**: `enhancement`, `development` · **Parent**: [\#53](https://github.com/ufal/atrium-project/issues/53) (factor III) · **Was blocked on**: [\#58](https://github.com/ufal/atrium-project/issues/58), [\#61](https://github.com/ufal/atrium-project/issues/61), [\#63](https://github.com/ufal/atrium-project/issues/63) — all cleared.

**Status**: 🟡 **All three acceptance bullets are met, and CI is green everywhere — but this issue is not closable, because a deliverable the thread reports as delivered was never delivered.** The two items the 09-14 comments left open are both discharged: the probe fix landed at `daf230c` and `v1` was retagged to `f675943`, which contains it, so all five repos' `Docker Build & Publish` runs are green on their current heads against `docker-tool.reusable.yml@v1`. Against that, the 11:37 comment's first bullet — *"Renamed; all 14 references now resolve"* — is false. The template is still `docs/templates/.env.example.template`, and **22 references across all six repos point at an undotted path that does not exist**, including the clickable partner-facing link this issue put in `docs/k8s_deployment.md:127`. That link is a 404 on GitHub today.

## 🎯 Goal

Publish the environment-variable contract the six repos had always had and never written down: a complete, machine-checked `.env.example` ledger per tool repo, and a curated partner-facing section in `docs/k8s_deployment.md`, cross-referenced with the reference manifest so the two cannot drift silently. The issue opened against **one** `.env.example` in the whole ecosystem (alto's, 4 lines) and a deployment reference containing the string "env" zero times.

## 🧵 Thread trail

- **2026-09-09** — #53's twelve-factor sweep marks factor III 🟡 and batches it with X and XI as plan item **F**.
- **2026-09-10 12:11, K4TEL** — opened as #60. Body: 178 env reads across six repos, ~40 distinct service-layer variables, one 4-line `.env.example`, three acceptance bullets.
- **2026-09-10 13:04, K4TEL** — comment: ordering is #58 → #61 → #63 → #60. Hard constraint: **do not document `PORT` until #58 lands**, since `healthcheck.py` reads it and the server ignores it in four repos. `MAX_UPLOAD_BYTES` is a deprecated fallback, not a second knob. `atrium-nlp-enrich/annotation/env.example` already exists and must not be collided with.
- **2026-09-11 16:55, K4TEL** — comment: four findings. The deliverable drifted within 24 h (nlp's four wrong defaults); `.env` reached **zero** containers; the "reads under `service/`" acceptance rule is not implementable as written; a sixth enumeration exists in the five `service/README.md`. Sets the direction that shaped everything since: `.env.example` = complete machine-checked ledger, `k8s_deployment.md` = curated partner subset.
- **2026-09-14 08:22, K4TEL** — comment: contract delivered, CI red. The `v1` retag to `455ba9b` surfaced a `docker-tool.reusable.yml` probe failure on llm-enrich and nlp-enrich.
- **2026-09-14 11:37, K4TEL** — comment: five residual fixes. **Its first bullet is wrong** — see ✏️ below.
- **2026-09-14, this refresh** — both open items closed; one live defect found and not yet fixed.

## 🔎 Verified state — 2026-09-14

Every citation below was re-checked by `grep -n` / `sed -n` against the working trees named in the provenance line. No container was started (no Docker daemon in the measuring environment); every container-level claim cites a GitHub Actions run.

### Acceptance bullet 1 — five `.env.example` files, mechanically checked ✅

**Five files, 122–200 lines each**, where the issue found one of 4 lines:

| Repo                | Lines | `LOG_LEVEL` | `.env` gitignored | `env_file:` passthrough                               |
|---------------------|-------|-------------|-------------------|-------------------------------------------------------|
| alto-postprocess    | 151   | ✅           | `.gitignore:18`   | `docker-compose.yml:39-41`                            |
| nlp-enrich          | 159   | ✅           | `.gitignore:20`   | `docker-compose.yaml:49-51`                           |
| llm-enrich          | 169   | ✅           | `.gitignore:151`  | `docker-compose.yaml:91-93`                           |
| page-classification | 122   | ✅           | `.gitignore:13`   | `docker-compose.yml:46-48`                            |
| translator          | 200   | ✅           | `.gitignore:6`    | `docker-compose.yml:64-66` (+ batch service `:32-34`) |

All five declare `LOG_LEVEL`, the variable that was withheld from every deliverable until #61 verified green. All five carry the identical `env_file: [{path: .env, required: false}]` shape — `required: false` because `.env` is gitignored, so a fresh clone still starts.

### Acceptance bullet 2 — the doc section ✅

`docs/k8s_deployment.md` is 267 lines and carries two sections this issue created:

- `## Configuring the port and bind address` — **`:88`**
- `## Environment variables — one contract, and the defaults that disagree with it` — **`:122-211`**, containing **Table A** (common to all five, `:132`), the per-repo **`MAX_UPLOAD_MB`** table (`:169`), and **Table B** (per-repo operator deltas, `:181`). `## Known limits` follows at `:212`.

The runbook carries the two acceptance gotchas the 09-11 pass asked for: `MAX_UPLOAD_MB` at `docs/k8s_acceptance_runbook.md:23` and `ALLOWED_ORIGINS` at `:29`, with both reappearing in the worked example at `:94`.

The issue's complaint that this file contains the string "env" **zero** times is long over.

### Acceptance bullet 3 — findable without a source tree ✅ (with the 404 caveat below)

Table B's page-classification row (`docs/k8s_deployment.md:199`) is the test of whether this was done honestly: it reads **"None."** and then explains *why* — the service layer reads nothing beyond Table A — closing with *"A short entry here means few deployment knobs, not a gap."* That is the shape of a table someone can trust. The caveat is that one of the two cross-references a reader is invited to follow does not resolve (✏️ below).

### The guards ✅

| Guard                            | Size                             | Scope                                                                                                                                                    |
|----------------------------------|----------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------|
| `tests/test_env_contract.py`     | **662 lines, 12 test functions** | md5 `ac1469b49a6bb2822e12f3e8a0fae611`, **byte-identical in 7 places**: the hub, the canonical copy at `docs/templates/shared/`, and all five tool repos |
| `tests/test_env_doc_contract.py` | **275 lines, 3 test functions**  | hub-local, **not** vendored — checks manifest ↔ Table A ↔ Table B ↔ each repo's `.env.example`                                                           |

The twelve tests are `test_env_example_documents_every_variable_the_code_reads` (`:478`), `…_documents_nothing_imaginary` (`:495`), `test_declared_omissions_are_still_read` (`:506`), `…_are_not_also_published` (`:523`), `test_consumed_elsewhere_entries_are_published` (`:534`), `…_are_not_read` (`:542`), `test_every_declaration_carries_a_reason` (`:552`), `test_the_shared_core_is_in_the_scan` (`:568`), `test_env_example_defaults_match_the_code` (`:585`), `test_dot_env_is_gitignored` (`:601`), `test_service_readme_is_a_subset_of_env_example` (`:639`) and `test_service_readme_defaults_match_the_code` (`:651`).

Parity is guaranteed through **#59's `MANIFEST.json` loop**, not a per-file step. The manifest row (16 rows total) reads `"canonical": "test_env_contract.py"`, `"dest": "tests/test_env_contract.py"`, `"issue": 60`, and — uniquely among the sixteen — a non-empty `"preconditions": ["tests/env_contract_data.py"]`, noted as *"imports a repo-local declaration module that cannot be vendored alongside it."* That per-repo module exists in all five tool repos (alto 50 lines, nlp 50, llm 27, pc 25, translator 25) and deliberately **not** in the hub; the hub's copy therefore skips at import (`tests/test_env_contract.py:68`, *"an honest skip beats a vacuous pass"*). Enforcement lives in the five tool repos; the hub copy is a parity artifact.

Alto's wildcard declarations are at `tests/env_contract_data.py:35-36` — `"ATRIUM_TEXT_UTILS_*"` and `"ATRIUM_CLASSIFY_*"`, each with the reason *"algorithmic scoring constant tuned via setup/config.txt, not a deployment-varying knob."* That is #53's own out-of-scope boundary, correctly drawn.

### Both previously-open items — discharged ✅

1. **The probe fix landed.** `daf230c` *"edit docker tool GHA"*, a single-file 27-insertion patch to `.github/workflows/docker-tool.reusable.yml`, (i) widened the level allowlist — `LEVEL_ALLOWLIST='(DEBUG|INFO|WARNING|ERROR|CRITICAL)'` at **`:430`**, with `:425-429` naming llm-enrich's `OPENROUTER_API_KEY` and atrium-project#60 explicitly — and (ii) replaced the single immediate grep with `log_deadline=$((SECONDS + 15))` at **`:435`**, whose comment names nlp-enrich's KeyBERT/HF-Hub warmup race.
2. **`v1` was retagged.** `v1` → `f675943`, and `git merge-base --is-ancestor daf230c f675943` returns true.

**All five repos are green on their current heads against `docker-tool.reusable.yml@v1` = `f675943`:**

| Repo                | Run                                                                                        | Head      | Conclusion |
|---------------------|--------------------------------------------------------------------------------------------|-----------|------------|
| alto-postprocess    | [34838653518](https://github.com/ufal/atrium-alto-postprocess/actions/runs/34838653518)    | `f6d9196` | ✅ success  |
| nlp-enrich          | [34838487713](https://github.com/ufal/atrium-nlp-enrich/actions/runs/34838487713)          | `bfabfca` | ✅ success  |
| llm-enrich          | [34838754391](https://github.com/ufal/atrium-llm-enrich/actions/runs/34838754391)          | `5ac82a5` | ✅ success  |
| page-classification | [34838971713](https://github.com/ufal/atrium-page-classification/actions/runs/34838971713) | `f11936f` | ✅ success  |
| translator          | [34865992804](https://github.com/ufal/atrium-translator/actions/runs/34865992804)          | `d0c1572` | ✅ success  |

The two red runs the 08:22 comment reported — llm-enrich [34810268819](https://github.com/ufal/atrium-llm-enrich/actions/runs/34810268819) and nlp-enrich [34810764880](https://github.com/ufal/atrium-nlp-enrich/actions/runs/34810764880) — both resolved `@v1` to `455ba9b`, the pre-fix tag. Every run listed above resolves it to `f675943`. The diagnosis in that comment was right and the remedy worked.

### The configuration surface, re-measured

The issue body's "178 places (alto 50 · nlp 47 · translator 30 · llm 29 · pc 11 · hub 11)" is stale. **Methodology**, stated so it can be re-run rather than trusted: `git grep -o -E "os\.getenv\(|os\.environ"` over tracked `*.py` in each repo, counting textual occurrences.

| Repo                | Excluding `tests/` | All tracked `.py` |
|---------------------|--------------------|-------------------|
| hub                 | 17                 | 25                |
| alto-postprocess    | 45                 | 58                |
| nlp-enrich          | 54                 | 61                |
| llm-enrich          | 34                 | 41                |
| page-classification | 15                 | 23                |
| translator          | 40                 | 51                |
| **Total**           | **205**            | **259**           |

Two caveats that matter more than the number. This counts **occurrences, not distinct variables** — the contract is ~40 service-layer names, not 205. And it **undercounts the real surface**, because alto's ~95 `ATRIUM_TEXT_UTILS_*` / `ATRIUM_CLASSIFY_*` names are composed as f-strings inside `_get_float`/`_get_int`/`_get_str`/`_get_csv_set` helpers and never appear at a literal `os.getenv` call site. No grep finds them; the resolver in `test_env_contract.py` does. Any future citation of a flat count should say which of the two it means.

## ✏️ Corrections to this digest's own previous edition

The 09-14 morning edition was written in the same hour as the events it describes, and six of its statements no longer hold. Each is quoted, then corrected.

1. **The headline, and the first thing a reader saw.** It opened *"🟡→🟢-shaped, **but currently red in CI**"* and carried a `## 🔴 New finding: the container-probe step has been red since the retag`. **Stale.** The fix landed at `daf230c`, `v1` moved to `f675943`, and all five repos are green — the table above. The whole red section is now history, and it is kept here only as the thread trail entry for 08:22.

2. **The one thing it said was still open.** *"The one thing still open is the retag: `v1` still resolves to `455ba9b`, two commits behind `daf230c`."* **Discharged.** `git rev-parse v1` → `f6759433fc62672308a7c74dc344f235c05c0fbf`.

3. **The template rename — this one got worse, not better.** The previous edition claimed the file *"has been renamed to match, which is the only change fourteen call sites needed,"* and the 11:37 comment repeated it on the public thread as *"Renamed; all 14 references now resolve."* **Not done.** `git ls-files docs/templates/ | grep env` returns `docs/templates/.env.example.template` — still dotted — and `docs/templates/env.example.template` does not exist on disk. See 🔴 below; this is the live defect.

4. **The guard's size.** *"`tests/test_env_contract.py` (520 lines)."* **Now 662 lines, 12 test functions**, byte-identical across seven copies (md5 `ac1469b49a6bb2822e12f3e8a0fae611`).

5. **The parity citation.** *"`para-drift.reusable.yml` gained a parity step (`:144-156`, "issue #60")."* **Stale citation, intact guarantee.** #59 replaced the per-file steps with the `MANIFEST.json` loop; the file is now **90 lines**, and `grep -n "issue #60"` returns nothing — `grep -c "issue"` returns **0**. Parity still holds, via the manifest row quoted above. The guarantee survived; the evidence for it moved.

6. **Two `.env.example` sizes.** *"alto 139 lines … translator 174."* **Now 151 and 200.** The other three (nlp 159, llm 169, pc 122) were right.

7. **The header.** *"**0 comments, opener only**."* **Four comments**, the last two of them the ones this refresh had to adjudicate.

8. **A hand-off that closed elsewhere.** The Open section listed *"`docs/templates/shared/test_atrium_service.py` still registered in none of the three places `revendor_shared.sh`'s own header requires."* **Resolved by #59.** That path no longer exists; the file is now `/home/user/atrium-project/tests/test_atrium_service.py` (tracked as `tests/test_atrium_service.py`), a hub-local test rather than a shared file, which is why `grep test_atrium_service scripts/revendor_shared.sh` still returns nothing — correctly, now.

## 🔴 The live defect — 22 references to a path that does not exist

The 11:37 comment's first bullet asserts a rename that did not happen. The tracked path is **`docs/templates/.env.example.template`**, with a leading dot. Every reference in the ecosystem spells it **without** the dot, so every one of them is broken. Re-verify with:

```
git -C /home/user/atrium-project ls-files docs/templates/ | grep env
```

**22 occurrences on 20 lines across all six repos** (counts exclude this digest and its plan, which are records rather than references):

| Repo       | Site                                                                          | Kind                                                                                                      |
|------------|-------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------|
| hub        | `docs/k8s_deployment.md:127`                                                  | **Clickable markdown link, partner-facing — 404 on GitHub.** Path appears twice (link text + target)      |
| hub        | `docs/templates/skill/serviceREADME.template.md:88`                           | **Clickable absolute `blob/main/` URL — 404.** Path appears twice. Seeds every future `service/README.md` |
| hub        | `docs/agent_skill_strategy.md:172`                                            | Prose, names it "the normative artifact"                                                                  |
| hub        | `docs/agent_skill_strategy.md:712`                                            | Prose                                                                                                     |
| hub        | `docs/templates/k8s/atrium-service.deployment.yaml:64`                        | Manifest comment: *"…fixes their layout"*                                                                 |
| hub        | `docs/templates/.env.example.template:93`                                     | **The file misnames itself**                                                                              |
| hub        | `docs/templates/shared/test_env_contract.py:586`                              | Canonical copy, `Rule 1` docstring                                                                        |
| hub        | `tests/test_env_contract.py:586`                                              | Vendored copy                                                                                             |
| alto       | `.env.example:8` · `service/README.md:410` · `tests/test_env_contract.py:586` | Ledger header, README, guard                                                                              |
| nlp        | `.env.example:12` · `tests/test_env_contract.py:586`                          | Ledger header, guard                                                                                      |
| llm        | `.env.example:9` · `tests/test_env_contract.py:586`                           | Ledger header, guard                                                                                      |
| pc         | `.env.example:8` · `service/README.md:320` · `tests/test_env_contract.py:586` | Ledger header, README, guard                                                                              |
| translator | `.env.example:8` · `tests/test_env_contract.py:586`                           | Ledger header, guard                                                                                      |

Two things make this worse than a typo. The two **clickable** links are exactly the ones a partner follows from the deliverable this issue exists to produce — acceptance bullet 3 says a reader can find every knob without opening a source tree, and one of the two cross-references offered to them dead-ends. And the six `tests/test_env_contract.py` copies are byte-identical, so the wrong spelling is now vendored six times over: fixing it means a revendor, not an edit.

The fix is one `git mv` plus zero reference edits — all 22 already use the spelling the renamed file would have. That is what the 11:37 comment described, and it remains true; it just was not performed.

## ⏳ Open

- **🔴 Rename the template.** `git mv docs/templates/.env.example.template docs/templates/env.example.template`, revendor `test_env_contract.py` to all five repos (the docstring text does not change, but the file must be re-synced if the canonical copy is touched for any other reason), and **post a correction on the thread** — the 11:37 comment stands on a public issue asserting this is done, and the next reader will believe it.
- **🟡 Do not close this issue until that rename is on `test`.** All three acceptance bullets verify, both CI items are discharged, and the guards are real — but closing now would bury a 404 in the partner-facing deliverable under a green checkmark.
- **Stale citation to repair in passing:** anything still pointing parity at `para-drift.reusable.yml:144-156` should point at the `MANIFEST.json` row (`docs/templates/shared/MANIFEST.json`, the `test_env_contract.py` entry with its `preconditions`).
- **Named, deliberately not folded in (unchanged since 09-11):** `LANGID_CONFIG`'s two incompatible default shapes across nine alto sites; `MAX_WORKERS` overriding two independent config keys. Alto-internal, batch-layer, own issue.
- **`RELOAD` wiring** — roadmap B8, out of scope by design.
- **`atrium-nlp-enrich/annotation/env.example`** — the second template the 09-10 comment named. Different stack, different audience, left alone.

---
_Digest of [ufal/atrium-project#60](https://github.com/ufal/atrium-project/issues/60). Sub-issue of [\#53](https://github.com/ufal/atrium-project/issues/53) (factor III). Strategy: [`../plans/60.plan.md`](../plans/60.plan.md). Measured against hub `f675943`, alto `f6d9196`, nlp `bfabfca`, llm `5ac82a5`, pc `f11936f`, translator `d0c1572` — in all six, the default branch equals `test` equals the measured tree. This pass supersedes the 09-14 morning edition, whose "red in CI" headline is spent, and contradicts the 11:37 thread comment, whose claimed rename was never performed._
