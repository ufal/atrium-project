# 📓 Digest — [\#57](https://github.com/ufal/atrium-project/issues/57) `open`: Create GH Pages for all repos

> _Condensed from [`../issues/2026-09-09.57.issue.open.md`](../issues/2026-09-09.57.issue.open.md) — opener + 2 comments. Carries a six-repo checklist; the hub is one of the six, not just the tracker._
> _Thread: 2026-09-09 → 2026-09-21 · K4TEL, stranak._

**Labels**: `documentation`, `enhancement` · Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover
· **Assignees**: none

**Status**: 🟠 **Open, and now partly shipped — four of the six sites serve, the hub's is RED and
one was never switched on.** Two rounds of "design only, nothing implemented" ended on 2026-09-18/19
when the drafts and the five static landing cards landed, and again on 2026-09-21 when an org owner
switched Pages on for the hub and pointed it at the wrong source. **Every premise in the previous
edition of this digest about the ecosystem having no Pages is now false**; they are listed in
_Corrections to the 2026-09-17 edition_ below rather than quietly overwritten.

Implementation plan in [`../plans/57.plan.md`](../plans/57.plan.md).

| Repository                   | `gh-pages` | Pages     | Source                  | Last build        |
|------------------------------|------------|-----------|-------------------------|-------------------|
| `atrium-project` (hub)       | ⚠️ absent  | on 09-21  | **`main` / `/docs`** ⚠️ | ✅ green, *fenced* |
| `atrium-page-classification` | ✅ 09-19    | on 09-19  | `gh-pages` / root       | ✅ success         |
| `atrium-alto-postprocess`    | ✅ 09-19    | ❌ **off** | —                       | — **never built** |
| `atrium-translator`          | ✅ 09-19    | on 09-19  | `gh-pages` / root       | ✅ success         |
| `atrium-nlp-enrich`          | ✅ 09-19    | on 09-19  | `gh-pages` / root       | ✅ success         |
| `atrium-llm-enrich`          | ✅ 09-19    | on 09-21  | `gh-pages` / root       | ✅ success         |

## 🔴 The hub's build: the failure, and the fence that resolved it

[Run 35582651532](https://github.com/ufal/atrium-project/actions/runs/35582651532), 2026-09-21 09:19
UTC, `actions/jekyll-build-pages@v1` with `source: ./docs`:

```text
Liquid Exception: Liquid syntax error (line 79): Variable
'{{ matrix.target != 'base' && format('-{0}'  was not properly terminated
with regexp: /\}\}/  in docker_gha_roadmap.md
```

**Jekyll runs Liquid over all markdown — inside fenced code blocks and inline code spans too.** That
is the finding: the corpus is full of quoted GitHub Actions expressions, and to Liquid they are
template variables. Nine occurrences across two hub files — `docs/docker_gha.md:213` and
`docs/docker_gha_roadmap.md:79,80,92,99,190,252,414,437`. Only `:79` hard-fails (the `{0}` inside
`format('-{0}', …)` swallows the closing `}}`); the other eight would render as empty strings,
silently gutting the two documents that explain the ecosystem's Docker and CI story.

**And the failure was the only thing holding §F's exclusion list.** `/docs` is the hub's canonical
internal tree. A *green* Jekyll build from it publishes `docs/arub-p_contacts.md` — **five ARÚP/ARÚB
partner email addresses** — plus `docs/plan_repo_review.md` (a personal address, self-declared stale)
and the 97 KB internal `docker_gha_roadmap.md`. The failing log **already reaches**
`Rendering: arub-p_contacts.md`; it dies four files later. Nothing was leaked **by accident, not by
design** — so escaping the Liquid on its own would have turned the build green and published the
partner list.

### The folder moved, and the fence had to move with it — run 35612083327

**Later on 2026-09-21 the Pages folder was changed from `/docs` to `/ (root)`.** The new run reports
`Source: /github/workspace/.` and **`Configuration file: none`** — Jekyll reads the `_config.yml`
beside its source, so `docs/_config.yml` stopped being consulted the moment the dropdown moved. The
fence was bypassed, not broken.

The blast radius went up with it: from 13 files plus `templates/` to **every tracked file**, including
all 79 of `agent_dev_logs/` — which §E says are not published at all, and which include 49 issue
exports, several of them open memos to named individuals. Addresses went from 2 files to **5**; the
three the docs-scoped fence never reached are `digests/project_state_1307.md`,
`digests/project_state_2706.md` and `issues/2026-06-12.21.issue.open.md`. Liquid-carrying files went
from 2 to **9**.

**One of those nine is `PAGES_SETUP.md`, at line 66 — written in the previous round of this issue.**
It quotes the failing Liquid expression verbatim in a fenced block, so documenting the error
reproduced it. That is the same defect class twice in one day, and it is worth recording as evidence
of how easy this trap is: the corpus is *about* CI, so it is full of `${{ … }}`, and every markdown
file in it is a loaded gun under a Jekyll build.

`_config.yml` at the repository root now excludes `agent_dev_logs`, `docs`, `docs_site`, `INDEX.md`,
`PAGES_SETUP.md`, `fixtures`, `tests`, `tools`, `scripts`, `mkdocs.yml` and `ruff.toml` — Jekyll skips
`.`/`_` entries itself. **`README.md` is the only thing published**, and `jekyll-readme-index` makes it
the site index, so <https://ufal.github.io/atrium-project/> serves something true at last and the
**20 hub-root links** on the five stub cards resolve. Both fences are kept: the dropdown has been set
both ways in one day, and neither config depends on the other.

`tests/test_docs_pages_exclude.py` is renamed `tests/test_pages_exclude.py` and now checks both, and
compares against `git ls-files` rather than the filesystem — the builder sees a fresh clone, so a
gitignored artifact like `site/` is not there. It caught that itself on the first run.

### The first fence, 2026-09-21 (source `/docs`)

`pages-build-deployment` is GitHub's built-in job, not a workflow file in this repository, so it
cannot be edited. The only two levers over it are **what lives under `docs/`** and **where the Pages
source points**. `docs/_config.yml` is the first lever: it excludes the three sensitive/internal files,
plus `docker_gha.md` (its one `{{version}}` resolves to the empty string rather than raising — the
page would ship silently claiming `type=semver,pattern=`) and `templates/` (vendored canonical code
and «placeholder» skeletons, not documentation). **No source file was edited.** The markdown is
correct; Jekyll is the wrong tool for this corpus, and escaping `{{` would have corrupted how GitHub
renders those files in the repository browser.

Nine files still publish, all checked clean of Liquid and of any address:
`agent_skill_strategy` · `document_schema` · `k8s_acceptance_runbook` · `k8s_deployment` ·
`paradata_schema` · `rocrate_export` · `skill_acceptance_runbook` · `skills_catalog` · `skos_strategy`.

**This is §F's "explicit, tested step", arriving early and under duress.**
`tests/test_docs_pages_exclude.py` is bidirectional in the #59 sense — every exclusion must name a
file that exists, and every file under `docs/` must be either excluded or on a reviewed allow-list —
plus two content assertions that would each have caught this incident: no publishable file may carry
a Liquid construct (the crash), none may carry a partner address (the leak). Its scoping follows §F's
warning: **the two ARÚP/ARÚB domains, not "any email address"**, because the tool repos publish a
maintainer address deliberately and a test that fails for the wrong reason gets disabled. All six
checks were deliberately broken and confirmed to fail before being trusted.

**Both files are stopgaps and should be deleted, not maintained**, the moment an owner points
Settings → Pages at `gh-pages` / `/ (root)`.

**Reproduced and fixed locally, not inferred.** `jekyll 3.10.0` / `liquid 4.0.4` — the versions the
Pages image pins — reproduce the error byte-for-byte, and the fenced build exits 0 with the excluded
files absent from the output. The reproduction needs `jekyll-optional-front-matter`, which GitHub
Pages forces on: without it, front-matter-less `.md` files are copied as static assets, Liquid never
runs, and a plain `jekyll build` passes while hiding the bug entirely.

## 🎯 Goal
Turn the ecosystem's markdown into published GitHub Pages — drawing on README content, the
documentation scattered through non-obvious folders, and an `agent_dev_logs` summary per repo — wired
together as **one interlinked system** rather than six unrelated sites. The 2026-09-17 TODO adds three
requirements: de-duplicate the repeating blocks, give the hub a page explaining the **external tools
and services** a newcomer would not recognise, and give it a page of **in-detail pipeline narratives**.

## 🧵 Thread trail
- **2026-09-09, K4TEL** — opened with the six-repo checklist and three requirements: go beyond the
  README, include an `agent_dev_logs` summary per repo, and *design a system of interlinked pages*.
  No generator, hosting model or scope boundary stated.
- **2026-09-17, Opus 5** — status comment: design refreshed against the tree, nothing built, five
  corrections folded into the plan (reuse #59's manifest pattern, `vit` as a prerequisite, tighter
  permissions, the hub having no `CONTRIBUTING.md`, the stale `README.html`).
- **2026-09-17, K4TEL (TODO)** — three additions, all of which change the shape of the design rather
  than adding pages to it:
  1. *"consider removal of repeating blocks or separation of common docs into a single one used
     across all repositories"*;
  2. *"a separate page should exist for a description of the external tools/services/infrastructure
     used in the project — SKOS, RO-Crate, LINDAT, HF, etc. — anything that a new user might be
     unfamiliar with"*;
  3. *"a page for in-detail narrative descriptions of the developed processing pipelines … all
     workflows should be depicted from beginning to end with a clear purpose and results stated."*
- **2026-09-18/19, K4TEL** — **the first thing actually built.** Commit `8bdc5b4 docs: added GH pages`
  put `mkdocs.yml`, `PAGES_SETUP.md`, `INDEX.md`, the 38 `docs_site/` draft shells and the seven
  `_generators/` scripts on the hub; `05f3a8b docs fixed tables` followed. Five orphan `gh-pages`
  branches were pushed, one per tool repo, each holding a static landing card. Comment: *"Made stubs
  of GHP for 4 repos… LLM-enrich repo tool and hub repo should be modified in settings to allow for
  GH Pages. Draft doc files added to hub."*
- **2026-09-21, stranak** — switched Pages on for `atrium-llm-enrich` (`gh-pages`, green at 09:18) and
  for `atrium-project` — the latter pointed at **`main` / `/docs`**, which failed at 09:19 and has
  been red since. `atrium-alto-postprocess` was not switched on and is still dark.
- **2026-09-21, Opus 5** — this round: the hub gets `.github/workflows/pages.yml` (MkDocs →
  `gh-pages`), the 40 sibling links on the five cards are repointed at the siblings' own published
  pages, and this digest and the plan are corrected against what shipped.

## 🔎 What's already known (surveyed against the tree, not from this thread)

**The corpus is large and badly reachable.** 212 markdown files, 49,476 lines, **4.15 MB**, effectively
navigable only by scrolling a README or browsing the file tree. Only a quarter of it is publishable
surface: excluding `agent_dev_logs/`, **56 files / 18,374 lines / 1.41 MB**.

| Repo                       |  all md |         bytes | publishable |         bytes | README | CONTRIBUTING |
|----------------------------|--------:|--------------:|------------:|--------------:|-------:|-------------:|
| atrium-project             |      98 |     1,371,436 |          19 |       388,993 |  1,094 |     *(none)* |
| atrium-page-classification |       7 |       188,650 |           3 |       173,099 | 87,938 |       62,223 |
| atrium-alto-postprocess    |      35 |     1,069,310 |          11 |       309,654 | 59,235 |       69,669 |
| atrium-translator          |      12 |       228,192 |           5 |       124,064 | 52,349 |       37,561 |
| atrium-nlp-enrich          |      36 |       878,552 |          10 |       253,282 | 90,154 |       55,575 |
| atrium-llm-enrich          |      24 |       415,437 |           8 |       164,485 | 36,280 |       32,455 |
| **total**                  | **212** | **4,151,577** |      **56** | **1,413,577** |        |              |

`agent_dev_logs/` holds the rest: **49 per-issue digests, 49 plans, 45 issue exports**, 6
`project_state_*` snapshots and six `DEVLOG.md` timelines (161 KB of curated chronology).

> **Re-measured 2026-09-21** at each repository's default branch (`main` / `vit` / `master` / `master`
> / `master` / `main`), which is what the site will read: **252 markdown files, 4,581,156 B (4.37 MB)**
> — hub 138 / 1,510,666 · page-classification 6 / 187,793 · alto 37 / 1,388,948 · translator
> 12 / 228,248 · nlp-enrich 34 / 844,186 · llm-enrich 25 / 421,315. The hub's jump (98 → 138) is this
> issue's own 38 draft shells plus `INDEX.md` and `PAGES_SETUP.md`; alto's (+320 KB) and nlp-enrich's
> (−34 KB) are ordinary drift in `agent_dev_logs/`. The **publishable-surface** column above has not
> been re-measured and is carried forward as of 2026-09-17.

### Nine structural findings that shape any solution

1. **The READMEs cannot simply be rendered.** `alto-postprocess`'s `## 🛤️ Workflow Stages` is **42,775 B
   — 72 % of its README** — and nests five heading levels; `nlp-enrich`'s LLM-enrichment section is
   29,907 B. Meanwhile `llm-enrich`'s README is already flat (17 `##`, none over 7,345 B). A single
   uniform split rule fits none of them.

2. **A naive `^## ` splitter shatters the hub's own pilot.** **218 heading-looking lines sit inside
   fenced code blocks** corpus-wide. At split depth that costs `docs/agent_skill_strategy.md`
   **5 phantom `##` and 3 phantom `###`** — the SKILL.md skeleton quoted in Appendix A — and
   `llm-enrich/README.md` one phantom `##`. The splitter must track fence state, and this bites the
   pilot repo before it bites any tool repo.

3. **Every CONTRIBUTING is mostly one table, and that table is the least shareable thing in the
   corpus.** `## 📦 Release History` is **62.4–81.1 %** of each file — **195,371 B, 75.9 % of all
   CONTRIBUTING bytes** — one 3-column table per repo with cells **799–1,694 characters** wide
   (alto: 40 release rows, 56,510 B). Rendered verbatim it is one horizontally-scrolling page.
   Transposed to one anchored `###` per version it yields **133 linkable releases** across the five.
   *It is 0 % shareable:* five distinct changelogs that must render five times.

4. **Footnotes are defined only at EOF** — 30 in nlp-enrich, 19 in page-classification, 9 in alto
   (numbering skips `[^3]`), 8 each in translator and llm-enrich. Any split orphans every `[^n]`
   reference in the file.

5. **The link-rewriting job is dominated by *intra*-file anchors, not inter-file links.** The
   publishable docs carry **92 relative links, 14 of them anchor-bearing**. But every tool README also
   carries a **hand-maintained Table of Contents** — 33 / 24 / 28 / 28 / 19 in-page anchors,
   **132 total** — which a split turns into cross-page links. Those TOC blocks then duplicate the
   generated nav and must be suppressed, not rendered.
   One link is **already broken**: `alto-postprocess/service/README.md:93` targets
   `../README.md#composite-quality-score`, but the only `## Composite Quality Score` heading is at
   `docs/categorization_logic.md:214` — the README merely links to it (line 619). Emoji headings make
   GitHub's slugs and a generator's slugs disagree, so anchors need an explicit map, not a convention.

6. **The duplication the TODO asks about is real but small, and concentrated in one place.**
   Measured, not assumed:
   - **READMEs: essentially nothing to share.** Whole-file similarity between any two of the five is
     **0.012–0.108**; exact shared blocks ≥80 B total **~2,400 B (0.7 %)** of 326 KB. There is no
     common install / Docker / env / licence section to centralise — most of those sections do not
     exist in most of the five.
   - **CONTRIBUTING: ~62 KB outside Release History, of which 17,636 B is exact duplication** across
     25 blocks ≥60 B. The clean wins: `## 🔁 Contributor Workflow` (**413 B, byte-identical ×4**),
     `## 📋 Pull Request Format` (**688 B, byte-identical ×4**), the 659 B commit-type table
     (**identical in all six, including the hub template**), `## 🌿 Branches & Environments`
     (~970–998 B ×5, similarity 0.75–0.91 — the only deltas are each repo's 3 example branch names and
     `master` vs `main`).
   - **Four files are byte-identical between nlp-enrich and llm-enrich** (**75,525 B**):
     `data_samples/vocab/RUNBOOK.md` (28,939), `prompts/RUNBOOK.md` (19,948) and the two decision
     packages (16,520 + 10,118). **The two decision packages are already excluded from publication**,
     so the recoverable figure is **48,887 B**.
   - **Total recoverable: ~67,700 B.** Worth doing — and worth saying plainly that it is *not* the
     "duplication everywhere" the corpus's size suggests.

7. **Not all of it is publishable.** `docs/arub-p_contacts.md` is 6 lines holding **5 personal email
   addresses of named ARÚP/ARÚB partners**; `plan_repo_review.md` carries the maintainer's personal
   email and self-declares as stale; two `nlp-enrich` decision packages are open memos addressed to
   named individuals; `llm-enrich/digital_born/README.md` calls itself a "Phase 0 exploration scratch
   space"; `tools/quality_model/EXPERIMENTS.md` is an unfilled results table; `docker_gha_roadmap.md`
   (97 KB) is an internal roadmap. Exclusion has to be an explicit, tested step.
   **But the exclusion grep must be scoped precisely:** all five tool READMEs and CONTRIBUTING files
   carry a contact address of their own (one shared maintainer address, **zero overlap** with the five
   partner addresses), and those files are published deliberately. A blanket "no email addresses in the
   built site" assertion fails on day one against content that is public by design.

8. **Assets are 94 % noise.** `atrium-page-classification` holds **266 image files (~59 MB)**; exactly
   **17 are referenced from markdown** (11 confusion-matrix plots under `result/plots/`, 6 figures
   under `supplementary/figures/`). The remainder is `small_data_samples/` training imagery. A
   directory copy would bloat the site ~20×; the site needs an allowlist derived from parsing image
   references. Those 11 plot references are also **URL-encoded** (`result%2Fplots%2F…`) — GitHub's
   renderer tolerates it, a static generator will not; there are 60 `result%2F` and 134 `%2F`
   occurrences in that README to decode.

9. **There is not one pipeline, and the one everybody draws is wrong.** Every diagram in the corpus —
   `plan_repo_review.md`, `skills_catalog.md`, `project_state_1307.md`, `fixtures/e2e/README.md` —
   draws five boxes with arrows between them. The file topology disagrees, and the disagreement is
   verifiable:
   - `nlp-enrich` reads `DOC_LINE_CATEG/` and the **original** `ALTO/` (`config_api.txt:2,15`), never
     the translator's output;
   - **no repo reads `TRANSLATED/`** — 0 hits across all four downstream repos. The translator is a
     **terminal branch**: its output is recorded in `derived_from.translated_xml` and read by nothing;
   - `alto-postprocess` never consumes `page_categories` (its only occurrences are in the vendored
     schema files, i.e. deep-copy pass-through), so page-classification → alto is a **human routing
     decision, not code**.

   What *is* linear is the **record**: `atrium_document` accretes block by block, each stage writing
   only its own block and deep-copying the rest. Only `docs/document_schema.md:16` states this, and
   only about the record, never about the file topology. Beyond the canonical OCR chain there are a
   dozen further workflows — the born-digital two-stage path (`e2e-digital-smoke.yml`), the
   containerised service path, agent skills, vocabulary harvesting, training/evaluation, alto's
   parameter sweep, the llm-enrich benchmark, flexiconv adaptation, annotation, and the RO-Crate
   export.

### Scope settled with the maintainer

READMEs and CONTRIBUTING files stay **full-length and canonical** (the sites are *additive*, nothing
moves); `agent_dev_logs` contributes **one history page per repo**, and — new this round — that page is
**derived from `DEVLOG.md`** rather than hand-written, so it cannot go stale the way
page-classification's header once did; the generator is **MkDocs Material**; de-duplication is
**site-only**, the sources are untouched; and the topology is **hybrid** — one aggregate hub site
carrying all six repos' documentation, plus a minimal stub site per tool repo that lands the reader in
that repo's section of the hub.

**The design consequence** of "nothing moves" is that pages must be *derived* at build time rather than
authored: a hub-canonical assembler slices the untouched sources into pages on every build, so the
split can never drift from the README it came from. **The design consequence of the hybrid topology**
is that every cross-repo link becomes *internal*, and therefore checkable — which is the whole reason
to prefer it. Both are the core of the plan.

## 🧩 What the 2026-09-17 TODO changes

1. **De-duplication is a measurement problem before it is a design problem.** Finding 6 says where the
   ~67,700 recoverable bytes are and, just as importantly, where they are not: no README machinery, no
   Release-History sharing. The plan builds dedup only where the bytes justify it.
2. **The external-tools page has ~60 entries in 7 categories.** About 60 % can lift existing prose —
   `skos_strategy.md`, `rocrate_export.md`, `translator/docs/translation-backends.md` and
   `k8s_deployment.md` are tutorial-grade. About nine are **used across the ecosystem and explained
   nowhere in it**: the Handle System / PID (the LINDAT handle is the project's primary dataset
   citation), LINDAT/CLARIAH-CZ as an institution, OAI-PMH, Dublin Core (`dcterms:` is emitted; the
   string "Dublin Core" appears in zero files), Korektor (a live API call in
   `tools/quality_model/correct.py`, absent from every Acknowledgements section and the licence
   inventory), **ARÚP / ARÚB / AISCR — acronyms never expanded anywhere** — the work packages and the
   DMP, the CI security stack (Trivy / CodeQL / SARIF / Codecov), and DOG / Switchboard.
3. **The pipelines page must correct the ecosystem's own diagrams,** per finding 9 — two layers, a file
   DAG and a record chain — and it has almost nothing to start from visually: **there is not one
   Mermaid block in the entire corpus.** Every diagram is ASCII box-drawing inside a fence (358 such
   lines in page-classification's README, 258 in alto's). The only real figures anywhere are
   page-classification's six `supplementary/figures/*.png`, of which `architecture.png`,
   `use-case.png`, `page-sort.png` and `dataset_timeline.png` are directly reusable.

## ✏️ Corrections to the 2026-09-17 edition

Ten claims. Nine were true when written and were falsified by what shipped on 09-18/19/21; **one was
wrong on its own terms and is the one that matters**, because the plan built a permissions strategy on
it.

| 2026-09-17 edition                                                                                                                              | Verified 2026-09-21                                                                                                                                                                                                                                                                                                                                                                                            |
|-------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ⚠️ *"`actions/configure-pages` with `enablement: true` is the only path — for every repo including the hub."*                                   | **Wrong, and it was wrong when written.** The action's `findOrCreatePagesSite` **GETs the existing Pages site first** and returns whatever it finds; it POSTs `build_type: workflow` only when that GET fails. On a repository where Pages is already enabled as a *branch* source it is a no-op, and `actions/deploy-pages` then fails because the site is not of type `workflow`. It cannot repoint the hub. |
| *"`has_pages` false on all six; no `gh-pages` branch or any other site scaffolding anywhere."*                                                  | **Five `gh-pages` branches exist** (pushed 09-19) and **five repositories have Pages on**. Only `atrium-alto-postprocess` is off, and it is off *with* its branch already in place.                                                                                                                                                                                                                            |
| *"the aggregate publishes from the hub's `main`"* (§A.3), *"all six publish from a `gh-pages` branch, folder `/ (root)`"* (`PAGES_SETUP.md`)    | The hub is set to **`main` / `/docs`** with the legacy Jekyll builder — neither of the two things the design specified. That is the whole of the reported breakage.                                                                                                                                                                                                                                            |
| No mention anywhere of Liquid                                                                                                                   | **A defect class the survey missed: Jekyll renders Liquid inside fenced code blocks and code spans.** Nine hits in two hub files, one fatal. Any branch-source Jekyll build over this corpus hits it; the fence-awareness work in §B.2 was scoped to *headings* and does not cover it.                                                                                                                         |
| §F: exclusion is *"an explicit, tested step"* to be built in round 3                                                                            | Exclusion is **the live blast radius**, and it is currently enforced by a build failure rather than by design. `Rendering: arub-p_contacts.md` appears in the failing log. The ordering constraint — source first, Liquid second — is now the guard, and it is written down in `PAGES_SETUP.md` and in `pages.yml`'s header rather than held in anyone's head.                                                 |
| §G: *"Stubs: `.github/workflows/pages-stub.reusable.yml` in the hub, with a thin `pages.yml` caller per tool repo"*                             | **Obsolete.** The stubs shipped as hand-written static cards on orphan branches. There is nothing for a reusable workflow to do: a static card needs no build, and five callers would be five more files to keep at `@v1`. Struck from the plan.                                                                                                                                                               |
| §B.3 / `mkdocs.yml:89-94`: the known-broken `#composite-quality-score` anchor is *"the cheapest possible regression test"* under `strict: true` | **`--strict` does not catch it.** MkDocs defaults `validation.links.anchors` to `info`, and `--strict` promotes *warnings*, not info. The claim was aspirational. Fixed by adding `validation: links: anchors: warn` to `mkdocs.yml` — three lines, which make the comment above them true.                                                                                                                    |
| The issue comment: *"Made stubs of GHP for 4 repos"*                                                                                            | **Five branches, four live sites** — and the four live ones were not the four the comment meant. `llm-enrich` (named in the comment as still needing settings) went live on 09-21; `alto-postprocess` (not named) never did.                                                                                                                                                                                   |
| `INDEX.md` lists `docs/site.yml`; and *"`_generators/` are not repository files and should not be committed as-is"*                             | **`docs/site.yml` is not in the repository** — `make_config.py` writes it into a scratch directory and it was never committed, so `nav ↔ docs_site/ ↔ docs/site.yml`, "38 = 38 = 38", had only two terms. **All seven `_generators/` files are committed.** Both corrected in `INDEX.md`.                                                                                                                      |
| Corpus: 212 files / 4.15 MB                                                                                                                     | **252 files / 4.37 MB** at the default branches — see the note under the corpus table. 38 of the 40 new hub files are this issue's own draft shells.                                                                                                                                                                                                                                                           |

## ✏️ Corrections to this digest's own previous edition

The 2026-09-16 edition's measurements have been re-taken; eight are wrong, one materially.

| Previous edition                                                                                         | Measured 2026-09-17                                                                                                                                                                                                                                                                                                                                                                    |
|----------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 223 markdown files, ~50,900 lines, **~2.9 MB**                                                           | **212 files, 49,476 lines, 4.15 MB.** The file count fell (#59 closed; #60–#64 digests and plans removed on 2026-09-17); the byte figure was simply wrong, by ~1.25 MB.                                                                                                                                                                                                                |
| 44 digests, 44 plans, 42 issue exports                                                                   | **49 digests, 49 plans, 45 issue exports**, + 6 `project_state_*` snapshots = 149 files.                                                                                                                                                                                                                                                                                               |
| Release History is **56–81 %** of each CONTRIBUTING; cells **728–1,335** chars; **13–42** releases       | **62.4–81.1 %**; cells **799–1,694** chars; **12–40** releases, 133 across the five.                                                                                                                                                                                                                                                                                                   |
| **77** relative links, **9** anchor-bearing                                                              | **92** relative links, **14** anchor-bearing — and the far larger job is the **132** intra-README TOC anchors the previous edition did not count at all.                                                                                                                                                                                                                               |
| "The five tool repos **vendor** `docs/templates/CONTRIBUTING.md`"                                        | **They do not.** That file is a 9,288 B skeleton with `«placeholders»`; it is **not** in `MANIFEST.json`, **not** in `revendor_shared.sh`, **not** diffed by `para-drift`. The five copies (32,455–69,669 B) have diverged freely — pairwise similarity never exceeds 0.57. The hub page renders the family standard *and must say the copies are unenforced*.                         |
| "`atrium-llm-enrich/scripts/revendor_shared.sh` was deleted on 2026-09-16"                               | **False — it exists**, git-tracked, 5,286 B, unmodified since 2026-09-07. It hand-maintains a **9-file** list against the **17-entry** `MANIFEST.json`, has no `--selftest` step, and **no CI looks at it** (the hub's guard test reads only the hub's own copy). It is a live instance of the defect #59 exists to prevent. The ⚡ note recorded an intention as an accomplished fact. |
| "page-classification's `DEVLOG.md` is factually stale — claims no digests/plans exist and 0 open issues" | **Already corrected.** Its header now reads *"1 open issue (#48; #15 and #26 closed)"* and explicitly notes *"This header previously claimed no such exports existed."* Also: `digests/49` never existed — only `48.digest.md` is present. Struck from Open.                                                                                                                           |
| "11 URL-encoded `result%2F…` image paths"                                                                | **11 is right for distinct image targets**; the decoding job is **60** `result%2F` and **134** `%2F` occurrences. Both figures kept — they measure different things.                                                                                                                                                                                                                   |

Two further premises were re-checked and **hold**: `has_pages: false` on all six; and the permission
picture — `admin: false` on **all six**, plus `maintain: false` on `atrium-project` and
`atrium-llm-enrich`, so `actions/configure-pages` is the only path for every repo including the hub.

One flag raised in survey and **stood down**: `docs/templates/workflows/update_issues.sh:92` exports a
`github_pat_…` literal, but it is an **elided placeholder** (it contains a literal `...`), already
analysed at length as R1 in `docker_gha_roadmap.md`. Not a usable credential; recorded here so it is
not re-raised as a new alarm each survey.

## ⏳ Open
- **One owner-level dropdown, twice.** `atrium-project` → Settings → Pages → Branch `gh-pages` /
  `/ (root)` (do it *after* `pages.yml` has run once on `main`, which creates the branch); and the
  same on `atrium-alto-postprocess`, whose branch has been waiting since 09-19.
  `actions/configure-pages` cannot substitute for either — see Corrections. The one remaining
  automation path, `PUT /repos/{owner}/{repo}/pages` with `pages: write`, was **not** attempted:
  `docs.github.com` is unreachable from the authoring environment, so the permission requirement
  could not be verified, and a CI-issued settings mutation on an unverified guess is worse than a
  dropdown.
- ~~Do not escape the Liquid until the hub's source has moved.~~ **Discharged 2026-09-21** by
  `docs/_config.yml`: the two files are excluded, so the ordering constraint no longer binds and the
  markdown stays as written. Neither file should be edited — the Liquid-looking strings are correct
  content, and escaping them would corrupt GitHub's own rendering of them.
- Still unbuilt from the plan: the assembler, `docs/site.yml`, `site_manifest.py` and the two test
  modules. The 38 `docs_site/` pages remain shells; `pages.yml` publishes them as shells, which is
  what makes the site exist at all.
- **`atrium-page-classification`'s default branch is `vit`.** Down another notch: the aggregate
  publishes from the hub's `main` and reads page-classification's docs at a named ref, and its stub
  publishes from the orphan `gh-pages` branch like the other four — so `vit` now appears in exactly
  two places, `repos.py`'s `default_branch` (which feeds the card's "the code lives on …" footer link,
  verified correct) and the ref the round-3 assembler will read. The previous edition said the stub
  "still publishes from `vit`, so the caller's trigger must name it"; there is no caller and no
  trigger. Desirable hygiene, own owner, not a #57 prerequisite.
- **`atrium-page-classification/README.html` (178,278 B, git-tracked, one README commit stale)** needs
  a delete-or-exclude decision before a generator emits a competing HTML tree.
- **The hub has no `LICENSE` and no `CITATION.cff`** — the five tool repos have both (MIT). A published
  site needs a footer licence statement the hub currently cannot make.
- **`atrium-llm-enrich/scripts/revendor_shared.sh`** needs a decision: delete it, or make it a thin
  wrapper over the hub's. Out of scope for #57, but it was mis-recorded here as already done.
- **`docs/templates/workflows/para-drift.caller.example.yml:7` says "currently 16 files"**; the manifest
  holds 17, and no test covers that file's prose count.
- See [`../plans/57.plan.md`](../plans/57.plan.md) for the topology decision, the assembler design, the
  site map, the two new hub pages and the rollout order.
