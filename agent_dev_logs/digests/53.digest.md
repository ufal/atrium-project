# 📓 Digest — [\#53](https://github.com/ufal/atrium-project/issues/53) `open`: Follow best practices in all repos - `12 Factor` rules

> _Condensed from [`../issues/2026-09-06.53.issue.open.md`](../issues/2026-09-06.53.issue.open.md) — **4 comments** (2026-09-09 measured sweep, 2026-09-10 correction + sub-issue map, 2026-09-15 and 2026-09-16 status passes). Confirmed live 2026-09-14._
> _Thread: 2026-09-06 · K4TEL. Cold cross-reference pass: 2026-09-07. Measured pass: 2026-09-09. **Re-measured pass: 2026-09-14** (this refresh)._

**Labels**: `enhancement`, `development` · Type: `Feature` · Milestone: WP4 — Pre-Closure Architecture Review
· **Assignees**: K4TEL · **Sub-issues**: [\#55](https://github.com/ufal/atrium-project/issues/55) · [\#58](https://github.com/ufal/atrium-project/issues/58) · [\#59](https://github.com/ufal/atrium-project/issues/59) · [\#60](https://github.com/ufal/atrium-project/issues/60) · [\#61](https://github.com/ufal/atrium-project/issues/61) · [\#62](https://github.com/ufal/atrium-project/issues/62) · [\#63](https://github.com/ufal/atrium-project/issues/63) · [\#64](https://github.com/ufal/atrium-project/issues/64) · [nlp-enrich\#35](https://github.com/ufal/atrium-nlp-enrich/issues/35)

**Status**: 🟢 **Nine of twelve factors are now green; the two reds are gone.** The 09-09 edition of
this digest was the first honest measurement of all twelve factors and it found **two reds (V, VII)**
and seven ambers. Every one of those was carved into a sub-issue on 09-10, and between **09-11 and
09-14** all nine sub-issues landed code on every default branch. This refresh re-measures the same
twelve rows against the trees at hub `f675943` / alto `f6d9196` / nlp `bfabfca` / llm `5ac82a5` /
pc `f11936f` / translator `d0c1572`.

Headline: **V and VII are closed**, III and IV went from amber to green, I closed via a single
shared-file manifest, XI closed with a static guard behind it. What is left is three ambers, each
with a named, small residual — and **three statements posted to sub-issue threads on 09-14 that are
factually wrong** and are corrected below.

**Refreshed 2026-09-24.** The 09-15 and 09-16 status comments (5680251411, 5700467240): eight of ten sub-issues closed (#59, #60, #61, #63, #64, #62 + alto-postprocess#50, nlp-enrich#35); **#58** (VII) has code in all five repos and lacks only the deliberate-breakage run; **#55** (IX) closes on ARÚP/ARÚB's word; factors II and XII needed no sub-issue. #53 can close once #58's run is linked. The plan file for this issue had been overwritten with #54's plan in `d006088`; it is restored in this round.

## 🎯 Goal
Apply the twelve-factor app methodology across all six repos.

## 🧵 Thread trail
- **2026-09-06, K4TEL** — opened with the full twelve-factor list and the six target repos.
- **2026-09-07, K4TEL** — opened **#55**, carving factor IX out with its own acceptance criterion.
- **2026-09-09, K4TEL** — comment: the measured sweep (2 🟢, 2 🔴, 7 🟡, 1 ⚪) and three decision items.
- **2026-09-10, K4TEL** — comment: correction (the `rocrate` para-drift gap had already been closed by
  `ce5a4c1`), plus the sub-issue map: #58 (VII) · #59 (I) · #60 (III) · #61 (XI) · #62 + alto#50 (V) ·
  #63 (IV) · nlp#35 (VI+VIII) · #64 (X). "II, IX and XII need none."
- **2026-09-11 → 2026-09-14** — all nine sub-issues land code. Not reported on this thread.
- **2026-09-14** — this re-measurement. No comment posted at the time of writing; one accompanies it.

## 🔎 Per-factor state — re-measured 2026-09-14

| Factor                     | State            | What was measured                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
|----------------------------|------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **I. Codebase**            | 🟢               | One list, four consumers. `docs/templates/shared/MANIFEST.json` (**16 rows**) is read by `tools/skill_drift_check.py:112-113` (`SHARED_FILES = tuple(entry["dest"] for entry in load_manifest(...))`), `.github/workflows/para-drift.reusable.yml:62-90` (one loop; the 16 hand-written `diff -u` steps are gone) and `scripts/revendor_shared.sh:90-97`, all via `tools/shared_manifest.py --tsv`. Measured directly: **80/80 canonical-file pairs byte-identical** across the five tool repos. Guarded by `tests/test_shared_manifest.py`. **Newly visible residual:** now that `agent-skill` is fetched, `skill_drift_check.py` actually runs and reports `0/5 repos aligned` — `atrium_document.schema.json` is out of parity on every skill branch, plus a translator version lag (skill `v0.10.5` vs test `v1.1.0-beta`). That is the guard working, not a regression, but it needs triage. |
| **II. Dependencies**       | 🟢               | `uvicorn` in all five `service/requirements.txt`. All three unpinned resolutions closed: `flexiconv @ git+…@v0.3.10` in `atrium-nlp-enrich/requirements_flexiconv.txt:29` and `atrium-llm-enrich/requirements_flexiconv.txt:29` (line 23 when written; since 2026-09-23 the pin also names the extras `[docx,odt,md,pdf,rtf]`) (resolution `a982b88f…` recorded at `:14-15`); `atrium-alto-postprocess/Dockerfile:47` `ARG LAYOUTREADER_COMMIT=3b46ee82cd02dcea94ee4064152e5f4de91e205e`, fetched by SHA. `alto-tools` is no longer a dependency at all — vendored as `alto_tools.py` under alto#50. Ecosystem sweep for `git+` returns those two pinned lines and nothing else.                                                                                                                                                                                                                  |
| **III. Config**            | 🟢 (1 defect)    | **Five** `.env.example` files, 122–200 lines each (alto 151 · nlp 159 · llm 169 · pc 122 · translator 200) — was one 4-line file. `docs/k8s_deployment.md` carries `## Configuring the port and bind address` (`:88`) and `## Environment variables — one contract…` (`:122-211`, Table A + per-repo Table B) — the issue's "contains the string 'env' zero times" is emphatically over. Guarded twice: `tests/test_env_contract.py` (662 lines, 12 tests, byte-identical hub + 5 repos, with a per-repo `tests/env_contract_data.py` as a manifest `precondition`) and hub-local `tests/test_env_doc_contract.py`. **Defect:** the template is still `docs/templates/.env.example.template` (leading dot), so the partner-facing link at `docs/k8s_deployment.md:127` is a 404 — see ✏️ below.                                                                                                   |
| **IV. Backing services**   | 🟢               | Both named asymmetries fixed, plus four surfaces the issue did not name. `nlp/api_util/call_udpipe.py:139-145` now has `--url` defaulting to `os.environ.get("UDPIPE_URL") or UDPIPE_URL` (the `or`, not `get(k, d)`, so an empty `${VAR:-}` passthrough counts as unset). `translator/processors/lemmatizer.py:48,65` and `processors/translator.py:137,159` resolve through env with the constants as defaults; `translator/service/api.py:401-405` reads the **effective** URL off the live backend instance and preserves the trailing slash, so an unconfigured run's paradata is byte-identical to before. Third repo caught in passing: `alto/tools/quality_model/correct.py:55` `KOREKTOR_URL`. Permanent guards: `tests/test_backing_service_urls.py` in nlp-enrich and translator. **No unguarded hardcoded external endpoint remains in any of the six repos.**                        |
| **V. Build, release, run** | 🟢 (at `latest`) | **B9 is closed.** `grep -c -- "--entrypoint" .github/workflows/e2e-pipeline-smoke.yml` → **0**; `grep -c "pip install"` → **1** (`:115`, runner-side). Stage 2 runs the image as built. **15 consecutive green E2E runs** since the overrides were deleted; the most recent, run [#149](https://github.com/ufal/atrium-project/actions/runs/34838074366) on `f675943`, has every stage executing at non-trivial duration (52 s / 57 s / 10 s / 76 s / 7 s) plus a green *Assert Final Document Contract* — which is what clears the duplicated-script-name trap empirically. Guard: `tests/test_e2e_entrypoints.py`. **Open:** the lane has **never** been run at `image-tag: test` (`workflow_dispatch` runs total: **0**), and the `-w` successor finding is unowned (below).                                                                                                                   |
| **VI. Processes**          | 🟡               | Unchanged in code, and correctly so — `nlp/service/jobs.py:19` is still a process-local `_jobs` dict and `service/enrichment.py:42` still writes workspaces under `API_JOBS_ROOT`. What the 09-09 sweep got wrong is that "nothing says so": `docs/templates/k8s/atrium-service.deployment.yaml:21-23` carries `replicas: 1` with a comment naming nlp-enrich, and `docs/k8s_deployment.md:226-238` states the limit in prose twice. **Residual:** `docs/k8s_acceptance_runbook.md` mentions nlp-enrich five times and contains **zero** occurrences of "replica" or "scale", so a partner working the runbook never learns it.                                                                                                                                                                                                                                                                   |
| **VII. Port binding**      | 🟢               | All five `api` stages carry `ENV PORT=8000 GRACEFUL_SHUTDOWN_S=20`; four use `ENTRYPOINT ["python", "-m", "service.api"]` (nlp `:82`, llm `:167`, pc `:133`, translator `:116`) and alto keeps `python service/text_api.py` (`:114`). No baked `--port` anywhere. The manifest's three probes now use `port: http` (`:151`, `:164`, `:177`) so they follow `containerPort`. **Proved in CI, not just read:** `docker-tool.reusable.yml:492-551` starts each image with `-e PORT=9000` and fails on `unhealthy` — green in all five repos on 2026-09-14. **Open:** the "verify by breaking it" half of the acceptance has never been run.                                                                                                                                                                                                                                                          |
| **VIII. Concurrency**      | 🟡               | One uvicorn process per container, scale-out by replica — the correct model, and now an honest one: the template's `replicas: 1` default plus the `k8s_deployment.md` note make the nlp-enrich exception explicit. Same single residual as VI (the runbook).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **IX. Disposability**      | 🟢 (external)    | HEALTHCHECK + `STOPSIGNAL SIGTERM` in all five `api` stages; `service/healthcheck.py` (md5 `dc986ad6e6e6b1888d83bf18426cf300`) and `service/atrium_service.py` (md5 `d6a3b61fcc6cb6c4578193b0a57d5e99`) byte-identical 6/6; `/ready` wired in all five; the drain contract has a test in all five `tests/test_api_contract.py`. The graceful-shutdown budget moved from the `--timeout-graceful-shutdown` ENTRYPOINT flag to `ENV GRACEFUL_SHUTDOWN_S=20` under #58 — same behaviour, now tunable. **Remaining work is external**: five unticked ARÚP/ARÚB boxes on #55.                                                                                                                                                                                                                                                                                                                          |
| **X. Dev/prod parity**     | 🟡               | `python-version` is **3.11 in every lane in all six repos** — zero 3.12, including the four hub reusable-workflow `default:` values. Translator's lane pinned in `fbb90e9` with the D1 scope decision recorded in the workflow itself (`scheduled-smoke.yml:11-17`). Held by two tests (`translator/tests/test_python_version_parity.py`, hub `tests/test_hub_python_version_parity.py`). B7's nlp-enrich instance is fixed (`docker-compose.gpu.yaml:1-3`). **Residual, and it is the hub's own:** `docs/templates/skill/server.template.sh:70` still hardcodes `-f docker-compose.yml -f docker-compose.gpu.yml`, and `docs/docker_gha_roadmap.md:285` still prescribes normalising every compose file to `.yml`, contradicting #64's own decision to leave the split alone.                                                                                                                    |
| **XI. Logs**               | 🟢               | Zero `logging.basicConfig` outside `__main__` in all six repos — the five that remain are the five entrypoints (alto `text_api.py:540`, llm `api.py:531`, nlp `api.py:558`, pc `api.py:360`, translator `api.py:510`), all the identical shape reading `LOG_LEVEL` and writing to `sys.stdout`. pc's import-time call is gone, replaced by alto's rule comment. nlp's per-job `processing.log` is no longer a dead end: `service/enrichment.py:306-348` relays combined stdout+stderr to the service logger at both call sites. Guard: `tests/test_logging_contract.py` (md5 `c3784b019da04a2860b30d9b4c42e91a`, 6/6, a `MANIFEST.json` row) plus a container-level `LOG_LEVEL=WARNING` suppression assertion at `docker-tool.reusable.yml:553-602`.                                                                                                                                              |
| **XII. Admin processes**   | 🟢               | Unchanged — one-off tasks run from the same image and codebase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Score:** **9 🟢** (I, II, III, IV, V, VII, IX, XI, XII) · **3 🟡** (VI, VIII, X) · **0 🔴**.
Prior edition (09-09): 2 🟢 · 2 🔴 · 7 🟡 · 1 ⚪.

## ⚠️ Three posted comments that are wrong

Recorded here because they are on public threads and will be believed. Each was checked by hand on
2026-09-14 against the tree at `f675943`.

1. **#60, 11:37** — *"`docs/templates/env.example.template` didn't exist … the file just had a stray
   leading dot hiding it from `ls`. Renamed; all 14 references now resolve."* The file is still
   `docs/templates/.env.example.template`; `git ls-files docs/templates/ | grep env` returns only the
   dotted name, and `f675943`'s stat does not list a rename. The clickable partner-facing link at
   `docs/k8s_deployment.md:127` is a 404 on GitHub.
2. **#59, 11:37** — *"Fixed 6 stale file-count citations … the roadmap said 'seven'."* Five of six.
   `docs/docker_gha_roadmap.md:59` still reads "All seven canonical shared files", and `:121` (D5)
   still describes "seven near-identical `diff -u` steps in `para-drift.reusable.yml` over a
   hardcoded file list" as live, although #59's own records claim the rewrite closed D5.
3. **#64, 11:38** — *"the hub's own `docs/templates/skill/server.template.sh` hardcoded `.yml` too …
   made it resolve the extension at runtime … Also … amended the roadmap to match."* Neither
   happened. `server.template.sh:70` is unchanged since `193697e`, and
   `docker_gha_roadmap.md:285` still says "normalise every compose file to `.yml`".

## ✏️ Corrections to this digest's own previous edition

The 09-09 edition was accurate when written. Five of its rows are now historical and one of its
three decision items was already resolved before it was published:

1. **III** — *"exactly **one** `.env.example` exists in the ecosystem (alto, 4 lines), and
   `docs/k8s_deployment.md` … contains the string 'env' **zero** times."* Five files, 122–200 lines;
   two doc sections.
2. **V** — *"`e2e-pipeline-smoke.yml:274` still `pip install`s `alto-tools` from
   `refs/heads/master.zip`."* Removed 2026-09-11 (`6ba4807`); the dependency itself was retired by
   vendoring (`6ddabf7`, 2026-09-10).
3. **VII** — *"The other four bake `--port 8000` into an exec-form `ENTRYPOINT`."* All four moved to
   `python -m service.api` on 2026-09-11.
4. **VIII** — *"nothing in the reference manifest says so."* The manifest's `replicas: 1` comment and
   the `k8s_deployment.md` note both predate the 09-09 sweep; this row was wrong when written.
5. **XI** — *"`pc/service/inference.py:22` calls `logging.basicConfig()` at import."* Fixed
   2026-09-12 (`9683c76`).
6. **Decision item 3** (`atrium_rocrate.py` unguarded) — already closed by `ce5a4c1` before the
   09-09 comment was posted; corrected on the thread on 09-10 and again here.

## ⏳ Open

- **Three ambers, three residuals**: the runbook replica paragraph (VI/VIII), and the two hub-side B7
  artefacts (X). None is larger than a paragraph.
- **One defect**: the dotted `.env.example.template` and the link it breaks (III).
- **Two findings with no issue**: the `-w /workspace/...` E2E workspace substitution
  (`e2e-pipeline-smoke.yml:298, 348, 370, 414`) that the roadmap §9 says "needs its own issue"; and
  the skill-branch drift the newly-runnable `skill_drift_check.py` reports (`0/5 aligned`).
- **Two acceptance runs never performed**: the `image-tag: test` E2E dispatch (#62) and the
  deliberate-breakage check (#58).
- **Relationship to issue #10** — still unaddressed by the opener, and still needs @K4TEL.

## 📌 Family state — 2026-09-15

The nine sub-issues now have refreshed records and a status comment each. Closure state:

| Sub-issue           | Factor  | State                                                               |
|---------------------|---------|---------------------------------------------------------------------|
| alto-postprocess#50 | V       | ✅ **closed 2026-09-15** (`completed`) — first of the family to land |
| #59                 | I       | 🟢 closable — code done, four corrections posted                    |
| #61                 | XI      | 🟢 closable — all three bullets verify                              |
| #63                 | IV      | 🟢 closable — record repaired first                                 |
| #58                 | VII     | 🟡 one run short (the deliberate-breakage check)                    |
| #62                 | V       | 🟡 one run short (the `image-tag: test` dispatch)                   |
| #60                 | III     | 🟡 one `git mv` short (the dotted template)                         |
| #64                 | X       | 🟡 two hub-side B7 artefacts                                        |
| nlp-enrich#35       | VI+VIII | 🟡 one runbook paragraph                                            |
| #55                 | IX      | ⏸ not ours — five partner checkboxes                                |

Nothing in the family is blocked on analysis. Every remaining item is a paragraph, a `git mv`, or a
CI run someone has to press.

---
_Digest of [ufal/atrium-project#53](https://github.com/ufal/atrium-project/issues/53). Measured against hub `f675943`, alto `f6d9196`, nlp `bfabfca`, llm `5ac82a5`, pc `f11936f`, translator `d0c1572` — in all six, the default branch equals `test` equals the measured tree. No container was started (no Docker daemon in the measuring environment); every container-level claim above cites a GitHub Actions run instead._
