# 📓 Digest — [\#40](https://github.com/ufal/atrium-project/issues/40) `open`: CI/CD: Enforce Branch Protection (GH) & Enable GPU Runner (GHA)

> _Condensed from [`../issues/2026-08-02.40.issue.open.md`](../issues/2026-08-02.40.issue.open.md) — 4 comments (export regenerated 2026-09-26 18:45)._
> _Thread: 2026-08-02 → 2026-09-26 · K4TEL, motyc, stranak. Related: hub #18 (the GHA/Docker hardening this issue closes out the tail of). **Refreshed 2026-09-26 (AMČR baseline, [#67](https://github.com/ufal/atrium-project/issues/67))**._

**Labels**: `enhancement`, `development`, `ci`, `github_actions` · Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover _(relabelled 2026-09-08; was Q3 [WP8 / WP7])_
· **Assignees**: stranak, rharasim, K4TEL

**Status**: ✅ **Finish — pilot (AMČR, #67), rescoped.** Branch protection now (all six repositories), immutable
releases in the five tool repos, reusable workflows referenced by commit; the GPU runner ⏸️ deferred until after the
pilot. Nothing started.
_Before this refresh:_ 🟡 **Reframed, not resolved.** The GPU-runner half was never going to be "configure a
runner and go" — @motyc ruled out wiring live GPU access into UFAL's CI at all. Branch protection
has no such reframing and simply has no recorded progress.

## 🧭 AMČR baseline — 2026-09-26 ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ✅ finish — branch protection now, the GPU runner later.** Decided by motyc (AMČR), adopted by ÚFAL:
> *"AMČR will build its production images from a release tag, after its own checks. For that, a tag has to mean one
> thing, permanently: immutable releases in the five tool repositories […]; reusable workflows referenced by commit,
> not by the moving `@v1` tag. A version comment beside the SHA is fine. Done when: new releases in the five
> repositories are immutable, and every `uses:` line that points to this repository names a commit. […] On the GPU
> runner, my comment of 2 August still stands: it is outside what AMČR needs, so we propose to defer it."* —
> [5847698000](https://github.com/ufal/atrium-project/issues/40#issuecomment-5847698000)

What the code has (2026-09-26): 35 `uses: ufal/atrium-project/…@v1` lines (seven reusables × five repos);
`para-drift` and `workflow-lint` also check the hub out at `hub-ref`, default `v1` (`para-drift.reusable.yml:21,41`,
`workflow-lint.reusable.yml:37,65`), so a commit-pinned `uses:` needs a matching `hub-ref`; all five `release.yml`
publish with `softprops/action-gh-release` (SHA-pinned v3.0.3), attaching `service_bundle.zip` in the same step;
Dependabot's `github-actions` ecosystem is on in all six repos. From #18 (closing), the branch-protection item and the
`@v1` trade-off now live here.

## 🎯 Goal
Close the last two infrastructure gaps left after the GHA/Docker hardening (hub #18): an external
GPU runner for `pytest -m slow`, and branch-protection rules on the hub's `main`/`test`.

## 🧵 Thread trail
- **2026-08-02, K4TEL** — opened. Context: GPU crons were already removed elsewhere in the
  ecosystem because it was advertising `slow`-test coverage it didn't actually have without a real
  runner; branch protection matters more now that the hub holds the canonical `atrium_document`
  standard and CI templates every tool repo depends on — a bad push here cascades to all five.
  Tasks: (1) configure the external GPU runner and point `pytest -m slow` at it — @rharasim; (2)
  verify the E2E smoke test's GPU-dependent phases actually run; (3) apply branch protection
  (required reviews, required status checks, no force-push) to `main`/`test` — @stranak.
- **2026-08-02, motyc** — **descopes the GPU half from UFAL infrastructure entirely**: "We do not
  want GH Actions runned in UFAL profile wired to any of our (ARUP/B) environment... the
  borderline is creating a Docker image, which will be then run at our Kubernetes environment...
  I do not think we need any live access to the GPUs here." If GPU testing is needed at all, the
  answer has to come from outside this infrastructure — points at e-INFRA.cz.
- **2026-08-03, K4TEL** — identifies the concrete outside option: Metacentrum
  (`metavo.metacentrum.cz`), the GPU grid e-INFRA.cz operates, application-gated
  (`metavo.metacentrum.cz/en/application/form`).
- **2026-08-03, motyc** — adds two more Metacentrum/CERIT services that might help:
  `docs.metacentrum.cz/.../galaxy` and CERIT's `n8n-agents` AI-as-a-service.
- **2026-09-26, motyc** — immutable releases and commit-pinned reusable workflows, next to branch protection; the
  GPU runner deferred (above).

## ✅ What's settled
- **The GPU runner is not going to be a self-hosted runner inside UFAL/ARUP-adjacent
  infrastructure.** That was the original framing of task (1); it's now off the table. Whatever
  replaces it has to be an outside service (Metacentrum-class) with its own access-application
  process.
- No progress recorded on branch protection since opening.

## ⏳ Open
- ~~**Metacentrum access has not been applied for**, as far as this thread shows. That application
  (`metavo.metacentrum.cz/en/application/form`) is the actual next concrete step for the GPU half,
  not "configure a runner."~~ ⏸️ Deferred until after the pilot (AMČR, #67).
- **Immutable releases** in the five tool repos — new, not started.
- **Reusable workflows pinned by commit** (35 lines, plus the matching `hub-ref`) — new, not started.
- **Branch protection** — @stranak's task, zero recorded movement; now for all six repositories, with tag rules. As of the
  2026-09-07 cross-repo state check (`../digests/project_state_0709.md`), `test` and `main` remain
  full, unprotected mirrors of each other in all six repos.
- See [`../plans/40.plan.md`](../plans/40.plan.md) for the concrete next steps on both halves.
