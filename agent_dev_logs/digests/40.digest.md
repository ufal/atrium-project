# 📓 Digest — [\#40](https://github.com/ufal/atrium-project/issues/40) `open`: CI/CD: Enforce Branch Protection (GH) & Enable GPU Runner (GHA)

> _Condensed from [`../issues/2026-08-02.40.issue.open.md`](../issues/2026-08-02.40.issue.open.md) — 3 comments._
> _Thread: 2026-08-02 → 2026-08-03 · K4TEL, motyc, stranak. Related: hub #18 (the GHA/Docker hardening this issue closes out the tail of)._

**Labels**: `enhancement`, `development`, `ci`, `github_actions` · Milestone: Q3 [WP8 / WP7]
· **Assignees**: stranak, rharasim, K4TEL

**Status**: 🟡 **Reframed, not resolved.** The GPU-runner half was never going to be "configure a
runner and go" — @motyc ruled out wiring live GPU access into UFAL's CI at all. Branch protection
has no such reframing and simply has no recorded progress.

## 🎯 Goal
Close the last two infrastructure gaps left after the GHA/Docker hardening (hub #18): an external
GPU runner for `pytest -m slow`, and branch-protection rules on the hub's `main`/`test`.

## 🧵 Thread trail
- **2026-08-02, K4TEL** — opened. Context: GPU crons were already removed elsewhere in the
  ecosystem because it was advertising `slow`-test coverage it didn't actually have without a real
  runner; branch protection matters more now that the hub holds the canonical `atrium_document`
  standard and CI templates every tool repo depends on — a bad push here cascades to all five.
  Tasks: (1) configure the external GPU runner and point `pytest -m slow` at it — @rharasim; (2)
  verify the E2E smoke test's GPU-dependent phases actually run; (3) apply branch protection
  (required reviews, required status checks, no force-push) to `main`/`test` — @stranak.
- **2026-08-02, motyc** — **descopes the GPU half from UFAL infrastructure entirely**: "We do not
  want GH Actions runned in UFAL profile wired to any of our (ARUP/B) environment... the
  borderline is creating a Docker image, which will be then run at our Kubernetes environment...
  I do not think we need any live access to the GPUs here." If GPU testing is needed at all, the
  answer has to come from outside this infrastructure — points at e-INFRA.cz.
- **2026-08-03, K4TEL** — identifies the concrete outside option: Metacentrum
  (`metavo.metacentrum.cz`), the GPU grid e-INFRA.cz operates, application-gated
  (`metavo.metacentrum.cz/en/application/form`).
- **2026-08-03, motyc** — adds two more Metacentrum/CERIT services that might help:
  `docs.metacentrum.cz/.../galaxy` and CERIT's `n8n-agents` AI-as-a-service.

## ✅ What's settled
- **The GPU runner is not going to be a self-hosted runner inside UFAL/ARUP-adjacent
  infrastructure.** That was the original framing of task (1); it's now off the table. Whatever
  replaces it has to be an outside service (Metacentrum-class) with its own access-application
  process.
- No progress recorded on branch protection since opening.

## ⏳ Open
- **Metacentrum access has not been applied for**, as far as this thread shows. That application
  (`metavo.metacentrum.cz/en/application/form`) is the actual next concrete step for the GPU half,
  not "configure a runner."
- **Branch protection on `main`/`test`** — @stranak's task, zero recorded movement. As of the
  2026-09-07 cross-repo state check (`../digests/project_state_0709.md`), `test` and `main` remain
  full, unprotected mirrors of each other in all six repos.
- See [`../plans/40.plan.md`](../plans/40.plan.md) for the concrete next steps on both halves.
