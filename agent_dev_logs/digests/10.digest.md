# 📓 Digest — [\#10](https://github.com/ufal/atrium-project/issues/10) `open`: LLM validation of source code

> _Condensed from `../issues/2026-03-15.10.issue.open.md` — 14 comments. The full export is the source of truth; this digest tracks the review rounds, milestones, and the remaining checklist items._
> _Thread: 2026-03-15 → 2026-06-26 · K4TEL, motyc_

**Labels**: `enhancement`, `development`, `a big one`
**Status**: ✍️ `In progress` — Multiple review rounds completed across the four main tool repos. The canonical cross-repo execution strategy is now centralized in `docs/plan_repo_review.md`.

## 🎯 Goal
Perform iterative, LLM-driven validation of the source code across the four main ATRIUM tool repositories (`atrium-page-classification`, `atrium-alto-postprocess`, `atrium-nlp-enrich`, `atrium-translator`) to ensure code quality, complete test coverage, and strict CI/CD alignment.

## 📅 Hard constraints
- Reviews must be executed iteratively across all four repos in order of dependencies: `pc` → `alto` → `nlp` → `translator`.
- Strict adherence to the shared templates (`ruff.toml`, `.coveragerc`) and centralized reusable GHA workflows.
- No changes committed without local validation (Tier 1: compileall/ruff, Tier 2: pytest/coverage).

## 📦 Artifacts produced (in thread)
- **Releases**: `alto` v0.19.1, `pc` v1.4.2-beta, `translator` v0.6.1, `nlp` v0.14.2 (from LLM review edits).
- **Consolidated Review Plan**: `docs/plan_repo_review.md` created and designated as the canonical guide (Commit `3c2280f`).
- **Validation Matrix**: Tier 1 (static analysis) and Tier 2 (fast tests) established per repo.
- **Opus 4.8 Audit Report**: Comprehensive breakdown of current regressions and technical debt (2026-06-21).

## 🔗 Latest follow-ups (2026-06-21 - 2026-06-26)
- **Identified Regressions (Opus 4.8)**: 
  - `nlp` Shellcheck is currently RED.
  - `/info` version drift in `pc` and `translator` APIs.
  - `para_licenses.py` deduplication (#12) landed only in `alto`; remaining repos are divergent and the resolution engine has **zero tests** everywhere.
  - `nlp` ruff is blocking against the advisory-first policy.
  - Secret scanning missing/unverified in GHA.
- `docs/plan_repo_review.md` updated as the single source of truth to be followed.

## ⏳ Open / next
Execute the 5-phase strategy established during the Opus 4.8 round:
1. Green the `nlp` shellcheck + sync `api.py` versions.
2. Fix self-contained test lanes (add root `requirements-test.txt` to `pc`, fix under-declared deps).
3. Test untested cores (`para_licenses.py`, translator `load_vocab`, alto in-process).
4. Ratchet `fail_under` / ruff gates.
5. Finalize release & paradata-provenance hygiene before moving to feature additions (#6, #7, #3).