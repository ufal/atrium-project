# 📓 Digest — [\#10](https://github.com/ufal/atrium-project/issues/10) `open`: LLM validation of source code

> _Condensed from `../issues/2026-03-15.10.issue.open.md` — 13 comments. The full export is the source of truth; this digest tracks the review rounds, milestones, and the remaining checklist items._
> _Thread: 2026-03-15 → 2026-07-16 · K4TEL, motyc · Last reconciled against code: **2026-07-22, after the code-quality landing wave** — release-gate + Phase-1/2 correctness/test work landed on every tool repo's `test` branch and re-verified against the remotes._

**Labels**: `enhancement`, `development`, `a big one` · Milestone: Q5-27 [WP1/WP2/WP7/WP8: Final Project Conclusion]
**Status**: ✍️ `In progress` — the 2026-07-16 round is **largely executed**. Release plumbing is fixed (inline `version-guard` gate live in all 5 tool repos; nlp re-tagged `v0.16.2`), a code-quality pass (correctness + test-gap closure + hygiene) landed across all tool repos, and the hub tracker was refreshed. Remaining is a small tail: the nlp↔llm engine-governance decision (deferred), a Tier-2 ratchet-up, gitleaks (policy-blocked), a couple of un-landed hygiene items, and the two heavy refactors (modularization / print→logging) that need a GPU-capable env to verify.

## 🎯 Goal
Perform iterative, LLM-driven validation of the source code across the ATRIUM tool repositories — five products (`atrium-page-classification`, `atrium-alto-postprocess`, `atrium-nlp-enrich`, `atrium-translator`, `atrium-llm-enrich`) plus, since the 2026-07-16 round, the **hub repo itself** — to ensure code quality, complete test coverage, and strict CI/CD alignment.

## 📅 Hard constraints
- Reviews executed iteratively across repos; 2026-07-16 round order: `alto` → `llm` → `nlp` → `translator` → `hub`.
- Adherence to the shared hub templates — `ruff.toml` is a **floor** (repos may keep documented supersets); `para_licenses.py` / `atrium_paradata.py` / `test_para_licenses.py` are **strict** (enforced by the `para-drift` workflow in all 5 tool repos).
- No changes committed without local validation (Tier 1: compileall/ruff; Tier 2: `pytest -m "not slow"` in a clean venv from the repo's own `requirements*-test.txt`).

## 📦 Artifacts produced
- **Releases (latest per repo)**: pc `v1.5.1-beta` (07-15) · translator `v0.8.1` (07-15) · alto `v1.1.0-beta` (07-22) · nlp **`v0.16.2`** (07-22 — **re-tagged**, replacing the `v1.16.2` mistag) · llm `v0.3.0` (07-22). All five `CITATION.cff` `date-released` values fresh. (nlp code is again a little ahead of its `v0.16.2` tag — the annotation/bbox tests landed after — the benign recurring pattern.)
- **Release gate**: an **inline `version-guard` job** now gates `release.yml` in all 5 tool repos (`needs: version-guard`), failing the release on `tag ≠ CITATION.cff ≠ para_config.txt`. Deliberately inline (not the cross-repo reusable) so it can't fail open on an `@test` resolution race at tag-push time. Verified against history: blocks both prior bad tags (alto `v1.0.0.-beta`, nlp `v1.16.2`).
- **Coverage ratchets**: `.coveragerc` `fail_under` set in **all five** repos (pc 28 · nlp 30 · llm 42 · alto 50 · translator 70), each annotated with the 2026-07-16 baseline.
- **Consolidated Review Plan**: `docs/plan_repo_review.md` — **refreshed 2026-07-22** (6-repo scope; F/G/H/I/COV closed; release-gate + code-quality pass recorded; nlp↔llm governance deferred).
- **Validation Matrix (fast lane, re-run 2026-07-22, post-landing)**: compileall OK ×6; ruff clean ×5; fast lanes green — alto **445** · translator **325** · nlp **289** · pc **281** · llm **247** passed.
- **State reviews**: `digests/project_state_1307.md` (07-13) and `digests/project_state_2207.md` (07-22). The dangling `issue10_alignment_1307.md` citation those carried was dropped in the refresh (the file was never committed).

## ✅ Closed this round (verified in code 2026-07-22)
- **Release-gate hardening** (was P2): inline `version-guard` live in all 5 `release.yml`; the class of mistag/malformed-tag defect can no longer reach publication.
- **nlp release mistag** (was P1): re-tagged `v1.16.2` → `v0.16.2`; tag now matches `CITATION.cff`/`para_config`.
- **Coverage ratchets** (was "unset everywhere" — that claim was stale): `fail_under` set in all five repos.
- **nlp C4** (was P2): `build_parser()` extracted in `api_util/summarize_nt_udp.py`; `test_teitok_integraion.py` now asserts via `build_parser().format_help()` instead of a subprocess.
- **Code-quality pass** (NEW, landed as the "LLM review" commits): correctness — alto removed a stale duplicate `categorize_line` service fallback (a broken import would have served stale categorization); llm logged the silent bare-excepts in the DU convertors; pc tightened `service/inference.py`; nlp logged genuinely-silent pipeline swallows. Test gaps — llm gained `test_llm_utils.py`+`test_llm_run.py` (the engine had none) and a shared image-dim test; nlp gained annotation-converter round-trip tests, the image-dim test, and a direct `test_bbox_scale.py` (parity with llm); pc gained `test_img2jpeg.py` and renamed the misleading `service/test_api.py` → `api_client.py`; translator gained `test_bakeoff.py`. Hygiene — pc `.gitignore` now ignores `__pycache__` recursively.
- **plan_repo_review.md refresh** (was open): landed on hub `test`.
- Carried from the prior cycle: **llm-enrich onboarding**, **Thread G** (alto test-deps), **shared-code parity** — all still holding.

## ⏳ Open / next
1. **nlp↔llm engine governance** (P1 → **DEFERRED**, maintainer decision 2026-07-22): `llm_utils.py`/`vocab_manager.py`/`llm_run.py` sit outside para-drift; measured divergence — `vocab_manager.py` byte-identical, `llm_run.py` one line (`program=` string), `llm_utils.py` forked (+343 lines in llm: the auto CPU-offload subsystem). Left as-is for now; revisit when divergence or an OOM-parity need forces it.
2. **Tier-2 ratchet-up**: the fast lanes are green above the current `fail_under` floors — record fresh per-repo coverage and raise the floors toward measured baselines.
3. **Un-landed hygiene**: translator design doc still at repo-root `translation_backends.md` — needs a `git mv` to `docs/translation-backends.md` (all code refs already point there).
4. **Heavy refactors — need a GPU-capable env, not done here**: Phase-4 modularization of the >800-line modules (alto `text_util_langID.py` 1560 L, `langID_classify.py` 938 L; pc `run.py` 1661 L) — its safety gate is byte-identical CSV output on `data_samples/` fixtures, which needs the GPU Qwen-0.5B perplexity worker; and the print→logging sweep (Phase 5) — flagged as stylistic churn against the plan's own guardrail and behaviour-affecting in the multiprocessing workers. Both belong on a GPU branch where the parity/runtime checks can run.
5. **gitleaks**: template ready (`secret-scan.caller.example.yml`); awaiting ARUB/ARUP policy sign-off + `GITLEAKS_LICENSE`.
6. **Forward-merge `test` → `main`** on the hub: `project_state_2207.md`, the refreshed digest/plan, and `plan_repo_review.md` live only on `test` — default-branch readers can't see them.
7. **Watch item**: digest cadence vs. auto-release cadence — re-check `list_releases`/CITATION at digest-write time; timestamp release tables, don't trust a stale one.
