# 📓 Digest — [\#10](https://github.com/ufal/atrium-project/issues/10) `open`: LLM validation of source code

> _Condensed from `../issues/2026-03-15.10.issue.open.md` — 13 comments. The full export is the source of truth; this digest tracks the review rounds, milestones, and the remaining checklist items._
> _Thread: 2026-03-15 → 2026-07-16 · K4TEL, motyc · Last reconciled against code: **2026-07-22 live re-verification** (releases, CITATION/`para_config` sources, requirements files, and branch HEADs checked against the remotes; builds on `digests/project_state_1307.md` and `digests/project_state_2207.md`)._

**Labels**: `enhancement`, `development`, `a big one` · Milestone: Q5-27 [WP1/WP2/WP7/WP8: Final Project Conclusion]
**Status**: ✍️ `In progress` — **a new review round was opened 2026-07-16** (comment by K4TEL): the Fable round's unit-test and requirements-coverage fixes landed in all repos and shipped as the 2026-07-15 release wave; the next round's checklist is `alto` → `llm-enrich` → `nlp-enrich` → `translator` → **`atrium-project` (hub — in scope for the first time)**, all boxes unchecked. `page-classification` is **not** on the new list (graduated from the cycle). Code quality is Tier-1 green ecosystem-wide; the weak spot this round is **release plumbing**, not code (see Open/next).

## 🎯 Goal
Perform iterative, LLM-driven validation of the source code across the ATRIUM tool repositories — five products (`atrium-page-classification`, `atrium-alto-postprocess`, `atrium-nlp-enrich`, `atrium-translator`, `atrium-llm-enrich`) plus, since the 2026-07-16 round, the **hub repo itself** — to ensure code quality, complete test coverage, and strict CI/CD alignment.

## 📅 Hard constraints
- Reviews executed iteratively across repos; current round order per the 2026-07-16 comment: `alto` → `llm` → `nlp` → `translator` → `hub`.
- Adherence to the shared hub templates — `ruff.toml` is a **floor** (repos may keep documented supersets); `para_licenses.py` / `atrium_paradata.py` / `test_para_licenses.py` are **strict** (enforced by the `para-drift` workflow in all 5 tool repos; sha256-parity re-confirmed 2026-07-22).
- No changes committed without local validation (Tier 1: compileall/ruff, Tier 2: `pytest -m "not slow" --cov` in a **clean venv from the repo's own `requirements*-test.txt`**).

## 📦 Artifacts produced
- **Releases (all current, latest per repo as of 2026-07-22)**: pc `v1.5.1-beta` (07-15) · translator `v0.8.1` (07-15) · alto **`v1.1.0-beta`** (07-22 — supersedes the malformed `v1.0.0.-beta` tag of 07-16) · nlp **`v1.16.2`** (07-22 — ⚠️ mistagged, source says `0.16.2`) · llm **`v0.3.0`** (07-22; first tags `v0.1.0`/`v0.2.0` on 07-15). All five `CITATION.cff` `date-released` values are fresh (07-15/07-22); the code-ahead-of-tag pattern is gone this cycle.
- **Consolidated Review Plan**: `docs/plan_repo_review.md` (canonical; still needs the scope+status refresh — add llm column, add hub row, close executed items).
- **Validation Matrix**: Tier 1 re-run 2026-07-22 (isolated worktrees) — `compileall` OK ×6; **ruff clean ×5 tool repos** (llm's 3 findings from 07-13 fixed in the v0.2.0 Fable round). Tier 2 last full run 2026-07-13 (fast lanes green: translator 303 · alto 350 · pc 251 · nlp 236 · llm 83); re-run due under the new self-contained requirements files.
- **State reviews**: `digests/project_state_1307.md` (07-13) and `digests/project_state_2207.md` (07-22, `test` only). ⚠️ Both cite `digests/issue10_alignment_1307.md` as evidence base — **that file was never committed** to `test` or `main`; the citation is dangling and should be dropped or the file restored.

## ✅ Closed since the 2026-07-13 audit (verified in code 2026-07-22)
- **llm-enrich onboarding (was P0)**: root `requirements-test.txt` added (pytest/pytest-cov/requests + fast-lane runtime deps); 3 ruff findings (`eval_metrics.py` B905, W292 ×2) fixed; first releases tagged (`v0.1.0` → `v0.3.0`). Residual: `plan_repo_review.md` scope doc still doesn't cover it.
- **Thread G (alto, was P1)**: `setup/requirements-test.txt` now declares pandas/numpy/lxml/tqdm + the FastAPI test stack — the "13 collection errors on clean install" gap is structurally closed (clean-venv confirmation run still worthwhile).
- **nlp release gap**: `v0.16.1` published 07-15 (the old "code ahead of tag" item) — but replaced by a **new** defect, the `v1.16.2` mistag (below).
- **alto promotion**: `0.20.2` → `1.0.0-beta` (07-16) → `1.1.0-beta` (07-22, configs/requirements moved to `setup/`, quality-model draft). The malformed `v1.0.0.-beta` tag (stray dot) was superseded by the clean `v1.1.0-beta` the same week — resolved without re-tagging.
- **Shared-code parity**: held through both alto version bumps; byte-identical ×5 == hub canonical (07-22 re-check).

## ⏳ Open / next
1. **Re-tag nlp `v1.16.2` → `v0.16.2`** (P1): the 07-22 release tag jumped a major version for a patch-level change; `CITATION.cff` and `para_config.txt [tool]` both say `0.16.2`.
2. **Harden the release gate** (P2, durable): `security.reusable.yml` is meant to enforce tag == CITATION == `para_config`, yet two bad tags reached publication in one week (alto's stray-dot tag, nlp's major mistag). Fix the gate — individual bad tags will keep recurring until it fails the release job on exact-match violation.
3. **Execute the 2026-07-16 round checklist**: alto → llm → nlp → translator → **hub** (hub has no ruff config and has never been in a per-repo review pass — define its Tier-1 scope: workflows, templates, docs link hygiene).
4. **Govern the nlp↔llm duplicated LLM modules** (P1, unchanged): `llm_utils.py`/`vocab_manager.py` twins sit outside para-drift; `llm_run.py` diverged by 07-13 and llm has since added DU code and remote clients (`v0.3.0`) — the gap is compounding. Extend para-drift or declare llm-enrich canonical.
5. **I1 remainder (nlp C4)**: `test_teitok_integraion.py` still shells out; extract `build_parser()` from `api_util/summarize_nt_udp.py`.
6. **Ratchets**: set `fail_under` per measured fast-lane coverage (still unset ecosystem-wide); keep ruff at 0.
7. **Hygiene**: gitleaks caller adoption (template ready, awaiting ARUB/ARUP sign-off); refresh `plan_repo_review.md`; drop the dangling `issue10_alignment_1307.md` citations; **forward-merge the 07-22 state digest from `test` to `main`** (it is currently invisible to default-branch readers — the exact blind spot these digests keep flagging).
8. **Watch item**: digest cadence vs. auto-release cadence — two findings in `project_state_2207.md` (alto tag, llm date) were already obsoleted by same-day releases when written. Treat any digest's release table as a lower bound; re-check `list_releases`/CITATION before acting.
