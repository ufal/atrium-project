# 📓 Digest — [\#58](https://github.com/ufal/atrium-project/issues/58) `open`: Honour `$PORT`, following the `alto-postprocess` pattern in all 5 repos

> _Condensed from [`../issues/2026-09-10.58.issue.open.md`](../issues/2026-09-10.58.issue.open.md) — **6 comments** (2026-09-10 13:01 · 2026-09-11 06:23 · 2026-09-11 13:04 · 2026-09-15 11:21 · 2026-09-16 10:19 · 2026-09-16 15:56), all K4TEL._
> _Verification passes: 2026-09-11 (pre-push) · **2026-09-14** (against merged branches and green CI). **Refreshed 2026-09-26 (AMČR baseline, [#67](https://github.com/ufal/atrium-project/issues/67))** — status only; no comment since 09-16._

**Labels**: `bug`, `development`, `integration` · Type: `Task` · Milestone: WP4 — Pre-Closure Architecture Review · **Parent**: [\#53](https://github.com/ufal/atrium-project/issues/53) (factor VII)

**Status**: ✅ **Finish — pilot (AMČR, #67); first in #53's close order** (#58 → #55 → #53). The breakage run is the
only item, and it now gates the umbrella as well.
_Before this refresh:_ 🟢 **Merged in all six repos and proved in CI.** The blocker the 2026-09-11 13:04 comment
set — *"Leaving open until the `PORT=9000` probe runs green"* — is **cleared**: the probe executed and
passed in all five repos on 2026-09-14. 🟡 The issue stays open for one reason only: acceptance
bullet 4 asks for the check to be verified *by breaking it*, and that has never been done.

**Refreshed 2026-09-24.** The 09-15 and 09-16 comments repeat this status on the issue: bullets 1–3 met in all five repos, both probes live (`docker-tool.reusable.yml` `PORT=9000` + HEALTHCHECK, and `HOST=127.0.0.1` + an unreachable published port). The one remaining task is still the deliberate breakage: re-bake `--port 8000` into one repo's `api` `ENTRYPOINT`, push to `test`, confirm the non-default-port step goes red, revert — keeping `ENV PORT=8000` in place, and remembering that `docker-build-smoke` has no `needs:`, so a red probe does not stop `:test` from publishing. Not done as of 2026-09-24.

## 🎯 Goal
Make `PORT` and `HOST` real knobs in all five published `api` images, so the manifest ARÚP/ARÚB
deploy from means what it says.

## 🧵 Thread trail
- **2026-09-09** — #53's sweep scores factor VII red and sequences it as plan item A.
- **2026-09-10 12:04, K4TEL** — opened, with the per-repo entrypoint table.
- **2026-09-10 13:01, K4TEL** — two corrections: the prescribed `python service/api.py` entrypoint
  crashes all four; use `python -m service.api`. And this is not an inert knob — `healthcheck.py:49`
  builds its probe URL from `PORT`, so a non-default value would make the container permanently
  unhealthy.
- **2026-09-11 06:23, K4TEL** — three more: the manifest hardcodes 8000 in four further places; the
  CI probe hardcodes the container side too, so acceptance needs a *second* start; `healthcheck.py`
  needs no change.
- **2026-09-11 13:04, K4TEL** — *"Implemented across all six repos."* Plus the blocker above.
- **2026-09-14** — this re-measurement. The blocker is cleared; not yet reported on the thread.

## 🔎 Verified state — 2026-09-14

### Dockerfiles: no baked `--port` anywhere

| Repo                | `ENV`                                         | `ENTRYPOINT`                               | `CMD`           |
|---------------------|-----------------------------------------------|--------------------------------------------|-----------------|
| alto-postprocess    | `:113` `ENV PORT=8000 GRACEFUL_SHUTDOWN_S=20` | `:114` `["python", "service/text_api.py"]` | `:115` `CMD []` |
| nlp-enrich          | `:74`                                         | `:82` `["python", "-m", "service.api"]`    | `:83`           |
| llm-enrich          | `:159`                                        | `:167` `["python", "-m", "service.api"]`   | `:168`          |
| page-classification | `:125`                                        | `:133` `["python", "-m", "service.api"]`   | `:134`          |
| translator          | `:108`                                        | `:116` `["python", "-m", "service.api"]`   | `:117`          |

The comment's `-m` correction was taken, not the issue body's form. Landed 2026-09-11 08:56–09:14:
translator `6e77466`, nlp `4a75160`, alto `9d344b6`, llm `c943137`, pc `71d9654`.

### `__main__` blocks: byte-identical across the four, env-driven

`LOG_LEVEL` → `RELOAD` → `_app_ref` → `HOST` → `PORT` → `GRACEFUL_SHUTDOWN_S`, at
nlp `service/api.py:559-593`, llm `:532-566`, pc `:361-395`, translator `:511-545`. The app-reference
question the 09-11 comment left open was decided and shipped as the `reload`-gated form, e.g.
`nlp/service/api.py:581`:

```python
_app_ref = f"{__spec__.name}:app" if reload and __spec__ is not None else app
```

alto is untouched by design (`service/text_api.py:550-561`).

### `healthcheck.py`: unchanged, still canonical
`service/healthcheck.py:48-51` probes `127.0.0.1` and reads `os.getenv("PORT", DEFAULT_PORT)`.
Byte-identical in all five (md5 `dc986ad6e6e6b1888d83bf18426cf300`). The fix stayed entirely outside
the para-drift set, exactly as the 09-11 comment predicted.

### The manifest: all four extra hardcodings fixed
`docs/templates/k8s/atrium-service.deployment.yaml` — `:52-54` a new comment naming `containerPort`
and `env: PORT` as the one pair that must agree; `:58` `containerPort: 8000` / `:59` `name: http`;
`:70-71` `PORT`; `:72` commented `HOST`; `:77` commented `GRACEFUL_SHUTDOWN_S`; and **all three probes
now `port: http`** (`:151` startup, `:164` readiness, `:177` liveness), so they follow the port name
rather than repeating the number. Commented Service at `:225-227` uses `targetPort: http`.

### The CI probe: added as a second start, and it has now run
`.github/workflows/docker-tool.reusable.yml` — `:46` the `probe-targets` input; `:379` the default
start unchanged at `-p 18080:8000`; **`:492` the new step** *"Run container on a NON-DEFAULT port
(issue #58)"*, `:519` `docker run -d -e PORT=9000 -p 18080:9000`, `:531-535` hard-fail on
`unhealthy`, `:549` a `/health` curl. Landed `11541f9` (2026-09-11 09:07:46) — **25 seconds after
the previous edition of this digest was written**, which is why that edition could not see it.

Green in all five repos, `docker-tool.reusable.yml@v1` = `f675943`, step *"Run container on a
NON-DEFAULT port"*:

| Repo                | run                                                                                 | date             | result |
|---------------------|-------------------------------------------------------------------------------------|------------------|--------|
| page-classification | [#317](https://github.com/ufal/atrium-page-classification/actions/runs/34838971713) | 2026-09-14 11:43 | ✅ 54 s |
| alto-postprocess    | [#454](https://github.com/ufal/atrium-alto-postprocess/actions/runs/34838653518)    | 2026-09-14 11:41 | ✅ 25 s |
| nlp-enrich          | [#310](https://github.com/ufal/atrium-nlp-enrich/actions/runs/34838487713)          | 2026-09-14 11:34 | ✅ 20 s |
| llm-enrich          | [#270](https://github.com/ufal/atrium-llm-enrich/actions/runs/34838754391)          | 2026-09-14 11:34 | ✅ 10 s |
| translator          | [#324](https://github.com/ufal/atrium-translator/actions/runs/34865982363)          | 2026-09-14 16:02 | ✅ 15 s |

The step is not vacuous: in pc run `34679959094` (2026-09-12) the **default-port** health step went
red and this step correctly skipped, so the gating works.

### Everything else the plan asked for
- Compose passthrough `"${PORT:-8000}:${PORT:-8000}"` in all five.
- `PORT=8000` and `GRACEFUL_SHUTDOWN_S=20` in all five `.env.example`.
- `tests/test_service_entrypoint.py` in all four `-m` repos (md5 `17f9266c9e69d5cb328e43a12bd76b13`),
  asserting `argv[:2] == ["python", "-m"]` and `"--port" not in argv`.
- `docs/k8s_deployment.md:88-115` — the port section, including *"to run on a different port, change
  two fields, not one"* and the `HOST=127.0.0.1` warning.
- pc's frontend dev-server port list fixed (`service/frontend/script.js:108-110`).

## ✏️ Corrections to this digest's own previous edition

The 09-11 edition was written at hub `0207a8e`, **25 seconds before** the hub port commit. It is
wrong in the one direction that matters — it tells a reader the work still needs doing.

1. **"Implemented (2026-09-11), not pushed — delivered as attachments for review rather than
   committed to any remote."** False since 2026-09-11 09:14. Everything is on `test` and on the
   default branch in all six repos.
2. **The Finding 3 line numbers** — `:52`→`:58`, `:53`→`:59`, `:55-56`→`:70-71`, `:72/:85/:98`→
   `:151/:164/:177` (and they now read `port: http`), `:146-148`→`:225-227`.
3. **"`docs/k8s_deployment.md` does not mention `PORT` anywhere at all (grepped: zero hits)."** Six
   hits and a whole section at `:88-115`.
4. **"`docker-tool.reusable.yml:349`"** → `:379`, and the second probe it called for exists at
   `:492-551`.
5. **"App reference form — object, string, or `__spec__`? Not decided."** Decided and shipped.
6. **"nlp-enrich's `api` stage has no `CMD []`."** `Dockerfile:83` has one.

## ⏳ Open

- **The deliberate-breakage run** (acceptance bullet 4, plan §F). Put a baked `--port` back on one
  repo, confirm the `PORT=9000` step goes red, revert. This is the only thing holding the issue open.
- ~~**`HOST` has no CI probe.**~~ **Resolved 2026-09-16**: `docker-tool.reusable.yml:553` implements exactly the step this bullet asked for. The line below is kept for the record and no longer describes the tree; nothing
  checks it.
- `RELOAD=true`, `GRACEFUL_SHUTDOWN_S=5` and pc's `cd service && python3 api.py` path are unverified.

---
_Digest of [ufal/atrium-project#58](https://github.com/ufal/atrium-project/issues/58). Sub-issue of #53 (factor VII). No container was started locally — every container claim above cites a GitHub Actions run and step._
