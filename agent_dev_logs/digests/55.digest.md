# 📓 Digest — [\#55](https://github.com/ufal/atrium-project/issues/55) `open`: Add `HEALTHCHECK` and `SIGTERM` handling to all five tool-repo services

> _Condensed from [`../issues/2026-09-07.55.issue.open.md`](../issues/2026-09-07.55.issue.open.md) — 0 comments, opener only. Explicitly scoped as "the concrete, actionable subset" of #53's broader twelve-factor checklist._
> _Thread: 2026-09-07 · K4TEL. Implementation pass: 2026-09-07 (local, unpushed)._

**Labels**: `enhancement`, `development` · Type: `Task` · Priority: `High` · Milestone: Q3 [WP8 / WP7: Advanced Transnational Access & Curriculum Drafting] · Parent: [\#53](https://github.com/ufal/atrium-project/issues/53)
· **Assignees**: none

**Status**: 🟢 **Implemented locally across all six repos, nothing pushed.** No thread discussion
yet. The issue's two named defects were real; measuring them turned up three more that block its
own acceptance criterion, so the scope grew — see 🔎 below.

## 🎯 Goal
Make the five published service images deployable unattended on ARÚP/ARÚB's Kubernetes: a
`HEALTHCHECK` in every Dockerfile, `SIGTERM` handled everywhere, and a rolling restart that does not
kill work already in progress.

## 🧵 Thread trail
- **2026-09-07, K4TEL** — opened, explicitly narrower than #53: factor **IX. Disposability** only
  (`HEALTHCHECK` is not itself a named twelve-factor item but serves the same operability goal),
  framed with an acceptance criterion rather than a checklist.

## ✅ What's settled (this pass)
- **The probe split.** `/health` = liveness, 200 **even while draining**; new `/ready` = readiness,
  503 the instant `SIGTERM` lands. Not cosmetic: a liveness probe that fails during a graceful
  shutdown gets the pod `SIGKILL`ed before the drain finishes, which is this issue's own failure
  mode moved one layer up. Keeping shallow `/health` byte-identical is also what leaves the five
  existing `test_health_shallow_ok` assertions and `skill-validate.reusable.yml`'s live probe
  untouched.
- **Signal chaining, verified empirically.** `serve_lifecycle` installs a `SIGTERM`/`SIGINT` handler
  that flips `draining` and then **calls the previously registered handler**. Against a real uvicorn
  0.52 subprocess, that previous handler is uvicorn's own `Server.handle_exit` — chaining is what
  keeps uvicorn's drain working; replacing it would break shutdown outright.
- **Exit code 143 is the correct clean exit**, not 0. uvicorn's `capture_signals()` deliberately
  re-raises the captured signal after a graceful shutdown so a supervisor sees the real cause. The
  CI assertion and the docs say 143; asserting 0 would have been asserting the wrong contract.
- **One implementation, five repos.** Everything service-side lives in the canonical
  `docs/templates/shared/atrium_service.py` (+ new `healthcheck.py`), re-vendored byte-identically
  and enforced by `para-drift.reusable.yml`.

## 🔎 What measuring the repos actually found
The two claims in the issue body were both true. Five further findings, in the order they matter:

1. **Kubernetes never reads a Docker `HEALTHCHECK`.** The kubelet's probes are pod-spec fields,
   independent of the image directive. So `HEALTHCHECK` alone could not make `/health` "visible to a
   Kubernetes liveness probe" — the partner-facing half had to be a probe contract plus a reference
   manifest (`docs/k8s_deployment.md`, `docs/templates/k8s/atrium-service.deployment.yaml`).
2. **Four of five services had no runnable API image.** Only nlp-enrich published one. alto,
   translator and pc reached FastAPI only via a compose `entrypoint:` override on the *batch* image;
   llm-enrich's `api` stage was declared but never built (roadmap **B5**). With the handoff boundary
   fixed at "a Docker image, run at their own Kubernetes" (#40, motyc), **there was nothing for
   ARÚP/ARÚB to pull** — the acceptance criterion was unreachable by configuration.
3. **A request counter cannot see the work that actually gets killed.** nlp-enrich's `POST /jobs`
   returned `queued` and left the job in a bare `asyncio.create_task(...)` with the result
   discarded. The request was long gone while the job ran, so uvicorn saw nothing in flight and
   exited — the issue's own sentence, exactly, and untouched by `--timeout-graceful-shutdown`.
   Fixed with `ServiceState.track()`, which the drain awaits (and which also retains a task
   reference that was previously GC-eligible even with no shutdown involved).
4. **Three services blocked the event loop**, so shutdown could not work at all there. pc, alto and
   translator called blocking inference inline in `async def` handlers; uvicorn's signal handler is
   an event-loop callback, so it could not run until the request returned. Moving those to
   `asyncio.to_thread` was a prerequisite, not a cleanup.
5. **nlp-enrich's shallow `/health` spawned a subprocess on every probe** — `run_pipeline.py
   --dry-run`, called before the `deep` check, from an `async def`, with no `timeout=`. Aiming a
   30s-interval `HEALTHCHECK` at it would have meant a subprocess and a loop stall every interval.
   Converged onto the shared `attach_health` with the dry-run behind `?deep=true` and bounded.

Plus two corrections to this repo's own records:
- **Roadmap B8 was already fixed** (alto's `uvicorn --reload`): `service/text_api.py:475` env-gates
  it, default `false`. Six documents still described it as open, including the previous version of
  this digest and `plans/55.plan.md`. Recorded in `docker_gha_roadmap.md` §8 per that file's own
  convention (append a dated round; never silently edit a findings row).
- **This digest's own cross-references were wrong.** It previously sent readers to "hub #56 and the
  standing partner ask for Kubernetes validation in the workplan's §7". **#56 is DOG/Language
  Resource Switchboard registration**, with nothing about Kubernetes, and no "workplan" document
  exists in the hub — the string occurred only in that sentence. #55's acceptance criterion had **no
  tracked vehicle at all**; `docs/k8s_acceptance_runbook.md` is now it.

## 📌 Scope decision
The prerequisites were absorbed into #55 rather than split out, so the issue can actually reach its
own acceptance bar. The cost is honest: this is well past "days of work" — 53 files across six repos
— and past the milestone's **2026-09-30** due date. Proposing the milestone move is part of the
GitHub-side follow-up rather than something to discover later.

## ⏳ Open
- **Nothing is pushed.** All six repos are dirty working copies on
  `claude/k8s-health-sigterm-jldemc`; the shared-code change is a one-window operation (hub edit +
  five re-vendorings + `v1` retag) per issue #10 finding **G4**, recorded in
  `scripts/revendor_shared.sh`'s header.
- The **container smoke has never been executed** — it needs Docker and a real image build, which
  this pass did not have. Its correctness rests on review, not on a green run yet.
- Each repo's **`agent-skill` branch** carries the unchanged `service/` tree, so it needs a refresh
  or `skill_drift_check.py` will report drift.
- **Partner acceptance** (`docs/k8s_acceptance_runbook.md`) is unrun; it needs a cluster and is by
  design out-of-band (#40).
- Deliberately still open: durable job records (#53 factors IV/VI), and the fact that a synchronous
  request longer than the grace budget is still cut short.
- See [`../plans/55.plan.md`](../plans/55.plan.md) for the per-action rollout and done-criteria.
