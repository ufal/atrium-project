# 📓 Digest — agent-skill ↔ test service-API alignment (#31 branch-sync · #32 meta-contract)

> _Branch-sync (#31 §12.2) that pulls the #32 OpenAPI meta-contract from each repo's `test`
> `service/` onto its `agent-skill` branch. Standard of truth:
> [`docs/agent_skill_strategy.md`](../../docs/agent_skill_strategy.md) §4/§5/§12.2 · related
> digests: [`31.digest.md`](31.digest.md), [`32.digest.md`](32.digest.md)._

**Labels**: `enhancement`, `development` · Milestone: Q3 [WP8/WP7]
**Status**: 🟢 **Landed on all five `agent-skill` branches (2026-07-2x)** — each carries the
shared `service/atrium_service.py` (byte-identical to the hub canonical, `md5 6557be0…`) and the
helper-based `service/api.py`(`text_api.py`) with the §4.1 `/info` envelope + `/health` + §4.4
codes. Skill-only assets preserved; runtime-dep gaps closed. **One residual**: page-classification
kept both `service/test_api.py` (old) and `service/api_client.py` (renamed) — delete the former.

## 🎯 Goal
Keep the `agent-skill` branches' `service/` **identical to the default/`test` service** (strategy
§5), so the skill wrappers front the same OpenAPI-standard API the pipeline ships — after the #32
meta-contract landed on `test`.

## 🧵 Thread trail
- **#32 (test branches)** — OpenAPI meta-contract landed on all five services + hub: shared
  `atrium_service.py`, `build_info` `/info`, `attach_health` `/health`, §4.4 codes, `MAX_UPLOAD_MB`.
- **Audit** — every `agent-skill` `service/` was pre-#32 (no `atrium_service.py`; inline
  `/info`/`/health`). Diverged per §12.2 (skill branch = trimmed derivative, kept current by
  syncing the default branch forward after service changes).
- **Sync prepared** — overlaid `test:service/` onto each `agent-skill` (add `atrium_service.py`;
  replace `api.py`/deps/README/frontends), preserving skill-only assets and excluding a stray blob;
  surfaced + fixed two nlp runtime-dep gaps. Delivered as full files.
- **Landed (maintainer)** — pc `9e61f0c` "fix API standard to align with test"; translator
  `01bbe75`; alto `fe44e59`; nlp `a85f8e5`; llm `1772374`.

## 📦 What exists now (origin/agent-skill)
- **All five**: `service/atrium_service.py` (== hub canonical); `/info` returns
  `service`/`version`(from `para_config.txt`)/`endpoints`/`limits` + capabilities; `/health`
  shallow+deep; §4.4 error codes; canonical `MAX_UPLOAD_MB` / `ALLOWED_ORIGINS`.
- **pc** `9e61f0c`: `+frontend-lindat/*` (resolves the #31 "phantom frontend-lindat" defect);
  `api_client.py` added; stray `service/Master_Diploma.pdf` correctly **not** carried over.
- **nlp** `a85f8e5`: `+api_util/bbox_scale.py` (new `rescale.py` imports it) and
  `+atrium_paradata.py` (was missing; `run_pipeline.py` needs it for `/health` dry-run + `/enrich`).
- **translator** `01bbe75` / **llm** `1772374`: their agent-skill-only `service/frontend/*`
  (+ translator `service/README.md`) **preserved** (test has none).
- Skill layer (`SKILL.md`, `scripts/atrium_*.py`, `server.sh`, `small_data_samples/`,
  `setup/para_config.txt`) unchanged — and forward-compatible (no stale `/info` shape or error codes).

## 🩺 Verified state
- `atrium_service.py` byte-identical (single md5) across all five `agent-skill` HEADs + hub.
- `compileall service/` clean everywhere; torch-free services (alto/nlp/llm) TestClient-smoke green
  from the landed HEADs — `/info` envelope, `/health` **200**, `app.openapi()` OpenAPI-3.1 valid.
- pc/translator import-skip on torch/fasttext (fast-lane behavior), structurally sound.
- Skill branches carry no contract test / api-contract CI (tests trimmed) — enforcement stays on
  the default/`test` branches; the skill service just tracks them.

## 🚦 Next steps
1. **pc**: `git rm service/test_api.py` on `agent-skill` (redundant with the renamed
   `service/api_client.py`) — the one incomplete rename.
2. **Re-acceptance** per branch: `scripts/server.sh` → client smoke on `small_data_samples/` →
   assert `/info`+`/health` (§31 W2), now against the aligned service.
3. **Re-tag / re-validate** skill branches once the §31 skill-validate CI is green (that check is
   separate from api-contract).
4. **Keep §12.2 sync live**: future `test` `service/` changes must forward-merge to `agent-skill`.