# 📓 Digest — [\#31](https://github.com/ufal/atrium-project/issues/31) `open`: AGENT SKILL — based on API service to each repo

> _No issue-log export yet (opened 2026-07-17, 0 comments at digest time); condensed from the
> live issue body + the strategy & execution sessions of 2026-07-17/18/20. Strategy source of
> truth: [`docs/agent_skill_strategy.md`](../../docs/agent_skill_strategy.md) · issue-level index:
> [`plans/31.plan.md`](../plans/31.plan.md)._

**Labels**: `enhancement`, `development` · Milestone: Q3 [WP8/WP7] · Type: Feature
**Status**: 🟡 **Skills implemented + finalized 2026-07-22 (delivered as files, not pushed); hub CI
wired, first CI run stale-failed** — all five `agent-skill` branches structurally complete and
functionally healthy; the hub reusable workflow + 5 caller workflows are landed and byte-verified,
but every CI run so far failed because the caller pushes (11:5x UTC 07-18) predated the reusable
landing on `test` (17:35 UTC same day) — needs a rerun or a trivial re-push per branch. The
2026-07-22 finalization pass closed all remaining locally-fixable drift — §4.4 415 alignment
(nlp+alto), CORS harmonized to the exemplar (nlp/alto/translator), §9 frontend API-docs (nlp), the
alto frontend response-schema fix (both variants), the page-classification residual test-deps
removal, the nlp-enrich README dangling-ref fix, and hub link hygiene — delivered as reviewed files
for maintainer landing. Remaining (needs a push / write-token / running models): green CI run,
forward-merge to default branches, §13 sub-issues, version tags, live acceptance, W5.

## 🎯 Goal
Make every service repo's API usable by LLM agents as an installable **Agent Skill**
([open standard](https://agentskills.io)): standardized entry points wrapped by a `SKILL.md` on
a dedicated `agent-skill` branch per repo, with the frontend UIs and `service/README.md`s
documenting API usage.

## 🧵 Thread trail
- **2026-07-17, K4TEL** — opened: check OpenAPI/agent-skill standards; standardize entry-point
  schemas; `SKILL.md` wrappers on separate `agent-skill` branches; page-classification already
  has the example branch.
- **2026-07-17 (session)** — all six repos surveyed (exemplar dissected; 4 defects catalogued),
  strategy decisions fixed, `docs/agent_skill_strategy.md` + `plans/31.plan.md` + this digest authored.
- **2026-07-18 (execution session)** — full rollout of §10.1–10.5; then a missing-file backfill and
  a trim/refinement pass. Maintainer (k4tel) landed the results directly on the `agent-skill`
  branches, closing runtime-breaking gaps (added `atrium_paradata.py`, `para_config.txt`,
  nlp-enrich `service/{enrichment,jobs,rescale}.py`) and applying the trim lists.
- **2026-07-18 (planning)** — refinement strategy authored: all workstreams in scope; **manual**
  drift model. §10 build items re-marked `[x]` against verified branch state.
- **2026-07-18, 17:35 UTC (maintainer)** — `skill-validate.reusable.yml` landed on hub `test`/`main`
  and the 5 `.github/workflows/skill-validate.yml` callers landed on each `agent-skill` branch
  (pushed ~11:5x UTC, **before** the reusable existed on `test`) — every first CI run failed with
  0 jobs (bad `uses:` resolution at push time), not a real defect.
- **2026-07-20 (state check)** — hub `main`/`test` converged at `33b79c0`; confirmed the reusable
  workflow, 5 callers, and refined digest/plan/strategy landed; found the CI-timing gap, two stale
  cross-doc links (renamed files), an incomplete `docs/templates/skill/` promotion, and a missing
  `docs/templates/workflows/skill-validate.caller.example.yml` + `.gitignore` — fixed in this pass.
- **2026-07-22 (session)** — authored a per-repo strategy plan, then, at the maintainer's request,
  **implemented the remaining W1/W3 drift-fixes** as working-tree edits (delivered as full files for
  review; not yet committed/pushed): nlp-enrich — 415 via new `UnsupportedMediaTypeError` (→ `/enrich`
  + `/jobs`), CORS→exemplar (`GET/POST/DELETE`), "API usage" footer added to **both** frontends,
  SKILL/README error-table sync; alto-postprocess — 415 for unsupported type, missing filename/CT
  500→422/415, frontend mount `/static`→`/frontend` (`html=True`) + `frontend-lindat` mount + `/`
  redirect, CORS guard, README sync; translator — CORS `["*"]`→`["GET","POST"]`; llm-enrich — SKILL
  typo; hub — the two stale doc links fixed. All edited `.py` byte-compile.
- **2026-07-22 (finalization)** — audited all five branches, then closed the remaining locally-fixable
  gaps: **alto-postprocess frontends** (both `frontend/` + `frontend-lindat/`, `index.html` + `script.js`)
  rewired from the stale `{type, cleaned_lines, raw_text}` shape to the real `/process` response
  `{task_type, num_lines, reading_order, lines, filename}` — previously the UI rendered empty tables
  (`node --check` clean); **page-classification** residual `setup/requirements-test.txt` removed with its
  two `Dockerfile` refs (test tooling no longer baked into the image — a deletion the maintainer must
  replicate via `git rm`); **nlp-enrich** `service/README.md` dangling `service/test_api.py`/`tests/`
  smoke-test refs replaced with the real `server.sh` + client flow and an explicit "tests live on the
  `test` branch" note. Audit confirmed the rest clean (SKILL order/frontmatter, path refs, client/API
  compiles, CI triggers, hub link tokens).

## 📦 What exists now
- **Five live `agent-skill` branches** (HEADs 2026-07-18): page-classification `97ddbfe`,
  nlp-enrich `e1c3b2d`, alto-postprocess `365b10c`, translator `1cc945a`, llm-enrich `cc5c4da`.
  Each: `SKILL.md` (frontmatter `name`==repo, routing description), zero-dep stdlib client
  (`scripts/atrium_<verb>.py`, all pass `--help`), idempotent `scripts/server.sh`, `service/`
  (`/info`+`/health`+domain endpoints, version from `para_config.txt`) with API-footer frontends
  (nlp-enrich's **two** frontends got the footer only in the 2026-07-22 pass — previously missing)
  and `service/README.md`, flattened/trimmed tree, `small_data_samples/`, Docker `api` profile.
- **Exemplar hardened**: page-classification's 4 defects (serve.sh naming, phantom paradata claim,
  phantom `frontend-lindat/`, over-documented response fields) all fixed and verified clean.
- **The standard** ([`docs/agent_skill_strategy.md`](../../docs/agent_skill_strategy.md) §§4–9):
  uniform `GET /info` (`service`/`version`/`endpoints`/`limits`) + `GET /health` (+`?deep`), error
  table 413/415/422/429/500/503, canonical `MAX_UPLOAD_MB`, client env `ATRIUM_{PC,TR,AP,NE,LE}_URL`,
  domain endpoints untouched; no committed OpenAPI snapshots (live `/openapi.json` + contract tests).
  The **415** row of that table was aspirational for two repos until the 2026-07-22 pass —
  nlp-enrich and alto-postprocess previously folded unsupported types into 422/500; both now return
  415 (pending push).
- **Templates**: `docs/templates/skill/` (Appendices A–D) and
  `docs/templates/workflows/skill-validate.caller.example.yml` — landed partially on `main` (only
  `atrium_client.skeleton.py`; the other 4 template files + the caller example are re-delivered in
  this pass). Hub index: `docs/skills_catalog.md` · `docs/skill_acceptance_runbook.md` (both landed).

## 🩺 Verified state (2026-07-20)
Green across all five `agent-skill` branches: SKILL frontmatter valid; clients `--help` succeed
(zero-dep proven); service import closures intact; `/info` reads `para_config.txt`; no dangling
doc refs; no false paradata claims. Hub CI infra (reusable + 5 callers) confirmed byte-identical
to source. **Open gaps** → CI never ran successfully yet (timing, see above — needs a rerun);
`atrium-llm-enrich@main` lacks an API test; the §10.1/10.3/10.4 "default-branch pre-work" fixes
confirmed **not yet merged forward** to the default branches (verified: `atrium-page-classification
@vit` still has the pre-fix `api.py` — no `/health`, `400` not `415`) — this is the documented
§12.2 manual-sync item, not a regression; no sub-issues created (unverifiable — `list_issues`/
`create_issue` return `403` for this integration); no version tags yet; no live end-to-end
acceptance run; minor consistency debt (W3, untouched).

## 🚦 Next steps (refinement — all workstreams; manual drift)
1. **W1 Validation/CI** — §4.4 **415** alignment done on nlp+alto (2026-07-22, pending push); still:
   get one green CI run per `agent-skill` branch (rerun or re-push); port the llm-enrich contract
   test to `main`; audit the other 4 default-branch API tests for §4.1 assertions.
2. **W2 Acceptance** — per repo: `server.sh` → client smoke on `small_data_samples/` → assert
   `/info`+`/health`; clean-room `~/.claude/skills/` round-trip using only SKILL.md.
3. **W3 Consistency** — CORS harmonized to the exemplar on nlp/alto/translator and nlp-enrich
   frontend API-docs added (2026-07-22, pending push); still: uniform SKILL section order/markdown;
   drop residual dev files; optionally wire paradata into the llm-enrich service; merge the
   §10.1/10.3/10.4 default-branch fixes forward.
4. **W4 Ecosystem** — §13 sub-issues `agent-skill: <repo>` × 5 (needs a token with Issues:write —
   this integration can't create them); version-tag each skill branch; AGENTS.md pointers;
   hosted-deploy plan (parked).
5. **W5 (optional)** — llm-enrich async jobs; more batch/endpoint modes; multi-language.
