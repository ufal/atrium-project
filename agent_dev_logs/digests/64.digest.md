# 📓 Digest — [\#64](https://github.com/ufal/atrium-project/issues/64) `open`: Dev/prod parity: same Python version for CI lanes

> _Condensed from [`../issues/2026-09-10.64.issue.open.md`](../issues/2026-09-10.64.issue.open.md) — **2 comments** (2026-09-10 13:03 sweep result, 2026-09-14 11:38 status claim). The export was generated 2026-09-14 10:25 and therefore predates the second comment; the count above is the live thread._
> _Thread: 2026-09-10 · K4TEL. Sweep pass: 2026-09-10. **Re-measured verification pass: 2026-09-14** (this refresh)._

**Labels**: `enhancement`, `development` · **Parent**: [\#53](https://github.com/ufal/atrium-project/issues/53) (factor X, plan item F) · **Closes**: roadmap **B7** · **Touches**: roadmap **D1**

**Status**: 🟡 **Open, and not closable. Acceptance bullet 1 is met in full; bullet 2 is not — and the comment posted 2026-09-14 11:38 says the remaining work was done when it was not.** The Python-version half landed on 09-13 and the nlp-enrich half of B7 on 09-14, both with guards behind them. What is still broken is the **hub's own** half of B7: `docs/templates/skill/server.template.sh:70` still hardcodes the `.yml` compose pair, and `docs/docker_gha_roadmap.md:285` still prescribes normalising every compose file to `.yml` — the opposite of the decision this issue took. Neither was touched.

## 🎯 Goal
Keep what CI runs and what ships the same. Two mechanical divergences, both cheap, one of which has a way to go wrong.

## 🧵 Thread trail
- **2026-09-09** — #53's sweep marks factor X 🟡: one 3.12 lane, roadmap B7 still live, and CI never running the real entrypoint (that third part became [\#62](https://github.com/ufal/atrium-project/issues/62)).
- **2026-09-10 12:26, K4TEL** — opened as #64 with both mechanical items, a "sweep the other four repos" instruction, and two acceptance bullets.
- **2026-09-10 13:03, K4TEL** — comment: the sweep result. nlp-enrich is the only broken header; do not find-and-replace, because `annotation/docker-compose.yml` is a genuine `.yml`; recommend not converging the extension split.
- **2026-09-13** — the Python-version half lands in translator `fbb90e9`, *"edit for pre-production release"* — a commit message that says nothing about #64 — together with `tests/test_python_version_parity.py`. Not reported on this thread at the time.
- **2026-09-14** — nlp-enrich `b1a62e8` (*"fixes for env standard"*) fixes the GPU compose header; hub `f675943` adds `tests/test_hub_python_version_parity.py`; alto `f6d9196` and pc `1e2cf9a` carry the README/ruff collateral.
- **2026-09-14 11:38, K4TEL** — comment: claims part (a) was already done (true), the nlp-enrich header fixed (true), **and that the hub's `server.template.sh` was made to resolve the compose extension at runtime and the roadmap amended to match (neither happened — see ❌ below).**
- **2026-09-14** — this re-measurement. Both files below were written 09-10 and never revised while all of the above landed.

## 🔎 Verified state — 2026-09-14

Measured against hub `f675943` · alto `f6d9196` · nlp `bfabfca` · llm `5ac82a5` · pc `f11936f` · translator `d0c1572`. Every citation below was re-derived from the working tree; the issue's own line numbers have drifted.

### 1. Acceptance bullet 1 — **met**

`python-version` is **3.11 in every lane in all six repos. Zero 3.12.** Twenty-five declarations, counted by hand (the three `${{ inputs.python-version }}` passthroughs at `api-contract.reusable.yml:51`, `skill-validate.reusable.yml:252` and `pre-commit.reusable.yml:54` are templates, not values, and are excluded):

| Repo                      | Lanes                                                                                                                                                                                                           | Values                   |
|---------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------|
| hub                       | `workflow-lint.reusable.yml:71` · `security.reusable.yml:50` · `docker-tool.reusable.yml:116` · `e2e-pipeline-smoke.yml:110` · `hub-self-check.yml:65` · `e2e-digital-smoke.yml:152` · `all-repos-smoke.yml:75` | all `3.11`               |
| hub (reusable **inputs**) | `api-contract.reusable.yml:30` → `default: "3.11"` (`:34`) · `codeql.reusable.yml:29` → `:33` · `skill-validate.reusable.yml:57` → `:61` · `pre-commit.reusable.yml:25` → `:29`                                 | all four defaults `3.11` |
| alto                      | `scheduled-smoke.yml:42`                                                                                                                                                                                        | `3.11`                   |
| nlp                       | `teitok-schema.yml:68` · `vocab-drift.yml:98` · `vocab-refresh.yml:67` · `scheduled-smoke.yml:67` · `gpu-inference.yml:59`                                                                                      | all `3.11`               |
| llm                       | `vocab-drift.yml:78` · `vocab-refresh.yml:67` · `scheduled-smoke.yml:69` · `gpu-inference.yml:59`                                                                                                               | all `3.11`               |
| pc                        | `scheduled-smoke.yml:30`                                                                                                                                                                                        | `3.11`                   |
| translator                | `scheduled-smoke.yml:63` · `integration.yml:60,86`                                                                                                                                                              | all `3.11`               |

The four reusable-workflow **input declarations** matter disproportionately: every caller-side lane backed by a hub reusable is a bare `uses:` with no `with:` block, so the version those lanes actually run is whatever the reusable defaults to. All four were checked individually and all four read `default: "3.11"`.

All five images are `python:3.11-slim` (`Dockerfile` in alto, nlp, llm, pc; translator's declares it twice).

**The issue's citation is stale, not wrong.** It names `atrium-translator/.github/workflows/scheduled-smoke.yml:51` as the 3.12 lane. That line is now **`:63`** and reads `python-version: '3.11'` — the file grew by the rationale comment block that documents the change.

Landed in translator **`fbb90e9`, 2026-09-13**, whose message is *"edit for pre-production release"* and mentions neither #64 nor Python. The same commit added `tests/test_python_version_parity.py` (66 lines).

### 2. The D1 scope decision — **recorded, in the workflow itself**

The old plan's §B asked for the decision to be made and written down, and named two defensible outcomes. The second one was taken.

- `atrium-translator/.github/workflows/scheduled-smoke.yml:11-19` states it: *"It is deliberately NOT `-m slow`, and that decision is re-confirmed here rather than inherited (roadmap D1, atrium-project#64)"*, then retires the original justification (it stopped being true when #58 added a slow-marked subprocess probe) and argues the surviving half — nlp-enrich and llm-enrich both ran `-m slow` nightlies that collected nothing and reported success.
- `:57-62` carries the twelve-factor-X rationale for the pin, naming `atrium-project#64`, immediately above `python-version: '3.11'` at `:63`.
- `:76` is still `run: pytest tests/ -rs` — the whole suite.

That is exactly outcome 2, *"stay broad, with the reason in a comment"*. **The box is ticked.**

### 3. B7, nlp-enrich half — **fixed**

`atrium-nlp-enrich/docker-compose.gpu.yaml:1-3` now reads `.yaml` throughout:

```
# GPU override — combine with docker-compose.yaml:
#   docker compose -f docker-compose.yaml -f docker-compose.gpu.yaml run --rm nlp
#   docker compose -f docker-compose.yaml -f docker-compose.gpu.yaml --profile llm run --rm nlp-llm
```

Both files on disk are `.yaml`, so both printed commands now match. Landed **`b1a62e8`, 2026-09-14**. The trap the old plan §C named was respected: `annotation/docker-compose.yml` is untouched and `annotation/env.example` is still beside it — the standalone campaign deployment still starts. The comment in `api_util/call_udpipe.py:141` was corrected to `.yaml` in the same commit.

### 4. Two guards now hold the version

- `atrium-translator/tests/test_python_version_parity.py` — derives the expected version from the repo's own Dockerfile and checks every `python-version:` in its `.github/workflows/*.yml` (`:45-46`) plus `ruff.toml`'s `target-version` (`:58-65`) against it.
- `atrium-project/tests/test_hub_python_version_parity.py` — the hub side, added in `f675943`. It anchors on `docker-tool.reusable.yml`'s own hardcoded `3.11` (the lane that builds and probes the published images), then checks every hub workflow, asserts **every `workflow_call` `python-version` input has a `default:`** at all (`:107-120`, so a defaultless input cannot hide), and checks the hub's `ruff.toml:28`. Its docstring names the hole it exists for: a hub-side `default: "3.12"` would be invisible to every tool repo's own parity test.

This is the old plan's §E — *"consider making the version stick"* — implemented **twice over**, hub and caller. It was marked optional; it was done anyway, and correctly.

## ❌ The live defect — and the comment that says otherwise

The 2026-09-14 11:38 comment states that the hub's own `docs/templates/skill/server.template.sh` *"hardcoded `.yml` too … made it resolve the extension at runtime instead"*, and that the roadmap, *"contradicting this issue's own plan"*, was *"amended to match, with the reasoning recorded"*. **Neither happened.** Checked by hand against `f675943`:

- `grep -n docker-compose docs/templates/skill/server.template.sh` returns **one line**, `:70`:
  `    gpu)   start_docker -f docker-compose.yml -f docker-compose.gpu.yml ;;`
  The file contains **zero** occurrences of `yaml` and zero of `#64`. A skill generated from this template in nlp-enrich or llm-enrich — the two `.yaml` repos — fails on `--mode gpu`. That is acceptance bullet 2, failing.
- `docs/docker_gha_roadmap.md:285` still reads *"normalise every compose file to `.yml` (B7), which also repairs `server.template.sh`"* — the direct opposite of §D of the plan below.
- `grep -n "#64" docs/docker_gha_roadmap.md` returns **nothing**. No reasoning was recorded anywhere in that file.

So **roadmap B7 is half-open, and it is the hub half.** This is the whole of what stands between #64 and closure.

Two further roadmap lines are stale in the same neighbourhood and should move with it:

- `:84` — the B7 row still describes the nlp-enrich header as live (it is fixed) and cites `server.template.sh:63` for a line that is now `:70`.
- `:117` — the D1 row still says *"Translator is the ecosystem's only **Python 3.12** job (everything else, hub included, is 3.11)"*. It has not been since `fbb90e9`.

## 🔎 One more live instance of the same bug class

In the very file the 09-10 comment cited as evidence: `atrium-nlp-enrich/annotation/README.md`.

- `:14` — the file table lists `` `docker-compose.yml`, `.env.example` ``
- `:28` — prints `cp .env.example .env          # Edit LS_ADMIN_USERNAME and LS_ADMIN_PASSWORD`

The file on disk is **`annotation/env.example`**, undotted. The copy-paste command at `:28` fails verbatim. Both issue comments spell the undotted name correctly while the README sitting next to the file does not — the identical shape as B7 (a doc disagreeing with its own directory), and identical to the dotted-template defect #60 is carrying.

## 🔎 Collateral worth recording

Landed alongside, not asked for by the issue:

- **Service READMEs corrected**: `atrium-alto-postprocess/service/README.md:239` and `atrium-page-classification/service/README.md:183` now both say **Python 3.11**, each naming `python:3.11-slim` and `atrium-project#64` (alto `f6d9196`, pc `1e2cf9a`, both 2026-09-14).
- **Repo-root READMEs still advertise 3.10+**: `atrium-alto-postprocess/README.md:2` (badge `python-3.10+`), `atrium-page-classification/README.md:2` (same badge) and `:288` (*"Make sure you have **Python version 3.10+** installed"*). A partner reads the root README first; it contradicts the service README two directories down.
- **`ruff.toml` `target-version`**: all five tool repos now carry `py311` — alto `:15`, nlp `:3`, llm `:10`, pc `:9`, translator `:14` — pc's added in `1e2cf9a`, translator's in `fbb90e9`. But **`docs/templates/ruff.toml`, the template the five derive from, has no `target-version` at all**, and neither parity guard reads it: both check their own repo's root `ruff.toml`. The one file that would propagate the pin is the one place it is absent.

## ✏️ Corrections to this digest's own previous edition

The 09-10 edition was accurate when written and was never revised. Four of its statements are now false, and so are two in its paired plan.

1. **Header** — *"**0 comments, opener only** (2026-09-10 12:26)."* There are **2** comments: 2026-09-10 13:03 and 2026-09-14 11:38. The first was already posted when this line was written.
2. **Status** — *"🟡 **Open. Both findings confirmed; the sweep is done and it changes the second one.**"* Still open, but for an entirely different reason. Finding 1 is closed; finding 2's cited instance is closed; what remains is the hub-side artefact the 09-10 edition did not measure at all.
3. **`## 🔎 Finding 1 — the 3.12 lane (confirmed, unchanged)`** — heading and body. *"`…/scheduled-smoke.yml:51` pins `python-version: '3.12'`"* and *"Grepping `python-version` across all six repos' workflows returns **exactly one** non-3.11 value, and this is it."* Both untrue since 2026-09-13. The line is `:63` and reads `'3.11'`; the grep returns zero non-3.11 values. The heading's *"confirmed, unchanged"* is the exact present-tense-over-a-landed-fix pattern this refresh exists to remove.
4. **Sweep table, nlp-enrich row** — *"`docker-compose.gpu.yaml` … **says `.yml`** (`:1-3`) · 🔴 **B7**"*. It says `.yaml` and has since `b1a62e8`. The other four rows of that table still verify exactly as written.
5. **Paired plan's status line** — *"**Status: 🟡 Not started.**"* Three of its five lettered actions (§A, §B, §C) are complete and §E, marked optional, is complete twice over. Only §A's hub-side residual — which the plan did not know about — is outstanding.
6. **Paired plan's done-criteria** — all six boxes are unticked. **Three are demonstrably satisfied** (the six-repo `python-version` grep; the D1 decision recorded in the workflow; `annotation/docker-compose.yml` untouched), one is satisfied by inspection but not by execution (the header commands — no Docker daemon here), and the last two are genuinely open, one of them by a file the plan never named. They are re-stated with evidence in the refreshed plan.

**Still vindicated, and worth saying so:**

- **§D, "leave the extension split alone."** Correct then, correct now, and the hub is the only thing that has not caught up with it. `docs/docker_gha_roadmap.md:285` is the surviving contradiction, and the fix is to amend the roadmap to match §D — not the other way round.
- **§E, "consider making the version stick."** Recorded as optional; implemented twice (translator caller-side, hub anchor-side), including the reusable-`default:` hole that neither the issue nor the plan anticipated.

## ⏳ Open

- **The hub half of B7.** `docs/templates/skill/server.template.sh:70` and `docs/docker_gha_roadmap.md:285` — plus the two stale roadmap rows at `:84` and `:117`. Hub-only, docs and a template; no retag.
- **A correction on the thread.** The 11:38 comment asserts both of the above were done. Left standing, the defect will not be found again.
- **`annotation/README.md:14,28`** — dotted `.env.example` against an undotted `env.example` on disk. Same bug class, one line each.
- **Root READMEs and the ruff template.** alto `README.md:2`; pc `README.md:2` and `:288`; `docs/templates/ruff.toml` with no `target-version`.
- **Acceptance bullet 2 was never *run*.** It says "verify by running them, not by reading them". No Docker daemon is available in the measuring environment, so every compose claim here is read, not executed — `docker compose config` on a runner would settle it.

---
_Digest of [ufal/atrium-project#64](https://github.com/ufal/atrium-project/issues/64). Sub-issue of #53 (factor X). Measured against hub `f675943`, alto `f6d9196`, nlp `bfabfca`, llm `5ac82a5`, pc `f11936f`, translator `d0c1572` — in all six the default branch equals `test` equals the measured tree; the hub is one docs-only commit behind `origin/test`, which re-exports `agent_dev_logs/issues/*.md` and touches nothing cited above. No container was started (no Docker daemon in the measuring environment): every compose claim is read from the tree, not executed._
