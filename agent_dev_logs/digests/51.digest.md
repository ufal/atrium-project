# 📓 Digest — [\#51](https://github.com/ufal/atrium-project/issues/51) `open`: Request DARIAH Vocabs Service Namespace

> _Condensed from [`../issues/2026-09-04.51.issue.open.md`](../issues/2026-09-04.51.issue.open.md) — 1 comment. Directly downstream of `atrium-nlp-enrich` issue #6's AMCR+TEATER vocabulary work._
> _Thread: 2026-09-04 · K4TEL, motyc._

**Labels**: `question`, `beginning`, `integration` · Milestone: Q3 [WP8 / WP7] · no assignees

**Status**: 🔵 **Deferred, explicitly non-blocking.** @motyc answered same-day: a separate,
18-month SKOSification project already covers this ground; don't let it block anything here.

## 🎯 Goal
Publish the harmonised AMCR+TEATER vocabulary (built in `atrium-nlp-enrich`'s issue #6) as SKOS,
which requires a DARIAH Vocabs Service namespace first. `vocab_sources.py` already captures the
AMCR `skos:exactMatch` URIs (`odkaz` field) during harvest, but nothing currently exports them —
there's no SKOS output path at all yet, namespace or not.

## 🧵 Thread trail
- **2026-09-04, K4TEL** — opened: requests coordinating a formal DARIAH Vocabs namespace request;
  notes the target decision date is **mid-September**.
- **2026-09-04, motyc** — responds same day: this is a "wider issue and definitely not to solve
  now and here." A **commencing SKOSification project for AMCR and TEATER**, led by
  **@petrpajdla**, already exists on an **18-month** timeline and will land the real namespace/PID
  infrastructure. Explicit instruction: **don't make this a blocking issue** — the vocabulary's
  current term IDs stay as-is for now and will become PID references to the published SKOS version
  once that separate project ships. Using SKOS as an internal alignment *framework* (not the
  externally-published, namespaced version) remains desirable and unblocked right now.

## ✅ What's settled
- **No namespace request will be made from this issue.** The eventual namespace comes from
  @petrpajdla's project, on its own 18-month timeline, not from a request initiated here.
- **Current vocabulary IDs are stable for now** — `(source, id)` pairs in
  `atrium-nlp-enrich`/`atrium-llm-enrich`'s vocabulary tooling don't need to anticipate a namespace
  change; they become PID references later, as a mechanical follow-on once the SKOS project ships.
- Internal SKOS-*framework* alignment (structuring the vocabulary the way SKOS would, without a
  published namespace) is fine to do now if useful.

## ⏳ Open
- The mid-September decision date @K4TEL named in the opening post is effectively **superseded**
  by @motyc's answer — there is no decision to make on this repo's timeline anymore; the decision
  belongs to the separate SKOSification project.
- Whether/when to check back in with @petrpajdla's project for status is the only realistic
  follow-up, and it's not urgent per @motyc's framing. See
  [`../plans/51.plan.md`](../plans/51.plan.md).
