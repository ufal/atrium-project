# 📓 Digest — [\#51](https://github.com/ufal/atrium-project/issues/51) `open`: SKOSification of internal data - DARIAH Vocabs Service standards

> _Condensed from [`../issues/2026-09-04.51.issue.open.md`](../issues/2026-09-04.51.issue.open.md) — 3 comments (motyc 09-04; K4TEL 09-17 and 09-25). Directly downstream of `atrium-nlp-enrich` issue #6's AMCR+TEATER vocabulary work._
> _Thread: 2026-09-04 → 2026-09-25 · K4TEL, motyc. Acted on 2026-09-08. **Refreshed 2026-09-26 (AMČR baseline, [#67](https://github.com/ufal/atrium-project/issues/67))** — deferred._

**Labels**: `enhancement`, `development`, `beginning`, `integration` · Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover _(relabelled 2026-09-08; was Q3 [WP8 / WP7])_ · no assignees

**Status**: ⏸️ **Deferred — after the pilot (AMČR, #67).** Nothing is lost: the actionable half was built on 09-08, and the remaining follow-ups wait.
_Before this refresh:_ 🟢 **Split in two, and the actionable half is done.** The namespace request stays
deferred to @petrpajdla's 18-month project, exactly as @motyc directed. The internal SKOS alignment
he called "definitely desirable" is built — see [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md).

**Refreshed 2026-09-24 — the 2026-09-17 status comment (5709754260) is not in the body below.** Direction unchanged: no namespace requested, current ids kept. Five of the eight follow-ups in `docs/skos_strategy.md` §7 are taken: **V-1** fixed (llm-enrich's line filter matched only digital-convert's categories, so on the OCR path every garbage line reached the model; it now uses `UNTRUSTWORTHY_LINE_CATEGORIES`), **V-2** fixed (page-classification's `collect_images()`), **F6** built (`enrichment.items[].teater_category_uri` carries the source concept's URI, via `concept_index()`), **F8** done, **F3** half (`load_vocab.py --from-flat`). Worth a reviewer's eye: the regenerated translator CSV is 159 terms lighter (5,087 → 4,952), and **V-5** — `teater_category` holds a source label (`kostel`), not one of the eleven ATRIUM themes, so the crate used to mint `w3id.org/atrium/theme/kostel`; it now prefers the source URI. The contract question (source label or theme in that field) is open for the maintainers. Still open: F4, F5 (register the `w3id.org/atrium` redirect), F7.

## 🧭 AMČR baseline — 2026-09-26 ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ⏸️ defer — after the pilot.** Decided by motyc (AMČR) in #67, adopted by ÚFAL; no comment on this thread.
- **Deferred:** F4, F5, F7, the V-5 contract question and the harvester retirement (Open, below).
- **Correction recorded (K4TEL, 2026-09-25, 11:39):** V-1 was fixed on **2026-09-25** in llm-enrich `0aef037`
  (`json_to_md.DROP_CATEGORIES` → `UNTRUSTWORTHY_LINE_CATEGORIES`, pinned by
  `test_convert_drops_trash_lines_on_the_ocr_path`), not on 09-16 as the 09-17 status said.
- **Where it meets the pilot:** #67 R7 asks for **one versioned vocabulary artifact** under `para-drift`, and
  nlp-enrich without its vocabulary code. The SKOS export (`docs/skos_strategy.md`, `atrium_vocab.py`) is the
  natural home of that artifact's format; building the artifact is #67's work, not this issue's.

## 🎯 Goal

Publish the harmonised AMCR+TEATER vocabulary as SKOS, which the issue assumed required a DARIAH
Vocabs namespace first. `vocab_sources.py` already captured the AMCR `odkaz skos:exactMatch` URIs
and nothing exported them.

## 🧵 Thread trail

- **2026-09-04, K4TEL** — opened: coordinate a formal DARIAH Vocabs namespace request; target
  decision date **mid-September**.
- **2026-09-04, motyc** — same day: a "wider issue and definitely not to solve now and here." A
  **commencing SKOSification project for AMCR and TEATER**, led by **@petrpajdla**, exists on an
  **18-month** timeline. **Do not make this a blocking issue**; current term IDs stay as they are
  and become PID references later. **EDIT:** *"This does not prevent using SKOS as framework to
  align the data internally. It is definitely desirable."*

## ✅ What's settled

- **No namespace request from here.** Unchanged.
- **Current IDs are stable, and now stable by construction.** ATRIUM mints no URI for any harvested
  concept, so there is nothing to migrate off when the real PIDs arrive.
- **The publication dependency applied only to publication.** Everything @motyc encouraged —
  internal alignment — needed no namespace, and is now built: a hub-canonical controlled-label
  registry (`atrium_vocab.py`, 6 schemes / 64 concepts / 75 mappings) and a SKOS view of the
  harvested vocabularies (`vocab_build.py --skos` → 5,594 concepts, 55,188 triples), both
  drift-gated by the mechanisms already in place.
- **The mid-September date is met** in the only sense left open to this repo: the URI policy is
  decided (`https://w3id.org/atrium/`, one swappable constant), and it commits nobody to anything.

## 🔎 What's already in place (verified against the tree, not from this thread)

- 707 `skos:exactMatch` URIs on 668 AMCR concepts (555 Getty AAT, 143 n2t.net) — harvested since
  issue #6, read by one reviewer column, **now exported and resolvable at runtime**.
- `entities[].pid.{wikidata,geonames,aat,amcr}` — declared in schema 1.0 as *"the ARIADNE/GoTriple
  hook"*, owned by llm-enrich, **written by nothing**. 432 labels now resolve to a Getty AAT concept
  via `vocab_manager.resolve_pid()`.
- Three live defects found in the gap, all from uncontrolled labels crossing repo boundaries — most
  notably **V-1**: `lines[].categ`'s two originators emit disjoint label sets and the only consumer
  filters on one, so `DROP_CATEGORIES` matched nothing on the OCR path. Documented on 2026-09-08 and
  deliberately not fixed then, because it changes what text reaches the model. ✅ **Fixed 2026-09-16**
  once the owner took that call; V-2 with it. A fourth, **V-5**, was found on the same pass — see
  Open below.

## ⏳ Open

- The namespace itself, and the eventual PID migration — @petrpajdla's project, unchanged, not
  urgent, and now a redirect rather than a data change.
- **Five of the eight follow-ups were taken on 2026-09-16**: F1 (the V-1 fix — it changes what
  text reaches the model, and the owner took it), F2 (V-2), F6 (`teater_category_uri`, so the 707
  captured URIs finally have somewhere to go), F8 (orphan fixture), and the half of F3 that removes
  the *need* to run the translator's duplicate harvester — `load_vocab.py --from-flat` rebuilds the
  vocabulary from nlp-enrich's committed artifacts. The regenerated CSV is five-column and
  **159 terms lighter** (24 gained, 11 retranslated): the reproducibility-vs-coverage trade V-3
  names, worth a second look if those terms matter.
- Still open: **F4** (needs the CNEC reference), **F5** (register the `w3id.org/atrium` redirect —
  external, blocks nothing), **F7** (needs network), and the full retirement of the duplicate
  harvester (needs a release plan).
- **New: defect V-5.** `enrichment.items[].teater_category` holds a SOURCE label (`kostel`), not one
  of the eleven ATRIUM harmonisation themes, and both `validate_labels` and the crate's
  `CONTROLLED_TERMS` read it as a theme — so the exporter was minting
  `w3id.org/atrium/theme/kostel`, an ATRIUM-authored identifier for an AMCR concept, against §3.
  Mitigated (the crate now prefers the source URI); the contract question needs an owner.
  See [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md) §6-§7.
