# 🔎 Findings register — [\#57](https://github.com/ufal/atrium-project/issues/57), relocated from the published site

> _Round 6 (2026-09-23). The site under `docs_site/` now carries only lasting descriptions. Every
> audit finding the round-3 → round-5 pages published — "Known drift" lists, defect write-ups,
> "the README says X, the code does Y", open-issue status — was moved here instead, so nothing is
> lost and nothing stale is published. This file lives outside `docs_site/`, so MkDocs never
> builds it._
>
> _Each line is a **candidate issue in the repository that owns the fix**. Status is as re-checked
> in round 6 against page-classification `vit` @ `adee922`, translator `master` @ `71feaef` and
> hub `main` @ `7b0b84e`: **open**, **resolved @ `<sha>`**, or **unverified** (not re-run this
> round). IDs in brackets are the round-6 audit IDs they came from._

## page-classification

### Numbers and prose that disagree

- **open** — README Results table gives `v4.3` Top-1 = **98.92 %**; the prose (`README.md` ≈ :781), `model_accuracies_top1.csv` and the HF model card say **99.16 %**. [KD-2]
- **unverified** — `v4.2` and `v5.2` accuracies swapped between the README table and the prose discussing it. [KD-1]
- **open** — README base-model table and Acknowledgements list `regnety_160` at **224 px**; `MODEL_STATIC` says **384**. [KD-3]
- **open** — README Versions table names `v1.2` = `efficientnetv2_s.in21k`, `v4.2` = `efficientnetv2_l`, while `v1.3/1.4` = `efficientnetv2_m`, `v4.3/4.4` = `regnety_160`; the "strange order" is acknowledged and never explained in the README (the explanation is the sweep vs published namespace, now on the Reference page). [KD-4]
- **open** — no accuracy figures published for the `v*.4` generation `--best` uses since v1.8.0-beta; the only measurement is the 24-of-229 prediction diff (DEVLOG #15). Tool issue **#48**. [KD-5, I-6, H-5]
- **resolved @ `adee922`** — README dataset licence: README §Data, `para_config.txt` and `small_data_samples/LICENSE` all now say **CC BY-NC 4.0**; round-3 pages had said CC BY-NC-SA. [I-7, R-8]
- **open** — hub `docs/document_schema.md` and the schema's `page_categories.description` say the label list is "derived from the filesystem at runtime (utils.py collect_images)"; only `--train`/`--eval` do that, inference uses `model_registry.CATEGORIES`. (Hub-owned text.) [I-3]

### Defaults and behaviour that differ from the prose

- **open** — README says `--file_format` defaults to `jpeg`; config ships `png`, which argparse uses. [KD-6]
- **open** — `service/api_client.py` defaults to `-v v4.3`, outside the warmed set `v1.4`–`v5.4`; first call downloads weights. [KD-7, G-7]
- **open** — `Dockerfile` batch `CMD` is `-d /data/input --hf -rev v4.3`, one generation behind the canonical ensemble. _(new in round 6)_
- **open** — `.github/workflows/scheduled-smoke.yml` checks that revision **`v4.3`** exists, not the `v*.4` ensemble. _(new in round 6)_
- **open** — HF `ufal/vit-historical-page` `main` still resolves to `v4.3`; `[HF] latest = v4.4`. README tells users to pass `-rev v4.4` "until `main` is moved". [I-5, G-3]
- **open** — `setup/setup_api_service.sh` downloads `v1.3`–`v5.3` and suggests `uvicorn service.api:app --reload`; the image runs `python -m service.api` (different `RELOAD` handling). [KD-8]
- **open** — `service/README.md` `/predict_image` examples show `model_version` and `requested_topn`; `ImageResponse` declares neither and FastAPI filters them. [KD-9]
- **open** — `GET /` documented as serving the static UI; it returns a JSON welcome; UI is at `/frontend`. [KD-10]
- **open** — `category_uri()` is defined in `model_registry.py` but never called; `validate_labels()` is never called on emitted labels, so an unknown label reaches a record unreported. [skos page]
- **open** — `/health?deep=true` returns 200 unless draining (it checks a static, never-empty version list); warm-up marks the service ready even if a model failed to load. [operations page]
- **open** — service records carry no paradata logger, so their record licence falls back to **CC BY-NC 4.0** with a `license_note`, while CLI records for the same inference resolve to MIT. [document-contract]
- **open** — CLI `paradata_ref` says `paradata/<run>_page-classification.json` but the file is written under `result/paradata/`; the service leaves it empty. [document-contract]
- **open** — `category_confidence` rounded to 3 decimals by the CLI, unrounded by the service. [schemas]

### Stale paths, links and files

- **resolved** — README `supplement_scripts/…` links now `supplementary/scripts/…`. [KD-11]
- **open** — README paradata-example link points at a file not in the repository. [KD-12]
- **open** — README tree lists `result/stats/model_accuracies.csv` and two plots; real files are `model_accuracies_top1.csv` / `_top3.csv`, the plots do not exist. [KD-13]
- **open** — `.gitignore` has `/chekcpoint/` (typo); `checkpoint/` is **not** ignored. [KD-14]
- **open** — README `transformers` 5.x caution ends mid-sentence. [KD-15]
- **open** — `README.html` (≈180 KB, git-tracked, lags `README.md`) at repo root, no stated generator. [KD-20]
- **open** — `service/README.md` footnote cites handle `1-5959`; README cites `1-6184`. [external-tools]
- **open** — `agent_dev_logs/issues/` does not exist in this repository (round-3 history page linked it; link removed in round 6). _(new in round 6)_

### Things that exist but are unwritten in the README

- **open** — YOLO support (`yolo_classifier.py`, `--yolo`, `--yolo_base`, `[YOLO]`) has no README/CONTRIBUTING mention. [KD-16]
- **open** — `--chunk/--no-chunk`, `[INPUT] chunk_size` undocumented (switch to append-mode writing). [KD-17]
- **open** — the five `--document-json*` flags and `[DOCUMENT]` undocumented on the CLI side. [KD-18, R-2, R-3]
- **open** — RO-Crate export (`atrium_rocrate.py`) and the SKOS registry have no README mention. [KD-19, R-7]
- **open** — no troubleshooting section/FAQ; `.env.example` is the only place the `ALLOWED_ORIGINS` / `HOST` pitfalls are written down. [G-6, G-8]
- **open** — `run.py` with none of `-f`, `-d`, `--dir` silently does nothing. [G-9]

### Branches and CONTRIBUTING

- **open** — `CONTRIBUTING.md` documents only `test` and `master` and says `master` is stable / "always branch from `test`"; default branch is `vit`, `master` is a short index unmoved since 2026-06-26. [G-2, H-7, contributing page]
- **open** — CONTRIBUTING's "recommended default (`main` branch)" means the HF revision `main`; the repo has no `main` branch. [contributing page]

### Data-preparation scripts (round-5 findings)

- **resolved @ `a95c6c6`** — `pdf2png.sh` deleted each converted PDF and skipped `.PDF`; now keeps PDFs unless `--delete`, finds `.PDF`.
- **resolved @ `a95c6c6`** — `sort.sh` read the CSV by position (wider CSV → nested folders), made empty label folders, rejected zero-padded page numbers as octal; now reads by header name, creates folders on first hit, accepts `08`.
- **resolved @ `a95c6c6`** — `filtering.py` refused the annotation format (`CLASS`) and dropped rather than relabelled moved pages; now reads `CLASS`/`CLASS-1`/`CATEGORY` and relabels (`--no-relabel` to drop).
- **resolved** — `collect_images()` used `sorted(os.listdir())`, so a stray file (`small_data_samples/LICENSE`) shifted class indices / raised `NotADirectoryError`; now directories only (skos V-2 / F2).
- **open** — `sort.bat` still reads the CSV by position; `.bat` scripts are not exercised by CI. [pipelines W12]
- **unverified** — README's `downscale.py` / `result_analysis.sh` invocations failed on their flags (round 5); README now shows `--src/--dst` and `--pattern` forms.

### Ensemble history (settled, kept for the record)

- **resolved 2026-09-16** — all five `v*.4` HF revisions served the same `regnety_160` checkpoint (322,925,148 B each) 09-13 → 09-16; re-uploaded and verified distinct before v1.8.0-beta; `tests/test_best_ensemble_distinct.py` guards it. [R-5]
- **open** — `MODEL_STATIC` `params_bytes` for `v*.4` copy the `v*.3` values of the same base model. [R-4]

### Agent Skill branch (`agent-skill`)

- **unverified** — `scripts/server.sh` default runs `docker compose -f docker-compose.yml up -d` with no `--profile api` → only the batch service starts, `/info` never answers, 15-min timeout; `--gpu` passes the overlay alone; `--local` runs `setup/setup_api_service.sh` with paths relative to `setup/`; branch `Dockerfile` copies a missing `setup/requirements-test.txt`. Hub `server.template.sh` does all four right. [agent-skills]
- **unverified** — `SKILL.md` examples use `--version v4.3` / fallback `v2.3`; claims `service/requirements.txt` holds PyTorch/transformers/timm (it holds the web stack). [agent-skills]

## translator

### Licence

- **open (verified @ `71feaef`)** — `process_single_file()` attaches the paradata licence block to the record (`main.py` ≈ :513) **before** backend components are logged (≈ :665-685, after the first success). First record of a run: no components with explicit `--source_lang` (the E2E case), FastText-only CC BY-NC 4.0 with `auto`; later records CC BY-NC-SA 4.0. The HTTP service logs components after the call → every service record under-states. The site's worked example avoids showing the resulting licence. [IDX, document-contract]
- **open** — README licensing section tells commercial users to "select a self-hosted CTranslate2 model"; true only for EuroLLM/MADLAD-400 (Apache-2.0) and Opus-MT (CC BY 4.0), not NLLB-200 (CC BY-NC 4.0). [IDX-4]
- **open** — README paradata example: `tool_version` `v0.5.0` / image `:v0.5.0` (current v1.1.0-beta); resolves `lindat_cubbitt` to CC BY-NC 4.0 (config: CC BY-NC-SA 4.0); two fields are markdown links inside JSON strings. [KD-1…3]

### Behaviour and defaults vs prose

- **open** — README says `--source_lang` defaults to `cs`; argparse `default=None` → `config.txt` `auto`; the flag's help text still says "or 'cs'". [GD-3, KD-4, REF-1]
- **open** — language-ID fallback documented two ways: below-0.2 confidence → Czech (metadata mode); model load failure → `("en", 0.0)` for everything. [KD-5]
- **open** — `--xsd` is never applied to ALTO (`process_alto_xml` takes no schema); stated only in a digest. [KD-7]
- **open** — `entities[].translation_en` declared as translator-owned; unimplemented (19-line comment in `utils.py` cites a digest not in the repo). [IDX-1, KD-8]
- **open** — `atrium_rocrate.py` never called by `main.py` or the service; README feature list implies runs produce crates. [KD-9]
- **open** — 43 `print()` calls in runtime code; `LOG_LEVEL` does not govern the batch CLI. [GD-11, KD-10]
- **open** — `.env` is not loaded by `main.py` (no `load_dotenv`); reaches only the container via Compose `env_file`. [GD-7]
- **open** — service paradata is written to a per-request temp dir deleted with the response; `paradata_ref` may be absolute in the CLI. [document-contract]
- **open** — in a batch run all files share one baseline and output path, so the last file's name wins in `derived_from.translated_xml`. [document-contract]
- **open** — a bad baseline record gives a generic 500 (page-classification gives 422); a baseline that loads but fails validation is only logged, nothing in the response. [document-contract]
- **open** — `translations.source_lang` records the literal `"auto"` rather than the detected language. [schemas]

### Evaluation (#4)

- **open** — `eval/bakeoff.py` has never been run; no `bakeoff.csv` committed; `unbabel-comet` commented out in `eval/requirements-eval.txt`; `4.plan.md` lists "execute bakeoff" as pending. No BLEU/chrF/COMET figure exists for the tool. [IDX-5, HI-1, HI-6]
- **open** — the `live-backend` integration job ("run before a release") has never been dispatched. [KD-11]
- **open** — v0.7.0's "finalized in theory (not tested in practice)" still describes the backend choice. [CL-2]

### Fixed since round 3 (kept for the record)

- **resolved** — `CT2_COMPUTE_TYPE` default was `int4` (CTranslate2 4.8.2: `ValueError: Invalid compute type: int4`); now `int8`, validated against the device. [IDX-3, KD-6]
- **resolved @ `71feaef`** — `ct2` not selectable without editing `processors/backend.py`; now registered, deps lazy. [KD-12]
- **resolved v1.1.0-beta** — `/translate` answered 200 with metadata untranslated (empty XPath list); now 422. [GD-9]
- **resolved v1.1.0-beta** — batch fallback (up to ≈20× more calls) hidden behind `except Exception: pass`; now counted and logged. [GD-10, REF-11]
- **resolved v1.0.0-beta** — retry clamp `max(10, max_retries)` (11 attempts, back-off to 2,046 s); CLI always exited 0; FastText load failure swallowed; release zip missing modules; 413 after full read. [REF-3, REF-9]
- **resolved** — languages without a UDPipe model used to fall back to the Czech model. [REF-12]

### Measurements removed from the site (one-off, dated)

- 79-page ALTO sample: ≈158 API calls with page batching vs up to ≈3,288 unbatched; 7,229 boxes in/out, none resized; 528 (7.3 %) no longer exactly one word — 224 empty, 304 multi-word. [IDX-6, REF-10, HI-2]
- AMCR samples: 426 `xml:lang="cs"` attributes on 23 element types, 0 on translated free-text fields. [IDX-6]
- 16-document sample: 46.45 s (≈20.7 files/min), 113 protected terms; committed runs 2026-06-08: 113 terms across 16 AMCR records, 869 in one ALTO document. [IDX-6, pipelines W7]
- `vocabulary.csv` regenerated 2026-09-23: 5,087 rows (1,266 AMCR / 3,821 TEATER); AMCR has 1,461 `heslo` records, 1,460 with both labels. [GD-5, pipelines W7]
- sample-count disagreement across old pages: "15 AMCR records" vs "16 GetRecord URLs" — `amcr-inputs.txt` holds **15** @ `71feaef`.

### Repository hygiene

- **open** — v1.1.0-beta and v1.0.0-beta published with GitHub's `prerelease` flag **false** (all others pre-release). [CL-1, KD-13]
- **open** — CONTRIBUTING footnote points at `ARUP-CAS/atrium-translator`. [KD-14]
- **open** — contact address differs between README and CONTRIBUTING. [KD-15]
- **open** — README links `data_samples/my_documents/MTX201501307.alto.xml`; committed file is `MTX201501307_anon.alto.xml`. [KD-16]
- **open** — `main` branch holds only the 2026-02-18 initial commit (`e6c35ee`) and is not the default. [KD-17, GD-1]
- **open** — CONTRIBUTING release-history table headers swapped (Version · Release Type · Key Features vs rows version · features · type). [contributing page]
- **open** — CONTRIBUTING "minimum checks" comment says pre-commit runs black/isort/flake8; hooks are whitespace/YAML/ruff. [contributing page]
- **open** — CONTRIBUTING says alternative/local backends are "not in scope… a planned direction"; README and code ship LLM and CT2 backends. [contributing page]
- **open** — `teater.aiscr.cz` serves its certificate without the RapidSSL intermediate; `requests` fails TLS unless `REQUESTS_CA_BUNDLE` includes it (external; README documents the workaround). [pipelines W7]
- **unverified** — TEATER fallback `search(value:"", limit:99999)` answered `{}` by the live API (round 5). [pipelines W7]

### Agent Skill branch

- **unverified** — `SKILL.md` says deep health checks LINDAT reachability (it checks backend warm-up and FastText); says `/info` lists languages (it lists none); says server runs are traceable via paradata (temp dir deleted). [agent-skills]

## hub (`atrium-project`)

### Documents that disagree with the code

- **open** — K8s template `docs/templates/k8s/atrium-service.deployment.yaml`, `docs/k8s_deployment.md` and both tools' `service/README.md` write the image as `ghcr.io/ufal/atrium-<tool>:<version>-api`; the registry publishes `atrium-<tool>-api:<version>`. Both tools' compose files tag local builds `atrium-<tool>:${ATRIUM_VERSION}-api`. [operations]
- **open** — K8s template does not mount `/app/model` (page-classification re-downloads weights per pod); its commented `MAX_UPLOAD_MB` example `10` would cut the translator's 50 five-fold. [operations]
- **open** — `docs/k8s_deployment.md` per-service table omits `OUTPUT_MODE` and `AMCR_FIELDS_PATH`. [operations]
- **open** — `docs/document_schema.md` lists 7 fields carrying `x-atrium-scheme`; the schema has 2 (`page_categories`, `lines[].categ`). [schemas]
- **open** — schema `translations.description` says it holds "the persistent translated artifact"; the artifact is `derived_from.translated_xml`. `translations.backend` examples are `lindat`, `ctranslate2`, `llm`; the registry names are `lindat`, `openai_compatible`, `ct2`. `output_mode` not declared. [schemas]
- **open** — schema `$id` `https://github.com/ufal/atrium-project/docs/templates/…` lacks `/blob/<ref>/` and does not resolve. [schemas, external-tools]
- **open** — `pages[].category` has no `x-atrium-scheme` annotation. [schemas]
- **open** — `atrium_vocab.schema.json`: `skos:Collection` "at least one member" not enforced; nothing validates registry output against it. [schemas]
- **open** — `docs/document_schema.md` lists only `digital-born…` and `docx` as digital-convert prefixes; code also accepts bare `pdf`. [repository-map]
- **open** — `docs/paradata_schema.md` describes nested `provenance/license/timing/config/statistics` blocks; the file is flat. [document-contract]
- **open** — `docs/rocrate_export.md` says `datePublished` comes from block stamps only; code reads stamps and contributor times. [rocrate]
- **open** — `docs/skos_strategy.md` marks F2 and F3 done; F3's `load_vocab.py --from-flat DIR` does not exist; V-3 says the harvester has no `main()` (it has one); V-5/F6 (source-vocabulary URI as term `@id`) recorded done but absent from the exporter. [skos, rocrate]
- **open** — `docs/skills_catalog.md` says `server.sh` polls `/ready` (both poll `/info`); `docs/agent_skill_strategy.md` §4.4/§6: "retry 3× with backoff; short connect, long read" vs 3 attempts at constant 10 s with one timeout; usage errors "exit 1" vs argparse 2; wrong media type "→ 415" vs page-classification's 400. [agent-skills]
- **open** — `atrium_vocab.py` docstring says "~90" concepts; registry has 64. [skos]
- **open** — `w3id.org/atrium` redirect not registered (skos_strategy F5, optional). [skos, external-tools]
- **open** — `atrium_rocrate.py` profile pin carries a "verify before first published crate" comment; no crate published yet. [external-tools]
- **open** — `atrium_rocrate.py` hard-codes the four authors' ORCIDs because the hub has no `CITATION.cff`. [rocrate, external-tools]
- **open** — the RO-Crate export drops per-page category assignment, confidences and the `translations` contents; `derived_from` values arrive as bare filenames / run-wide files; no commit SHA, no output checksums, no DOI/PID (`rocrate_export.md` §7). [rocrate]

### Fixture, templates, config

- **open** — `fixtures/atrium_document.example.json` uses `page_categories` `"Text"`/`"Plate"` (not labels), `derived_from.translated_xml` = `TRANSLATED/…`, no `output_mode`. [I-4, document-contract]
- **open** — `fixtures/e2e/README.md` truncated mid-sentence after "`langID_classify.py` hard-requires CUDA". [pipelines W6]
- **open** — E2E runs published images by `image-tag` but checks tool source out with no `ref:` (default branch); only the hub is pinned `@v1`. Coupling `ref:` to `image-tag` proposed separately. [pipelines W6]
- **open** — `compose/docker-compose.pipeline.yml` does not exist; no cross-service orchestration; the E2E workflow is the only end-to-end recipe. [G-5, GD-4]
- **open** — skeleton `docs/templates/CONTRIBUTING.md`: pipeline order puts nlp-enrich before the translator and omits llm-enrich; says 2 shared files (17); names `paradata-drift.reusable.yml` and `docs/paradata-schema.md` (real: `para-drift.reusable.yml`, `docs/paradata_schema.md`). Neither tool's CONTRIBUTING has the skeleton's "Shared (drop-in) code" section. [contributing page]
- **open** — both tools' CONTRIBUTING call `tests/test_paradata.py` "shared across all repos"; it is not in `MANIFEST.json` and the copies differ. [architecture, contributing]
- **open** — hub `.pre-commit-config.yaml` pins older hook versions than the tools; `ruff.toml` template lists 7 first-party modules, page-classification 5, translator none. [architecture]
- **open** — `gpu-inference.yml` (page-classification) needs a self-hosted GPU runner that does not exist. [architecture]
- **open** — `mkdocs.yml` comments are stale: "ROUND 3", the retired assembler, `edit_uri` "pages are derived", `.gitignore` has no build rule (it has `/site/`), "ZERO mermaid blocks"; `docs_site/assets/extra.css` header still says "DRAFT, round 2". _(new in round 6; not rendered)_
- **open** — the hub has no `LICENSE` and no `CITATION.cff`; `mkdocs.yml` `copyright:` and every landing card say "MIT licensed". [carried]
- **open** — `docs/templates/workflows/para-drift.caller.example.yml` says "currently 16 files"; the manifest holds 17. [carried]

### Shared modules

- **resolved @ hub `4dd8e20`** — `para_licenses.py`: SPDX spellings (`CC0-1.0`, `cc-by-sa-4.0`) and case variants fell through as unrecognised; unrecognised licences resolved with both restriction flags false.
- **open** — `atrium_paradata.py` falls back to CC BY-NC 4.0 for a run that logs no component, while `para_licenses` alone returns MIT for an empty list. [external-tools]
- **open** — PyMuPDF (AGPL-3.0 / commercial) and Ghostscript (AGPL-3.0) are not components in page-classification's `para_config.txt`. [external-tools]

## cross-repo

- **open** — `alto-postprocess` never reads `page_categories` (only vendored schema occurrences); ecosystem diagrams draw pc → alto as a file handoff. No repository reads `TRANSLATED/`. The site now states both as design facts. [I-1, IDX-2]
- **open** — page-classification's Dockerfile floats `python:3.11-slim`; only the translator and hub template pin by digest and declare a `docker` Dependabot ecosystem. [external-tools]
- **open** — coverage floors: 38 % (page-classification) vs 81 % (translator). [external-tools]
- **open** — `skill-validate` check 5 downgrades to a warning when a service cannot import in CI (likely page-classification, PyTorch); `/ready`, static mounts and `server.sh` are not exercised. [agent-skills]
- **open** — SSH Open Marketplace workflow records parked on a marketplace-side error. [development-history]
- **open** — hub DEVLOG has no entries for 09-11 → 09-14 (dates recovered from the #53 and #59 digests); page-classification's own DEVLOG header drifted. [development-history]

## Site defects fixed in round 6 (not candidate issues)

- The `atrium-pipeline` strips on both tool index pages linked `../<tool>/index.md` inside `markdown="0"` raw HTML; MkDocs does not rewrite those, so the built links 404'd. Now `../<tool>/`.
- `external-tools.md` linked `[W6](pipelines.md)` with no anchor, and linked a Known-drift item (handle `1-5959`) that the list did not contain.
- The translator Reference's `--backend` row omitted `ct2`; the translator Overview listed `entities[].translation_en` as owned-and-written.
- Round-3 → 5 pages asserted `checkpoint/` is git-ignored (it is not) and that the upload limit is `MAX_UPLOAD_BYTES` (it is `MAX_UPLOAD_MB`, with the bytes form as a fallback).
