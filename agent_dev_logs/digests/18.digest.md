# 📓 Digest — [\#18](https://github.com/ufal/atrium-project/issues/18) `open`: Docker composer + GH action coded as wrapper for CU repo forks

> _Condensed from [`../issues/2026-05-27.18.issue.open.md`](../issues/2026-05-27.18.issue.open.md) — 20 comments, ~9k words. The full export is the source of truth; this digest keeps the strategy, the landed commits and the open bug list only._
> _Thread: 2026-05-27 → 2026-07-27 · K4TEL, motyc_

**Labels**: `enhancement`, `help wanted`, `development`, `a big one`
**Milestone**: Q3 [WP8 / WP7] · **Assignees**: motyc, K4TEL
**Status**: ✍️ `In progress` — **every tag-gated guard has fired on a real tag**, all **42** references resolve `@v1`, and as of 2026-07-31 **the hub's policy linter and its 52 tests run in CI**, in the hub and in all five tool repos. One item remains at the code level: the five `action-gh-release` SHA pins are committed on `test` but not on any default branch, so those branches still resolve the mutable `@v3`. The linter now flags that itself, in every repo, without being told.

## 🎯 Goal
Provide a Docker-compose + GitHub-Action wrapper for the four ARUP-CAS CU repo forks. Opened by motyc.

## 🧠 Strategy (settled, unchanged)
Separate the **common ground** (reusable workflows, `on: workflow_call` — single source of truth in `atrium-project`) from the **per-repo differences** (thin caller workflows). Reuse rather than reinvent: `atrium_paradata.py` already reads `ATRIUM_RUNNER_IMAGE`/`REPO`/`REF`, so **GHCR-published, self-identifying containers** are the mechanism. motyc: orchestration to be discussed with rharasim; **no overall runtime wrapper needed** — containers are reachable via API. This scopes the issue to **packaging + CI**, not a runtime orchestrator.

## 📍 Where everything actually is (2026-07-31)

Verified against the remotes, not from memory — the branch/tag layout has moved several times and stale local refs have misled this work before.

| Repo                         | default              | `test` vs default | `agent-skill` |
|------------------------------|----------------------|-------------------|---------------|
| `atrium-project`             | `main` @ `1fc652f`   | **in sync**       | —             |
| `atrium-alto-postprocess`    | `master` @ `b55e85d` | **2 ahead**       | `8dbaf16`     |
| `atrium-llm-enrich`          | `main` @ `88bc060`   | **2 ahead**       | `815dcbb`     |
| `atrium-nlp-enrich`          | `master` @ `73f47fb` | **2 ahead**       | `9f48128`     |
| `atrium-translator`          | `master` @ `32a15cf` | **2 ahead**       | `6176730`     |
| `atrium-page-classification` | `vit` @ `e5f65ff`    | **2 ahead**       | `0e45201`     |

`v1` → `1fc652f`, which is also `main` and `test` — hub tag and default branch are the same tree again.

**The two commits on each tool repo's `test` are the `action-gh-release` SHA pin and the new workflow-lint caller.** They must travel together: the linter flags the mutable `@v3` pin, so a caller merged without the pin would go red on arrival.

**Reference counts:** 42 resolve `@v1` today — 30 live caller jobs on default branches, 5 on `agent-skill`, 7 doc templates. Zero on any other ref. The five pending lint callers take it to 47 once `test` merges.

## ✅ The two P0s are closed

| Was                                          | Now                                                                                                                                                                                                     |
|----------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **E2E stage 5 dark, reporting green**        | ✅ **CLOSED** — run [`30515547822`](https://github.com/ufal/atrium-project/actions/runs/30515547822), 05:03→05:07: `Enrich the fixture lines (OpenRouter)` executed. All five stages run nightly; ≈4 min |
| **Schedule-triggered fixes inert on `test`** | ✅ **CLOSED** — `test` reached default in all six; `schedule` fires only from the default branch                                                                                                         |
| **`docker.yml` `startup_failure` ×5**        | ✅ **CLOSED** — the Wave B `security-events` regression is fixed; green in all five                                                                                                                      |

## ✅ Landed and green

| Item                              | State                                                                                                                                  |
|-----------------------------------|----------------------------------------------------------------------------------------------------------------------------------------|
| Release bundle integrity          | ✅ page-classification shipped `run.py` without the 9 modules it reaches transitively; now bundled + a closure guard that has **fired** |
| Two no-op stub workflows          | ✅ Deleted — neither had a `uses:`, both passed on every push while asserting nothing                                                   |
| Trigger normalization             | ✅ `pull_request` matches `push` per repo; `llm-enrich` dead `master` → `main`; page-classification's `vit`/`clip` covered              |
| `timeout-minutes` / `concurrency` | ✅ 45 runner jobs bounded (was 0 of 53); 33 workflows grouped, tag pushes exempt                                                        |
| `secrets: inherit`                | ✅ Gone from every caller **and** every template. Only `docker-tool` takes a secret (`CODECOV_TOKEN`)                                   |
| Container-scan race               | ✅ Post-publish Trivy scan sits inside `build-and-push`, addressed by immutable digest                                                  |
| Lint policy                       | ✅ Blocking in all five. llm-enrich's lone B008 was FastAPI's `Body(...)` idiom — fixed via bugbear `extend-immutable-calls`            |
| version-guard duplication         | ✅ ~200 lines removed → one vendored `check_version.py`, held byte-identical (md5 `d36004d0`) in all six copies by `para-drift`         |
| Dependabot                        | ✅ Hub covered; all ecosystems grouped; page-classification watches the real manifest dirs                                              |
| `issue-log-refresh.yml`           | ✅ 3 defects fixed against the installed `gh2md`                                                                                        |

## ✅ Release path proven twice, on real tags

Read from step lists, not run conclusions — a green run can still skip everything, which is the stage-5 lesson.

**`atrium-translator` v0.10.1** (2026-07-30): `check_version.py --require-tag` blocked-or-passed correctly (run [`30561448118`](https://github.com/ufal/atrium-translator/actions/runs/30561448118)); `build-and-push` succeeded under the new `security-events` grant, Trivy digest scan 15s, SARIF upload 7s (run [`30561449166`](https://github.com/ufal/atrium-translator/actions/runs/30561449166)); GHCR index carries an `attestation-manifest`.

**`atrium-page-classification` v1.7.1-beta** (2026-07-31, run [`30602523745`](https://github.com/ufal/atrium-page-classification/actions/runs/30602523745)) — this closed T1 by firing the **last guard that had never run**, the one covering a defect that actually shipped:

| Guard                            | Evidence                                                                                                                           |
|----------------------------------|------------------------------------------------------------------------------------------------------------------------------------|
| `check_version.py --require-tag` | `Block release on version/tag mismatch` — success                                                                                  |
| **bundle closure guard**         | `Verify bundle is self-contained` — success, logging `OK — bundle is self-contained (15 modules, 10 first-party imports resolved)` |
| release published                | `service_page_classifier.zip` uploaded                                                                                             |

The logged counts match the local replay exactly. **Every tag-gated guard in the ecosystem has now executed on a real tag.**

## ✅ `@v1` migration complete — 42 caller jobs (2026-07-31)

The last gap closed: the **five `agent-skill` callers moved from `@test` to `@v1`**. They had been missed by the original 26-ref migration, which only walked default branches — and that is precisely how a `test`-branch change was able to break them (below). All 42 caller jobs across six repos now resolve `@v1`; zero `@test` pins remain anywhere.

Two channels, documented in `docs/docker_gha.md` §1: **`@v1`** is what every caller pins (a moving major tag only a maintainer advances); **`@test`** is for validating a hub change before `v1` moves.

## 🐞 `skill-validate.reusable.yml` was clobbered — found, restored, verified

Commit `39a9edd` hand-applied one round's `security.reusable.yml` edits **into the wrong file**. One slip, two symptoms, both live at `v1`:

1. **`skill-validate.reusable.yml` was overwritten with a copy of `security.reusable.yml`** — 348 lines destroyed, including the `validate` job and all six `workflow_call` inputs.
2. **`security.reusable.yml` therefore never received its SHA pin**, keeping the mutable `aquasecurity/trivy-action@v0.36.0` — in the file whose own comment documents that action being force-pushed to malware.

**Not cosmetic.** All five tool repos have an `agent-skill` branch whose live caller passes `client-script`; the replacement declared `image-ref`/`citation-path`/`para-config-path` instead. GitHub rejects an undeclared input at parse time, so those five callers were **already broken** — latently, because nobody had pushed to an `agent-skill` branch since.

✅ **Resolved.** Restored verbatim from `7fa727b`; verified at `v1` (348 lines, `name: ATRIUM Skill Branch Validation`, job `validate`, six inputs) and all five `agent-skill` callers now match its declared inputs.

## ✅ Supply-chain pinning and the Trivy release gate

**SHA pins.** Every third-party action resolved through a mutable tag. `security.reusable.yml` already documented that `aquasecurity/trivy-action` tags ≤ 0.34.2 were force-pushed to credential-stealing malware on 2026-03-19 (GHSA-69fq-xp46-6x23, **76 of 77 tags**) and recommended pinning to the release commit — advice that had sat unacted-on. Now applied to the actions holding write scope: `docker/build-push-action` ×3, `aquasecurity/trivy-action` ×2, `peter-evans/create-pull-request` ×1 (all live on `main`), plus `softprops/action-gh-release` ×5 — **those five are still only on `test`.**

**Trivy `exit-code` policy — settled.** Rather than flip the existing step, a **separate gate** was added to `docker-tool.reusable.yml`: the SARIF step is unchanged (runs every build, never blocks, so the full CRITICAL+HIGH picture always reaches code scanning), and a new step blocks — placed **after** the SARIF upload so a blocked release still records its findings. Three narrowings: `if: startsWith(github.ref, 'refs/tags/')` (stops a *release*, not day-to-day pushes), `severity: CRITICAL` (HIGH stays report-only), `ignore-unfixed: true` (an unpatchable CVE cannot wedge releases). The weekly scan stays report-only. **Unfired — it needs a tag push.**

## ✅ Hub self-coverage — LIVE (T7 closed, 2026-07-31)

The hub is the source of truth for 42 references and six vendored files, and had **no caller workflows at all** — every file a `workflow_call` reusable plus the E2E and issue-log jobs. Nothing in CI parsed, linted or policy-checked any of it. That is why two copy-paste clobbers and a missed pin were all caught only by a human reading files.

**Now running, verified from step lists rather than run conclusions:**

| What                                  | Evidence                                                                                                                                                                                                                                                        |
|---------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `Hub Self-Check` on `main`            | run [`30617892015`](https://github.com/ufal/atrium-project/actions/runs/30617892015) — job `workflow-lint` step `Check workflows and callers` ✅; job `Test canonical shared modules` steps `Run release-gate tests` ✅ and `Run canonical shared-module tests` ✅ |
| same on `test`                        | run [`30617882578`](https://github.com/ufal/atrium-project/actions/runs/30617882578) ✅                                                                                                                                                                          |
| `Workflow Policy Lint` in a tool repo | atrium-translator run [`30618019962`](https://github.com/ufal/atrium-translator/actions/runs/30618019962), resolving `workflow-lint.reusable.yml@v1` → `1fc652f`, step `Check workflows and callers` ✅                                                          |
| `codeql.yml` / `pre-commit.yml`       | green on `main`; call the hub's reusables by local `./` path so a broken reusable is caught *before* the tag moves                                                                                                                                              |

The linter's exit code **is** the assertion — it exits 1 on any finding — so a succeeded step is a real result, not a skipped one.

**The design: a reusable, not a vendored copy.** `workflow-lint.reusable.yml` checks out both the caller's tree and the hub, mirroring `para-drift.reusable.yml`. Six copies of the script would need a `para-drift` parity step to stop them drifting, and adding such a step for a file the repos do not have yet fails `para-drift` in all five on their next push.

**What that unlocked.** The caller/callee permission check could only resolve a callee from the repo being linted. In a tool repo the callee lives in `atrium-project`, so it resolved nothing and the check silently passed — running the script against `atrium-translator` reported *"0 caller/callee permission pairs"*, which reads as a clean bill of health but is really an unasked question. With `--hub-root` and a hub checkout, **each tool repo now gets 13 real permission assertions** plus its inputs checked. That is the check that catches the Wave B `startup_failure` class, and it had no coverage anywhere outside the hub. The same resolver also handles `./`-form calls, so the hub's own `codeql.yml`/`pre-commit.yml` callers are checked at last (hub pairs: 12 → 17).

**Tests now run too**, including `docs/templates/shared/test_para_licenses.py` — 36 passing tests that had been in this repo the whole time, invoked by nothing, predating #18 entirely. Same shape as the stage-5 problem; found only because something finally ran the directory.

The linter's six checks, each matching a defect this issue actually hit: every workflow and template parses · write-scoped actions SHA-pinned **with a version comment whose tag really resolves to that SHA** · caller/callee permission pairs satisfied · no structural `secrets: inherit` · **no two workflows in a directory sharing a `name:`** · **no template passing an input its callee does not declare**. The last two are the clobber detectors; both real incidents are caught when replayed. Every check was made to fail on its own defect before being trusted, and seven mutations of `check_version.py` are each detected by the tests.

**It immediately earned its keep.** Pointed at all five tool repos' default branches it flags the mutable `action-gh-release@v3` — the one open item below — without being told to look for it.

## ✅ codeql + pre-commit consolidated (T3) — verified in CI

431 lines across ten near-identical files → 300, as two hub reusables plus thin callers on `@v1`. The only real differences were an arbitrary weekly cron and a cache present in 2 of 5; crons are now staggered and the cache is universal. Dropped `pre-commit/action@v3.0.1`, the last patch-pinned third-party dependency. Green on page-classification `vit` (`90deffc`) and alto-postprocess `master` (`b55e85dc`).

Caught in passing: **page-classification's `codeql`/`pre-commit` never ran on `vit`, its own default branch** — they covered `[test, master]` while its other four workflows covered `[vit, test, clip, master]`. Fixed.

## 🐞 Open — carried forward

1. ~~**Nothing runs `workflow_lint.py` or the release-gate tests**~~ ✅ **CLOSED 2026-07-31** — `hub-self-check.yml` plus `workflow-lint.reusable.yml` and five thin callers. Verified green in CI; see above.
2. **The five `action-gh-release` SHA pins are only on `test`.** Default branches still resolve the mutable `@v3` for an action that runs with `contents: write`. **The linter flags this in all five repos**, so it is no longer something anyone has to remember. Two fast-forwards' worth of commits per repo — the pin and the lint caller must merge together, since a caller arriving without the pin goes red immediately.
3. **`test` → default in the five tool repos** — same commits as (2). `schedule` fires only from the default branch, so anything schedule-driven stays dormant until it merges.
4. **Trivy release gate has never fired** — it cannot without a tag push.
5. **Negative test** — tag once with a deliberately mismatched `CITATION.cff` and confirm `version-guard` blocks. Proven locally against every failure mode; never fired on a real tag.
6. **Branch protection** on the hub's `main`/`test` — a repo-settings action, not code.
7. **No GPU runner**, so every `slow`-marked test remains unexecuted and the E2E `alto → nlp` boundary still uses the committed fixture CSV. With rharasim.
8. ~~**gitleaks**~~ ✅ **RESOLVED** — decided against; template deleted. GitHub-native secret scanning with push protection covers it.

## 🧩 Things worth remembering

- **Green does not mean covered.** The organizing lesson. Stage 5 was dark for weeks while the run reported green; `test_para_licenses.py` has 36 passing tests that nothing invokes; a caller example was a copy of another file and still parsed, pinned and permissioned correctly.
- **Copy-paste clobbers are this repo's characteristic failure, and they pass every ordinary check.** Twice a file was overwritten with another's contents. Both results were *well-formed YAML*, correctly pinned, correctly permissioned. Parsing, pin, permission and secret checks all pass happily on a perfectly valid file that is simply **the wrong file**. The detectors are duplicate `name:` within a directory, and a template passing an input its callee does not declare.
- **Redundant checks hide broken ones.** `check_version.py` compares the tag against *both* `CITATION.cff` and `para_config.txt`; when those agree, either alone blocks a bad tag, so deleting one is unobservable. That mutation survived the first draft of the test suite. Isolating it needed fixtures where the two files deliberately **disagree**.
- **A pytest collection error is not a test failure and does not look like one.** A broken import reported one *error* and zero tests, which scans as "fine" far more easily than "16 failed". Watch the test count, not the pass/fail line.
- **`git fetch --tags` does not move a tag that already exists locally** — it needs `--force`. A stale local `v1` had this work reasoning about the wrong reusable for a whole session. Resolve against the remote when it matters.
- **Annotated tags need `^{}` to pin.** `git ls-remote refs/tags/v3` returns the tag *object*; the commit is `refs/tags/v3^{}`. Two of the four pinned actions are annotated.
- **The `@test` pin enforces across every branch.** `para-drift.reusable.yml` checks the hub out at a ref and its callers fire on default-branch pushes, so the newest templates are enforced against branches that may lack the matching vendored files. Adding a parity step for a file the tool repos do not yet have breaks `para-drift` in all five.
- **llm-enrich's `docker.yml` carries a long `build-targets` comment for a reason.** The line was briefly lost in an edit, silently falling back to the reusable's `["base"]` default — which would have published an unrunnable image (the `base` stage has no ENTRYPOINT).

## 🔗 Where this lands in the plan
Feeds `docs/plan_repo_review.md` §8 (cross-service orchestration) and §7 Phase 3 (drift-check landed; ruff now blocking everywhere). The GPU self-hosted runner remains the blocking external dependency for §7 Phase 4.

---
_Condensed digest of [ufal/atrium-project#18](https://github.com/ufal/atrium-project/issues/18), generated 2026.06.25, refreshed 2026-07-31 against the live remotes after the `agent-skill` `@v1` migration, the `skill-validate` restore, and verification that the hub's linter and tests are present but unwired. Nothing removed from the issue itself (per hub #29); this is a derived reading aid in `agent_dev_logs/digests/`._
