# 📓 Digest — [\#18](https://github.com/ufal/atrium-project/issues/18) `open`: Docker composer + GH action coded as wrapper for CU repo forks

> _Condensed from [`../issues/2026-05-27.18.issue.open.md`](../issues/2026-05-27.18.issue.open.md) — 20 comments, ~9k words. The full export is the source of truth; this digest keeps the strategy, the landed commits and the open bug list only._
> _Thread: 2026-05-27 → 2026-07-27 · K4TEL, motyc_

**Labels**: `enhancement`, `help wanted`, `development`, `a big one`
**Milestone**: Q3 [WP8 / WP7] · **Assignees**: motyc, K4TEL
**Status**: ✍️ `In progress` — Wave A landed on `test` and is green (33 runs, 0 failures). Waves **B, C and D are now implemented** too: the container-scan race is gone, `secrets: inherit` is eliminated ecosystem-wide, lint is blocking in all five repos, and ~360 lines of copy-pasted version-guard collapsed into one vendored `check_version.py` held by `para-drift`. What is left is **not code**: (1) `test` → default-branch merges, without which every schedule-triggered fix stays inert, and (2) the `@v1` reusable pin, which needs a tag plus 20 caller edits as one atomic operation. `OPENROUTER_KEY` has now been added to `atrium-project`, so stage 5 will run once the merge reaches `main`.

## 🎯 Goal
Provide a Docker-compose + GitHub-Action wrapper for the four ARUP-CAS CU repo forks. Opened by motyc.

## 🧠 Strategy (settled, unchanged)
Separate the **common ground** (reusable workflows, `on: workflow_call` — single source of truth in `atrium-project`) from the **per-repo differences** (thin caller workflows). Reuse rather than reinvent: `atrium_paradata.py` already reads `ATRIUM_RUNNER_IMAGE`/`REPO`/`REF`, so **GHCR-published, self-identifying containers** are the mechanism. motyc: orchestration to be discussed with rharasim; **no overall runtime wrapper needed** — containers are reachable via API. This scopes the issue to **packaging + CI**, not a runtime orchestrator.

## ✅ Closed — verified in CI on `test` (2026-07-30)

| Item                                 | Previous state      | Now                                                                                                                                                                                                                                                            |
|--------------------------------------|---------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `page-classification` release bundle | ❌ BROKEN            | ✅ **FIXED** — shipped `run.py` without the 9 first-party modules it reaches transitively; `service/api.py` and `service/inference.py` were affected too. Now bundled, plus a dependency-free closure guard that fails the release if an import stops resolving |
| Two no-op stub workflows             | ❌ passing-but-empty | ✅ **DELETED** — `llm-enrich-execution.caller.yml`, `hf-model-sync.caller.yml`; neither had a `uses:`, both carried `[cite: 1, 2]` artifacts                                                                                                                    |
| PR triggers narrower than push       | ❌ gap in all 5      | ✅ **ALIGNED** — `pull_request` now matches `push` per repo; `llm-enrich` dead `master` → `main`; page-classification's active `vit`/`clip` now covered                                                                                                         |
| `timeout-minutes`                    | 0 of 53 workflows   | ✅ **45 runner jobs** now bounded (was the 6h default)                                                                                                                                                                                                          |
| `concurrency`                        | 2 of 53             | ✅ **33 workflows**, tag pushes exempt. **Proven live**: translator's 03:49 runs were cancelled by the 03:50 push                                                                                                                                               |
| `atrium-project` Dependabot          | ❌ absent            | ✅ **ADDED** — the hub hosts the shared pins and was the only repo without it, which is why the reusables drifted (`checkout@v7` vs `@v5`)                                                                                                                      |
| `issue-log-refresh.yml`              | 🔍 never run        | ✅ **3 defects fixed** — verified against installed `gh2md`: needs `GITHUB_ACCESS_TOKEN` not `GH_TOKEN`, takes `output_path` positionally (the `>` redirect could never work), and needed `contents`/`pull-requests: write`                                     |
| `para-drift` license parity          | ✅ DONE              | ✅ **VERIFIED** — shared modules byte-identical across all five repos                                                                                                                                                                                           |
| translator caller P0/P1 fixes        | 🔍 VERIFY           | ✅ **DONE** — triggers `[test, master]`, `image-ref`, `pre-install-torch` gone                                                                                                                                                                                  |

## ✅ Closed — Waves B/C/D (2026-07-30, implemented but not yet CI-verified)

| Item                        | Now                                                                                                                                                                                |
|-----------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Trivy/SBOM publish race     | ✅ Post-publish scan moved into `docker-tool.reusable.yml`, addressed by the digest just pushed, per-target SARIF category. `security.reusable.yml` keeps only the periodic re-scan |
| `anchore/sbom-action@v0`    | ✅ Retired — SBOM + provenance are buildkit attestations on the image. Last floating major-zero pin gone                                                                            |
| `secrets: inherit`          | ✅ Gone from all 10 callers. `docker-tool` declares `CODECOV_TOKEN`; the other four reusables consume no secrets at all                                                             |
| Expression-into-`run:`      | ✅ `apt-packages` / `pre-install-torch` / `requirements` now arrive via `env:` and are quoted                                                                                       |
| Hardcoded CPU torch index   | ✅ New `torch-index-url` input; alto's bare `torch` now pins `torch>=2.1,<3.0` to match its own manifest                                                                            |
| HF cache key                | ✅ Keys on `hf-cache-key: v4.3` (the model revision) instead of `hashFiles('**/*.py')`, which invalidated on any source edit                                                        |
| Lint policy split           | ✅ Blocking in all five. llm-enrich's lone B008 was FastAPI's `Body(...)` idiom — fixed via bugbear `extend-immutable-calls`, not by changing code                                  |
| ~360 lines of version-guard | ✅ One `check_version.py` in `docs/templates/shared/`, vendored to all five repos, held byte-identical by a new `para-drift` step. Both call sites share it                         |
| Missing caller examples     | ✅ Added for `security`, `api-contract`, `para-drift` — every active reusable now has one                                                                                           |
| Dependabot drift            | ✅ All ecosystems grouped. page-classification's pip entry watched `/` (only a forwarding stub lives there) — now split across `/setup` and `/service`                              |
| Lagging hub pins            | ✅ `checkout@v5` / `setup-python@v5` / `upload-artifact@v4` levelled; `checkout` is uniformly `@v7` ecosystem-wide                                                                  |

Verified locally: 68 YAML files parse, `check_version.py` byte-identical across all five repos and passing each repo's own ruff config, the version guard green against every repo's real files and tag, `ruff check .` clean in all five. **Not yet verified in CI** — these changes have not run on GitHub.

**Deliberately deferred:** `codeql.reusable.yml` / `pre-commit.reusable.yml`. They would collapse ~325 duplicated lines, but also move 10 more workflows behind a `@test`-pinned cross-repo ref — raising mutable-branch coupling from 20 refs to 30 *before* `@v1` exists. Sequence them after the pin migration.

**E2E stages 1–4 have real coverage** (run [`30512204306`](https://github.com/ufal/atrium-project/actions/runs/30512204306), step-by-step): pc classified the rendered fixture, alto ran split→stats→extract with the categ-header tripwire passing, translator did cs→en via LINDAT, nlp ran UDPipe→NameTag→TEITOK. Whole pipeline ≈3m20s.

## 🐞 Open — the two live P0s

Full sequencing in [`../plans/18.plan.md`](../plans/18.plan.md).

**P0-1 — stage 5 dark until the merge reaches `main`.** _(secret now added — 2026-07-30)_
In run `30512204306`, job `5. llm-enrich`: `Gate on OPENROUTER_KEY` succeeded and then **all 11 downstream steps skipped**, including `Enrich the fixture lines (OpenRouter)`. The renamed gate step proves the new code is live. `OPENROUTER_KEY` has since been added to `ufal/atrium-project`, so the gate will pass — but the **nightly** still runs the old file on `main`, so P0-2 below gates the actual fix.
Worth recording: a fully-skipped stage still reports job `success`, so the report job's `contains(needs.*.result, 'failure')` stays false. That is exactly why the schedule-gate was added rather than trusting the report job to notice.

**P0-2 — every schedule-triggered fix is inert.**
GitHub fires `schedule` **only from the default branch**. All this work landed on `test`.

| Repo                       | Default    | `test` ahead | Workflow files differing |
|----------------------------|------------|--------------|--------------------------|
| atrium-project             | `main`     | 6            | 8                        |
| atrium-alto-postprocess    | `master`   | 1            | 8                        |
| atrium-llm-enrich          | `main`     | 1            | 10                       |
| atrium-nlp-enrich          | `master`   | 1            | 10                       |
| atrium-page-classification | **`vit`**  | 1            | 10                       |
| atrium-translator          | `master`   | **0**        | **0** — fully live       |

Verified consequences: the nightly E2E still runs the old file on `main` (line 395 still reads `secrets.OPENROUTER_API_KEY`), so the hard-fail gate cannot fire and the nightly keeps reporting green; the GPU crons still fire on all three default branches; llm-enrich/nlp-enrich scheduled-smoke still has no failure reporting on default. **Translator is the only repo where the hardening is fully effective.**

Also flagged: page-classification's default branch is **`vit`**, not `master` or `test` — trigger lists and mental models across the ecosystem say "master", which is misleading.

## 🐞 Open — carried forward from the audit
1. **Mutable cross-repo pin + blanket secret inheritance.** All 20 caller refs point at `…@test`; `atrium-project` has only `main`/`test`, **neither protected**, and 4/5 callers pass `secrets: inherit`.
2. **No GPU runner exists**, so every `slow`-marked test in the ecosystem is still unexecuted. The crons were removed on `test` so the ecosystem stops advertising coverage it lacks; provisioning remains with rharasim.
3. **`@v1` reusable pin + branch protection** — still the largest structural risk. Needs a tag plus 20 caller edits in one atomic operation.

## 🧩 Still unadopted
`secret-scan.caller.example.yml` (gitleaks) exists in `docs/templates/workflows/` but **no repo calls it** — consistent with the 2026-06-21 note that "the secret-check workflow is under question for now". _(The three missing caller examples were added on 2026-07-30.)_

## 🔗 Where this lands in the plan
Feeds `docs/plan_repo_review.md` §8 (cross-service orchestration) and §7 Phase 3 (drift-check — landed — plus ratcheting ruff to blocking). The GPU self-hosted runner (rharasim) remains the blocking external dependency for §7 Phase 4 and for the E2E's `alto → nlp` bridge, which still uses a committed fixture CSV instead of a live classify stage.

---
_Condensed digest of [ufal/atrium-project#18](https://github.com/ufal/atrium-project/issues/18), generated 2026.06.25, updated 2026-07-30 after reviewing the pushed `test` branches, then again once Waves B/C/D were implemented. Nothing removed from the issue itself (per hub #29); this is a derived reading aid in `agent_dev_logs/digests/`._
