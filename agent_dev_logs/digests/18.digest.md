# 📓 Digest — [\#18](https://github.com/ufal/atrium-project/issues/18) `open`: Docker composer + GH action coded as wrapper for CU repo forks

> _Condensed from [`../issues/2026-05-27.18.issue.open.md`](../issues/2026-05-27.18.issue.open.md) — 16 comments, ~5.1k words. The full export is the source of truth; this digest keeps the strategy, the landed commits and the bug list only._
> _Thread: 2026-05-27 → 2026-07-12 · K4TEL, motyc_

**Labels**: `enhancement`, `help wanted`, `development`, `a big one`
**Status**: ✍️ `In progress` — wrapper + reusables landed and the cross-cutting template bugs are **fixed on HEAD** (Codecov gate, action-version floor, caller renames); `paradata-drift` renamed **`para-drift.reusable.yml`** with default license-parity steps; `docs/docker_gha.md` refreshed (2026-07-12). Remaining: translator pilot caller verification, rharasim end-to-end run, and the proposed pipeline-wide smoke test.

## 🎯 Goal
Provide a Docker-compose + GitHub-Action wrapper for the four ARUP-CAS CU repo forks. Opened by motyc.

## 🧠 Strategy (settled)
Separate the **common ground** (a reusable workflow, `on: workflow_call` — the single source of truth) from the **per-repo differences** (thin caller workflows). Pilot/reference template: **atrium-translator**. Reuse what already exists rather than reinvent: `atrium_paradata.py` already reads `ATRIUM_RUNNER_IMAGE` / `ATRIUM_RUNNER_REPO` / `ATRIUM_RUNNER_REF`, so **GHCR-published, self-identifying containers** are the mechanism. motyc: orchestration to be discussed with rharasim; **no overall wrapper needed** — containers are reachable via API (this scopes the issue down to packaging + CI, not a runtime orchestrator).

## ✅ Landed
Commit `676a1fe` — centralized DRY CI/CD: `ci-cd-strategy.md`, `docker-tool.reusable.yml`, a caller example, shared `.coveragerc` / `ruff.toml`, and a Dependabot appendix. All four repos pass GH Actions; docs updated for rharasim to test.

## 🐞 Cross-cutting bugs (shared template) — all three **fixed on HEAD**
1. ~~**Codecov upload never fires.**~~ ✅ **FIXED** — `CODECOV_TOKEN` is now written to job `env:`
   in `docker-tool.reusable.yml:37-40` (the in-file comment cites #18), so the
   `if: env.CODECOV_TOKEN != ''` gate works.
2. ~~**`@main` vs `@test` pin drift**~~ ✅ **SUPERSEDED** — caller templates were renamed with the
   `.caller.example.yml` suffix (`4340a21`, 2026-06-28); `security.caller.example.yml` is gone,
   replaced by `secret-scan.caller.example.yml` (gitleaks-action@v3 directly, no `@main` pin).
3. ~~**Version floor**~~ ✅ **DONE** (2026-07-12 "fix GHA - bump version") — reusables now at
   checkout@v7 / setup-python@v6 / codeql-action@v4 / trivy-action@v0.36.0 / gitleaks-action@v3.

## 📦 Landed since (2026-06-26 → 2026-07-12)
- **2026-06-26**: opening task list checked off — ✅ docker builds on new tags/releases, ✅ test-set
  coverage for new commits + report generation, ✅ pip dependency updates. **Proposal**: an
  end-to-end integration smoke test (single-page ALTO → postprocess → translate → enrich →
  TEITOK) run in CI — no issue tracks pipeline-wide regression yet.
- **2026-07-12**: versions bumped in the yml files; `paradata-drift` renamed **`para-drift.reusable.yml`**,
  which now includes **license-parity steps by default** (`para_licenses.py` /
  `tests/test_para_licenses.py` diffed against `docs/templates/shared/`); GHA docs updated in
  `docs/docker_gha.md`.

## 🌐 atrium-translator (verified against real repo state)
| Pri | File | Issue / fix |
|---|---|---|
| **P0** | codeql / docker / pre-commit / secret-scan / version-check.yml | Triggers use `branches: [main]`; real branches are `test` / `master` → workflows never run on real activity. Fix: `branches: [test, master]`. |
| **P0** | security.yml | Passes `image-name`; `security.reusable.yml` only declares `image-ref` / `citation-path` / `para-config-path` → undeclared input, call fails validation. Fix: `image-ref: ghcr.io/ufal/atrium-translator:latest`. |
| **P1** | docker.yml | `pre-install-torch: true` (bool) into a `type: string` input → coerces to `"true"`, runs `pip install true` and fails. |

## 🔗 Where this lands in the plan
Feeds `docs/plan_repo_review.md` §8 (cross-service orchestration: `compose/docker-compose.pipeline.yml` chaining pc → alto → nlp → translator over the shared `atrium_data` volume) and §7 Phase 3 (drift-check — now `para-drift.reusable.yml` — + ratchet ruff to blocking). All four repos subsequently released vX.Y.Z+1 passing ruff/pre-commit.

---
_Condensed digest of [ufal/atrium-project#18](https://github.com/ufal/atrium-project/issues/18), generated 2026.06.25, updated 2026-07-12 from the issue export. Nothing removed from the issue itself (per hub #29); this is a derived reading aid in `agent_dev_logs/digests/`._
