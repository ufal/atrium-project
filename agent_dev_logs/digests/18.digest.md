# 📓 Digest — [\#18](https://github.com/ufal/atrium-project/issues/18) `open`: Docker composer + GH action coded as wrapper for CU repo forks

> _Condensed from [`../issues/2026-05-27.18.issue.open.md`](../issues/2026-05-27.18.issue.open.md) — 20 comments, ~9k words. The full export is the source of truth; this digest keeps the strategy, the landed commits and the open bug list only._
> _Thread: 2026-05-27 → 2026-07-27 · K4TEL, motyc_

**Labels**: `enhancement`, `help wanted`, `development`, `a big one`
**Milestone**: Q3 [WP8 / WP7] · **Assignees**: motyc, K4TEL
**Status**: ✍️ `In progress` — **all hardening work is merged to the default branch in all six repos** and CI is green across the board. **No P0s remain open.** The nightly E2E now exercises all five pipeline stages for real, including the `llm` stage that was dark for weeks. What is left: the **release path has never run** (every tag-gated guard is unexercised — see 🎯 Next), and the `@v1` reusable pin is still open.

## 🎯 Goal
Provide a Docker-compose + GitHub-Action wrapper for the four ARUP-CAS CU repo forks. Opened by motyc.

## 🧠 Strategy (settled, unchanged)
Separate the **common ground** (reusable workflows, `on: workflow_call` — single source of truth in `atrium-project`) from the **per-repo differences** (thin caller workflows). Reuse rather than reinvent: `atrium_paradata.py` already reads `ATRIUM_RUNNER_IMAGE`/`REPO`/`REF`, so **GHCR-published, self-identifying containers** are the mechanism. motyc: orchestration to be discussed with rharasim; **no overall runtime wrapper needed** — containers are reachable via API. This scopes the issue to **packaging + CI**, not a runtime orchestrator.

## ✅ The two P0s are closed

| Was                                        | Now                                                                                                                                                                                                             |
|--------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **E2E stage 5 dark, reporting green**       | ✅ **CLOSED** — run [`30515547822`](https://github.com/ufal/atrium-project/actions/runs/30515547822) on `main`, 05:03→05:07: `Gate on OPENROUTER_KEY` passed, `Require the secret on scheduled runs` correctly skipped, and **`Enrich the fixture lines (OpenRouter)` executed successfully**. All five stages now run nightly; pipeline ≈4 min |
| **Schedule-triggered fixes inert on `test`** | ✅ **CLOSED** — `test` merged to default in all six repos, so `schedule` (which only fires from the default branch) now reaches them. GPU crons removed in all three repos that had them; smoke failure-reporting present in all five; `check_version.py` vendored on every default branch |
| **`docker.yml` `startup_failure` ×5**        | ✅ **CLOSED** — the Wave B `security-events` regression is fixed; Docker Build & Publish green in all five (05:07–05:11)                                                                                            |

## ✅ Landed and green

| Item                                  | State                                                                                                                                                        |
|---------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Release bundle integrity               | ✅ page-classification shipped `run.py` without the 9 first-party modules it reaches transitively; now bundled + a dependency-free closure guard                  |
| Two no-op stub workflows               | ✅ Deleted — neither had a `uses:`, both carried `[cite: 1, 2]` artifacts and passed on every push                                                              |
| Trigger normalization                  | ✅ `pull_request` matches `push` per repo; `llm-enrich` dead `master` → `main`; page-classification's `vit`/`clip` covered                                       |
| `timeout-minutes` / `concurrency`      | ✅ 45 runner jobs bounded (was 0 of 53); 33 workflows grouped, tag pushes exempt. Cancellation observed live                                                     |
| `secrets: inherit`                     | ✅ Gone from all 10 callers. Only `docker-tool` takes a secret (`CODECOV_TOKEN`); the other four take none                                                       |
| Container-scan race                    | ✅ Post-publish Trivy scan sits inside `build-and-push`, addressed by immutable digest; SBOM/provenance are buildkit attestations                                 |
| Lint policy                            | ✅ Blocking in all five. llm-enrich's lone B008 was FastAPI's `Body(...)` idiom — fixed via bugbear `extend-immutable-calls`, not by changing code                |
| version-guard duplication              | ✅ ~200 lines net removed (5 × 36 from `release.yml` + 21 from the hub's drifted variant) → one vendored `check_version.py`, held byte-identical by a `para-drift` step |
| Action pin drift                       | ✅ Every action in the ecosystem on a single major (`setup-python` was the last split)                                                                           |
| Dependabot                             | ✅ Hub now has one at all; all ecosystems grouped; page-classification's pip watches `/setup` + `/service` instead of a directory holding only a forwarding stub  |
| `issue-log-refresh.yml`                | ✅ 3 defects fixed against the installed `gh2md`: reads `GITHUB_ACCESS_TOKEN` not `GH_TOKEN`, takes `output_path` positionally, needed `pull-requests: write`      |

## 🎯 Next: the release path has never run

Latest tags — `v1.4.0-beta`, `v0.4.0`, `v0.18.0`, `v1.7.0-beta`, `v0.10.0` — all date to **2026-07-26 or earlier**, before any of this work landed, and each `para_config.txt` already matches its tag. So five guards are **written, merged, and completely unexercised**, all of them tag-gated:

1. `check_version.py` invoked from `release.yml` with `--require-tag`
2. page-classification's release-bundle closure guard
3. the post-publish Trivy digest scan and its SARIF upload
4. the buildkit `sbom: true` / `provenance: true` attestations
5. `build-and-push` under the caller's new `security-events: write` grant

**One patch release exercises all five.** Pilot: **atrium-translator** — lightest repo, no GPU, no torch, single build target; the same pilot choice this issue settled on originally.

**The bump is prepared (2026-07-30):** `CITATION.cff` `0.10.1` + `date-released 2026-07-30`, `para_config.txt` `v0.10.1`, and a `CONTRIBUTING.md` release-history row. Verified locally against the vendored script exactly as `release.yml` invokes it — passes for `v0.10.1`, and correctly rejects `v0.10.0`, a missing tag, and a CITATION/para_config mismatch. **Awaiting the `v0.10.1` tag push**, which is what actually fires the five guards.

## 🐞 Open — carried forward
1. **`@v1` reusable pin + branch protection.** All 20 refs still point at `…@test`. This got *more* valuable, not less: with `test == default`, a bad hub push now lands directly on every repo's production branch instead of an integration branch. Needs a tag plus 20 caller edits as one atomic operation.
2. **No GPU runner**, so every `slow`-marked test remains unexecuted and the E2E `alto → nlp` boundary still uses the committed fixture CSV rather than a live classify stage. With rharasim.
3. **Trivy `exit-code` is `"0"`** in both scan paths — report-only. Triage after the first real scan (see 🎯 Next), then decide whether a release should fail on CRITICAL.
4. ~~**gitleaks**~~ ✅ **RESOLVED 2026-07-30** — decided against. The template sat unadopted by every repo from 06-21 onward and has been deleted; GitHub-native secret scanning with push protection covers the case without the ARUB/ARUP policy sign-off and org-owned `GITLEAKS_LICENSE` that `gitleaks-action` requires.

## 🧩 Two things worth remembering
- **`codeql.reusable.yml` / `pre-commit.reusable.yml` are deliberately deferred** behind the `@v1` pin. They would collapse ~325 duplicated lines but raise mutable-branch coupling from 20 refs to 30. The Wave B regression — one hub-side permission change that took down `docker.yml` in all five repos simultaneously — is the standing argument for that ordering.
- **The `@test` pin enforces across every branch.** `para-drift.reusable.yml` checks the hub out at `ref: test` and its callers fire on default-branch pushes, so the newest templates are enforced against branches that may not have the matching vendored files. This nearly bit when `check_version.py` parity landed while no default branch had the file; the merge delivered both together. Keep it in mind when adding a parity step.
- **llm-enrich's `docker.yml` carries a long `build-targets` comment for a reason.** The line was briefly lost in an edit, silently falling back to the reusable's `["base"]` default — which would have published an unrunnable image (the `base` stage has no ENTRYPOINT) and never published `-remote`/`-llm`. Restored.

## 🔗 Where this lands in the plan
Feeds `docs/plan_repo_review.md` §8 (cross-service orchestration) and §7 Phase 3 (drift-check landed; ruff now blocking everywhere). The GPU self-hosted runner remains the blocking external dependency for §7 Phase 4.

---
_Condensed digest of [ufal/atrium-project#18](https://github.com/ufal/atrium-project/issues/18), generated 2026.06.25, updated 2026-07-30 after the `test` → default-branch merges in all six repos and verification of the five-stage nightly E2E. Nothing removed from the issue itself (per hub #29); this is a derived reading aid in `agent_dev_logs/digests/`._
