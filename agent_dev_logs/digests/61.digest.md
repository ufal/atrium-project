# 📓 Digest — [\#61](https://github.com/ufal/atrium-project/issues/61) `open`: LOGS: as an event stream — no `basicConfig()` in library modules, no per-job log file

> _Condensed from [`../issues/2026-09-10.61.issue.open.md`](../issues/2026-09-10.61.issue.open.md) — **1 comment** (2026-09-12 07:10, a status update on the landed work). Confirmed live 2026-09-14._
> _Thread: 2026-09-10 · K4TEL. Measured passes: 2026-09-11, 2026-09-12. **Re-measured pass: 2026-09-14** (this refresh)._

**Labels**: `enhancement`, `development` · Type: `Task` · Milestone: WP4 — Pre-Closure Architecture Review
· **Parent**: [\#53](https://github.com/ufal/atrium-project/issues/53) (factor XI) · **Assignees**: none

**Status**: 🟢 **Done and closable. The record is what is holding this open.** All three acceptance
criteria pass, re-measured 2026-09-14 against hub `f675943` / alto `f6d9196` / nlp `bfabfca` /
llm `5ac82a5` / pc `f11936f` / translator `d0c1572`. The code landed 09-11→09-12, the static guard
and the container probe landed 09-12, the doc parity landed 09-13→09-14, and every one of them is
green in CI on 09-14. Nothing in this issue's scope is outstanding.

What was outstanding is this pair of files. The previous edition ran four bullets under **⏳ Open**
and seven unticked boxes under **S5–S8**; every one of them has since been satisfied, and its
`## 🔎 Verified state` section still described the *pre-fix* tree in the present tense, directly
above the section recording the fix. That is corrected below and the pre-fix measurements are moved
into a dated historical block. **Close the issue.**

## 🎯 Goal

Logs are an event stream on stdout, configured once at the entrypoint, at a level a partner can set
at deploy time. Nothing a partner needs in order to diagnose a failed request should be reachable
only from inside a dead container.

## 🧵 Thread trail

- **2026-09-10 12:13, K4TEL** — opened. Two counter-examples (`pc/service/inference.py:22`'s
  import-time `basicConfig`, `nlp/service/enrichment.py:234`'s `LOG_FILE`), three acceptance
  criteria, and the claim that `LOG_LEVEL` was read in **exactly one** place ecosystem-wide.
  Carved out of [\#53](https://github.com/ufal/atrium-project/issues/53), factor XI.
- **2026-09-11** — [\#58](https://github.com/ufal/atrium-project/issues/58) lands across the five
  tool repos and, while rewriting each `__main__`, creates the root-logger block in the four that
  lacked one. The issue's opening premise stops being true — in the good direction.
- **2026-09-12 07:10, K4TEL** — the thread's **one** comment: a status update naming what landed,
  the hub self-check fix that made the guard skip cleanly where there is no `service/` tree, and the
  three `.env.example` blocks still claiming `LOG_LEVEL` was withheld.
- **2026-09-12 07:59, `9683c76`** — pc's import-time `basicConfig` deleted and replaced by alto's
  rule comment. The defect the issue was filed against is gone.
- **2026-09-12 08:46–08:54** — `tests/test_logging_contract.py` created in the hub and all five tool
  repos inside eight minutes (alto `855d5ef` · pc `f5e6a38` · llm `1562799` · translator `f4b08d2` ·
  nlp `046d768` · hub `bf169f6`).
- **2026-09-13 → 2026-09-14** — doc parity lands with the #60 env-contract sweep; the three
  "withheld" blocks are deleted; the hub's `v1` tag moves to `f675943`.
- **2026-09-14 11:29–16:03** — the container probe runs green on all five repos.
- **2026-09-14** — this re-measurement. No comment posted at the time of writing.

## 🔎 Verified state — re-measured 2026-09-14

Every path:line below was re-checked with `grep -n` / `sed -n` against the working trees on
2026-09-14. The previous edition's line numbers had drifted; so have the **issue body's own**
(`text_inference.py:51-55` → `:27-33`, `text_api.py:537` → `:540`, `inference.py:22` → deleted,
`enrichment.py:234` → `:254`).

### The five configuration points — one per repo, all inside `__main__`

`grep -rn "basicConfig" --include=*.py` over all six repos returns exactly five calls. Every other
match is prose: three rule comments and the guard's own source text.

| Repo                | `basicConfig` call        | `LOG_LEVEL` read | Entrypoint (`api` stage)     |
|---------------------|---------------------------|------------------|------------------------------|
| alto-postprocess    | `service/text_api.py:540` | `:541`           | `python service/text_api.py` |
| llm-enrich          | `service/api.py:531`      | `:532`           | `python -m service.api`      |
| nlp-enrich          | `service/api.py:558`      | `:559`           | `python -m service.api`      |
| page-classification | `service/api.py:360`      | `:361`           | `python -m service.api`      |
| translator          | `service/api.py:510`      | `:511`           | `python -m service.api`      |

All five carry byte-identical bodies:

```python
logging.basicConfig(
    level=os.getenv("LOG_LEVEL", "INFO").upper(),
    format="%(asctime)s %(levelname)s %(name)s %(message)s",
    stream=sys.stdout,
)
```

**`LOG_LEVEL` is read in exactly five production places, one per tool repo, all inside `__main__`.**
The issue's opening claim — "read in exactly **one** place ecosystem-wide, so four of five services
cannot have their verbosity changed at deploy time at all" — is now false, in the good direction.

### The rule is written in the files that have to obey it — three copies now

- **page-classification** — `service/inference.py:21-26` carries alto's rule comment verbatim and
  `:27` is a plain module-level `logger = logging.getLogger(__name__)`. **This is the defect the
  issue named, and it no longer exists** (`9683c76`, 2026-09-12 07:59:07).
- **alto-postprocess** — the comment **moved**, from `service/text_inference.py:51-55` to `:27-33`,
  hoisted above the imports so that the one call which can fire at import time has a logger. `:34`
  is the `getLogger`; `:39` is `logger.critical("'v3' folder not found in project root — layout
  reordering unavailable.")`, which used to be a bare `print` twenty-one lines *above* the comment
  documenting the rule. Alto's own documentation is now true of alto.
- **translator** — `processors/identifier.py:6-7`, a third copy of the rule in a library module,
  with the `getLogger` at `:8`. Not recorded in any previous edition of this digest.

The only `print(` token left anywhere under the five `service/` trees outside the three CLI scripts
is at `alto/service/text_inference.py:33` — inside that rule comment, not a call.

### nlp-enrich's `processing.log` — relayed, not orphaned

The file is still written: `service/enrichment.py:220` names `LOG_FILE` in the passthrough set and
`:254` assigns it `"{ws}/out/processing.log"`. **That is fine, because the records no longer stop
there.** `_run_and_log()` at `:306-347` wraps the pipeline subprocess and ends:

```python
combined = proc.stdout + proc.stderr
for line in combined.splitlines():
    logger.info(line)
return proc.returncode, combined[-4000:]
```

- Both streams are covered (`api_4_stats.sh` tees to `>&2`), so no stage's failure records are lost.
- Used at **both** call sites: `:449` (the dry-run health path, `timeout=30` preserved) and `:511`
  (the enrich path).
- The docstring at `:312-335` records why: the stage scripts' `log()` helper already `tee`s to both
  `$LOG_FILE` and its own stdout, so everything a partner needs was always reaching this process's
  pipes — `capture_output=True` merely stopped it going anywhere onward. It also records that the
  4000-character tail contract and the `subprocess.run` shape are deliberately unchanged, so
  existing `PipelineError` messages, `EnrichmentResult.stdout_tail` and every test fixture that
  patches `service.enrichment.subprocess.run` keep working.
- `logger` itself is `service/enrichment.py:37`, with `import logging` at `:11`.

**Acceptance bullet 3 is satisfied**: `processing.log` is now a per-job artifact, not the only copy.

### The regression guard — `tests/test_logging_contract.py`

- **md5 `c3784b019da04a2860b30d9b4c42e91a`, byte-identical 6/6**: hub canonical
  `docs/templates/shared/test_logging_contract.py` plus `tests/test_logging_contract.py` in all five
  tool repos.
- **Four checks, one per acceptance criterion / defect**: `:151`
  `test_basicconfig_only_in_the_main_block`, `:177` `test_no_print_in_service_library_modules`,
  `:203` `test_entrypoint_reads_log_level`, `:224`
  `test_log_level_controls_at_least_one_call_site` — the last being the "not a placebo" check that
  checks 1 and 3 alone would have passed without.
- **Static, `ast`-based, no imports of the modules under test** — deliberately, because the natural
  runtime check needs pc's `service.inference`, which needs `torch`, which the fast lane omits.
- **`PRINT_ALLOWLIST` at `:80`** = `{"healthcheck.py", "api_client.py", "test_api.py"}` — the three
  standalone CLI scripts whose stdout genuinely is their output contract. Scoping the check to
  `service/*.py` is also its structural answer to the batch-CLI scope question.
- **Skips honestly where there is no `service/` tree** (`:70-75`, `allow_module_level=True`) so the
  hub's own self-check cannot pass two checks vacuously while failing two on an empty set.
- **Manifest row**: `docs/templates/shared/MANIFEST.json:169-178` — `"canonical":
  "test_logging_contract.py"`, `"dest": "tests/test_logging_contract.py"`, `"issue": 61`,
  `"ruff_format_exclude": true`, `"selftest": false`. One of 16 rows.
- **Enforced by one manifest-driven loop**, not a hand-written step:
  `.github/workflows/para-drift.reusable.yml:45-90`. Green at pc `f11936f`,
  [run 34838971686](https://github.com/ufal/atrium-page-classification/actions/runs/34838971686),
  2026-09-14 11:35 UTC, resolving `@v1` to `f675943`.
- **Excluded from `ruff format` in all six**: alto `ruff.toml:58` · llm `:66` · nlp `:43` ·
  pc `:104` · translator `:84`, and for the hub `docs/templates/ruff.toml:110` (the hub's root
  `ruff.toml:30-34` explains that ruff resolves the nearest config, so the declaration has to live
  in the extended base, not the root). Checked directly: `ruff check` passes in alto and nlp, and a
  copy taken out of tree reports "already formatted" at **both** line-length 100 and 120 — so the
  exclusion is belt-and-braces rather than load-bearing today.

### Container proof — `docker-tool.reusable.yml`

Two steps, both gated on `probe-targets`, which is `'["api"]'` in all five repos
(alto `docker.yml:50` · llm `:79` · nlp `:53` · pc `:50` · translator `:37`).

1. **The control**, `:415-450`, folded into the existing HEALTHCHECK step: after the container
   reports healthy, poll its logs for one record matching
   `^[0-9-]+ [0-9:,]+ (DEBUG|INFO|WARNING|ERROR|CRITICAL) [^ ]+ ` (`:430` allowlist, `:438` grep).
   Without it, step 2 could pass because nothing logs at all. The allowlist is deliberately all five
   levelnames, not `INFO` alone: a service whose warmup legitimately fails for want of a CI secret
   logs at `WARNING` through the same formatter and should still pass.
2. **The assertion**, `:553-602`: run the same image with `-e LOG_LEVEL=WARNING` (`:573`), wait for
   healthy, and fail if the `INFO` pattern appears (`:596`). Deliberately the WARNING direction — a
   DEBUG-only test would have passed throughout this repo's own history.

Green on all five repos on **2026-09-14**, every run resolving `@v1` to `f675943`:

| Repo                | Run (branch `test`, at the measured head)                                                              | `LOG_LEVEL=WARNING` step      |
|---------------------|--------------------------------------------------------------------------------------------------------|-------------------------------|
| alto                | [34838653518](https://github.com/ufal/atrium-alto-postprocess/actions/runs/34838653518) @ `f6d9196`    | success, 11:42:22 → 11:42:48Z |
| translator          | [34865982363](https://github.com/ufal/atrium-translator/actions/runs/34865982363) @ `d0c1572`          | success, 16:03:04 → 16:03:20Z |
| page-classification | [34838971713](https://github.com/ufal/atrium-page-classification/actions/runs/34838971713) @ `f11936f` | run green                     |
| nlp-enrich          | [34838487713](https://github.com/ufal/atrium-nlp-enrich/actions/runs/34838487713) @ `bfabfca`          | run green                     |
| llm-enrich          | [34838754391](https://github.com/ufal/atrium-llm-enrich/actions/runs/34838754391) @ `5ac82a5`          | run green                     |

The `base` matrix target skips both steps by design — it is not a runnable server. No container was
started to write this digest (no Docker daemon in the measuring environment); the step timings above
are read off the Actions job records.

### Doc parity — landed

- **`.env.example` declares `LOG_LEVEL=INFO` in all five**: alto `:82` (the one that was missing) ·
  translator `:81` · llm `:86` · pc `:88` · nlp `:90`.
- **All five `service/README.md` are on one wording**, "root logger level for the `<start path>`
  start path (issue #61)", differing only in the start path: alto `:400` (`python
  service/text_api.py`), llm `:102`, nlp `:185`, pc `:312`, translator `:114`.
- **The three "withheld until #61 is verified green" blocks are gone.**
  `grep -rn withheld /home/user/atrium-*/.env.example` returns **nothing**.
- Guarded going forward by `tests/test_env_contract.py`: `LOG_LEVEL` is in the canonical
  `_SHARED_CORE` set at `:104`, and `test_declared_omissions_are_not_also_published` (`:523`)
  catches exactly the withheld-and-published contradiction that shipped here.

### The three acceptance criteria, one by one

| Criterion (issue body)                                                                                 | State                                                                                        |
|--------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------|
| `grep -rn "basicConfig"` across the five tool repos matches only `__main__` blocks (and test fixtures) | ✅ five matches, all `__main__`; the rest are rule comments and the guard's own text          |
| `LOG_LEVEL=DEBUG docker run …` visibly changes output volume for all five services                     | ✅ proved in CI in the WARNING direction, with a format control beside it, green 5/5 on 09-14 |
| Nothing a partner needs to diagnose a failed request is written only to a file inside the container    | ✅ `_run_and_log()` relays combined stdout+stderr at both call sites                          |

Supporting counts, re-measured: `logger.<level>()` call sites under `service/` are alto **10** ·
pc **18** · nlp **3** · llm **3** · translator **4** (plus 3 more in `translator/processors/`).
None is zero, so `LOG_LEVEL` controls something everywhere.

## 🕰️ Historical — the pre-fix tree, as measured 2026-09-11

**Everything in this section describes a tree that no longer exists.** It is kept because it is why
the guard has the shape it has, and it is marked because the previous edition presented it in the
present tense directly above the section recording the fix.

**As measured on 2026-09-11**, `LOG_LEVEL` was wired in five places and effective in two:

| Repo                | `logger.*` sites in `service/` **on 2026-09-11** | Effect of `LOG_LEVEL` **then**                     |
|---------------------|--------------------------------------------------|----------------------------------------------------|
| page-classification | 18                                               | none — overridden by the import-time `basicConfig` |
| alto-postprocess    | 8                                                | worked                                             |
| llm-enrich          | 3                                                | worked                                             |
| nlp-enrich          | **0**                                            | placebo                                            |
| translator          | **0**                                            | placebo                                            |

**As measured on 2026-09-11**, six `print()` calls in four files carried records that belonged in
the event stream — `alto/service/text_inference.py:30`, `alto/service/text_api.py:285`,
`nlp/service/enrichment.py:340` and `:342`, `translator/service/api.py:68` and `:78`. All six were
converted on 2026-09-12. The 41 other `print()` calls in the five `service/` trees were correct then
and are correct now: `healthcheck.py` (×5, invoked by the Docker `HEALTHCHECK`),
`pc/service/api_client.py`, `nlp/service/test_api.py`. **They must stay.** A later reader sweeping
`service/` for `print` would break five healthchecks; `PRINT_ALLOWLIST` (`:80`) is the guard's
record of that, so the exclusion is enforced rather than remembered.

## ✏️ Corrections to this digest's own previous edition

The 2026-09-12 edition was accurate when written. Six of its statements are now false.

1. **Header** — *"**0 comments, opener only** (2026-09-10 12:13), still 0 as of this revision."*
   There is **1 comment**, posted **2026-09-12 07:10**, and the issue's `updated_at` moved with it.
   The thread is no longer silent about the work.
2. **⏳ Open** — *"**Doc parity** (D4/S5) — alto's `.env.example`, both READMEs' wording. Mechanical,
   not yet applied."* Applied. Alto's `.env.example:82` declares `LOG_LEVEL`, all five READMEs are
   on one wording, and the three "withheld" blocks are deleted.
3. **⏳ Open** — *"**No regression guard exists yet** (D5/S6/S7) — designed and proven this round,
   not yet landed."* Landed in all six repos on 2026-09-12 between 08:46 and 08:54, with the
   `MANIFEST.json` row, the six `ruff` exclusions, the manifest-driven para-drift loop and the two
   container steps; the hub's `v1` tag moved to `f675943` and the 09-14 runs resolve to it.
4. **⏳ Open** — *"**The issue trail** (D6/S8) — #61, #58, #60 all show no comment activity despite
   #58 and (now) #61's code being on `test`."* Stale for all three: #61 has 1 comment, #58 has 3,
   #60 has 4 (last updated 2026-09-14 11:37).
5. **D7** — *"`docs/templates/shared/test_atrium_service.py` exists in the hub (396 lines) and in
   **none** of the five tool repos — a canonical file with no vendored copies and no para-drift
   step."* **Resolved by [\#59](https://github.com/ufal/atrium-project/issues/59).** There is no
   `docs/templates/shared/test_atrium_service.py`; the file is now the hub's own
   `/home/user/atrium-project/tests/test_atrium_service.py`, a hub-local test rather than an
   orphaned canonical.
6. **D1 / the "Verified state" `basicConfig` sweep and the D2 table** — presented in the present
   tense, they describe the tree of 2026-09-11 and read as live defects sitting above their own fix.
   Moved to **🕰️ Historical** above and re-labelled. Line drift with them: the translator
   configuration site cited as `service/api.py:335` is now **`:510`**, with `LOG_LEVEL` at `:511`;
   alto's `text_api.py:537` is `:540`; alto's rule comment at `text_inference.py:51-55` is `:27-33`.

## ⏳ Open

- **Nothing in this issue's scope.** All three acceptance criteria pass; the guard, the container
  probe and the doc parity are all landed and green.
- **The issue itself, and only the issue.** #61 is still `state: open`. Closing it is the one
  remaining action — see [`../plans/61.plan.md`](../plans/61.plan.md).
- **Structured/JSON logging remains out of scope**, unchanged from every prior edition. Factor XI
  asks for an event stream on stdout, not a schema.
- **Not ours**: nlp-enrich's workspace lifetime on failure — `API_JOBS_ROOT` is container-local, so
  `processing.log` is unreadable from another replica regardless of the relay. That half stays with
  [nlp\#35](https://github.com/ufal/atrium-nlp-enrich/issues/35).

---
_Digest of [ufal/atrium-project#61](https://github.com/ufal/atrium-project/issues/61). Sub-issue of #53 (factor XI). Re-measured 2026-09-14 against hub `f675943`, alto `f6d9196`, nlp `bfabfca`, llm `5ac82a5`, pc `f11936f`, translator `d0c1572` — in all six, the default branch equals `test` equals the measured tree. Every path:line above was re-checked by hand; no container was started (no Docker daemon in the measuring environment) and no test suite was run locally (no `pytest` there either), so every container- and test-level claim cites a GitHub Actions run instead. Strategy: [`../plans/61.plan.md`](../plans/61.plan.md)._
