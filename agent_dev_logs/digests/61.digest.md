# 📓 Digest — [\#61](https://github.com/ufal/atrium-project/issues/61) `open`: LOGS: as an event stream — no `basicConfig()` in library modules, no per-job log file

> _Condensed from [`../issues/2026-09-10.61.issue.open.md`](../issues/2026-09-10.61.issue.open.md) — **0 comments, opener only** (2026-09-10 12:13), still 0 as of this revision. Carved out of [\#53](https://github.com/ufal/atrium-project/issues/53), factor XI, plan item F._
> _**Re-measured 2026-09-12 against `origin/test`.** The 2026-09-11 revision's code findings (D1–D4) are confirmed **landed** below; this revision adds what landing them exposed as still open._

**Labels**: `enhancement`, `development` · **Parent**: #53 (factor XI) · **Ordering**: the #58 gate has **cleared**, and S1–S3 are now **shipped**

**HEADs measured** — alto `ee51ec3` · page-classification `9683c76` · nlp-enrich `61d9533` · translator `8aeac5f` · llm-enrich `04540e7` (unchanged)

**Status**: 🟢 **Code done and verified on `origin/test`.** 🟡 **Issue still open** — nothing guards the fix, two doc gaps remain, and GitHub has no record any of this shipped. Every claim below was re-measured against the pushed commits, not assumed from the prior revision.

## 🎯 Goal

Logs are an event stream on stdout, configured once at the entrypoint, with a level a partner can set at deploy time. Nothing a partner needs to diagnose a failed request reachable only from inside a dead container.

## ⚠️ What changed under the previous revision of this digest

The 2026-09-10 measurement recorded that four of five services configured the root logger **nowhere** and that `LOG_LEVEL` was read in **exactly one place ecosystem-wide** (`alto/service/text_api.py:538`). That was correct when written. It is now obsolete in both halves.

[\#58](https://github.com/ufal/atrium-project/issues/58) ("port 8000 standard") landed on `origin/test` across all five tool repos and, while rewriting each `__main__`, **created the root-logger configuration block in four of them**. Three of those blocks carry an `(issue #61)` comment. `LOG_LEVEL` is now read in **five** places, not one.

So the headline argument of the old digest — "four of five services have no log-level control of any kind" — can no longer be made from the config sites. It survives, but for a different reason, and the reason is worse: **the knob is now wired in all five and works in two.**

> One consequence for the reader of a neighbouring plan: [`../plans/60.plan.md`](../plans/60.plan.md)'s status line asserts that "#58, #61 and #63 all landed". **#58 did; #61 did not.** #60's sequencing rests on that belief and should be re-checked against this digest.

## 🔎 Verified state (2026-09-11)

### The configuration points — all five now exist, and all five are reachable

Every API stage executes its `__main__` in the container, so none of these blocks is decorative:

| Repo                | Config site               | Container entrypoint (api stage)               |
|---------------------|---------------------------|------------------------------------------------|
| alto-postprocess    | `service/text_api.py:537` | `ENTRYPOINT ["python", "service/text_api.py"]` |
| page-classification | `service/api.py:360`      | `ENTRYPOINT ["python", "-m", "service.api"]`   |
| nlp-enrich          | `service/api.py:558`      | `ENTRYPOINT ["python", "-m", "service.api"]`   |
| translator          | `service/api.py:335`      | `ENTRYPOINT ["python", "-m", "service.api"]`   |
| llm-enrich          | `service/api.py:531`      | `ENTRYPOINT ["python", "-m", "service.api"]`   |

All five carry alto's block verbatim:

```python
logging.basicConfig(
    level=os.getenv("LOG_LEVEL", "INFO").upper(),
    format="%(asctime)s %(levelname)s %(name)s %(message)s",
    stream=sys.stdout,
)
```

The full `basicConfig` sweep the acceptance criterion asks for, re-run on `origin/test`:

```
$ grep -rn "basicConfig" --include=*.py <five repos>
alto/service/text_api.py:537:    logging.basicConfig(              ← __main__ ✅
alto/service/text_inference.py:51:# (12-factor XI) No basicConfig() here…  ← the rule, in a comment
page-classification/service/inference.py:22:logging.basicConfig(level=logging.INFO)   ← ✗ THE defect
page-classification/service/api.py:360:    logging.basicConfig(     ← __main__ ✅ (dead, see D1)
nlp-enrich/service/api.py:558:    logging.basicConfig(              ← __main__ ✅
translator/service/api.py:335:    logging.basicConfig(              ← __main__ ✅
llm-enrich/service/api.py:531:    logging.basicConfig(              ← __main__ ✅
```

**One line fails the criterion.** It is the same line the issue named — but its consequence is not the one the issue describes.

### D1 — page-classification: the #61 fix is already written, and it is dead code

`service/api.py:16` imports the library module at **module scope**:

```python
try:
    from .inference import manager
except ImportError:
    from inference import manager
```

That import executes `service/inference.py:22`:

```python
# Setup logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)
```

…which installs a handler on the **root** logger. The `__main__` block at `service/api.py:360` then runs and calls `basicConfig` again — and `logging.basicConfig` is **documented to do nothing** when the root logger already has handlers and `force=True` is not passed.

The issue predicted "the first import wins over any later `basicConfig` in a real entrypoint." That is exactly right, and it has now actually happened. Three consequences, none visible unless you read both files together:

1. **Level pinned to `INFO`.** `LOG_LEVEL` is read at `api.py:361` and discarded.
2. **Stream is `stderr`**, `basicConfig`'s default — not the `sys.stdout` the block three lines below asks for.
3. **Format is the default** `%(levelname)s:%(name)s:%(message)s` — not the agreed alto string. **Format parity across the five already fails, silently.**

And `service/README.md:312` documents `LOG_LEVEL` as working. This is worse than an unimplemented knob: it is a knob that reads as implemented in code review, in a file whose `__main__` says `(issue #61)`.

Blast radius is small — only `service/api.py:16-18` and `tests/` import `inference.py`; the batch CLI path (`run.py`, `entrypoint.py`) does not. The `try/except` at `inference.py:15-20` means the module is importable under two names; both must reach a configured root after the fix.

### D2 — `LOG_LEVEL` is a placebo in three of five services

This is the finding that is **not in the issue** and not in the previous revision.

Neither `atrium-nlp-enrich/service/` nor `atrium-translator/service/` imports `logging` at module scope anywhere, and neither contains a single `logging.getLogger` call. Their only `import logging` sits **inside** the `__main__` block that configures the root logger. They configure an event stream that no record ever enters.

| Repo                | `logger.*` call sites in `service/`                                  | Effect of `LOG_LEVEL` today |
|---------------------|----------------------------------------------------------------------|-----------------------------|
| page-classification | 18 — `inference.py` 13, `api.py` 5                                   | **none** — overridden by D1 |
| alto-postprocess    | 8 — `text_inference.py` 5, `utils.py` 3; **`text_api.py` itself: 0** | ✅ works                     |
| llm-enrich          | 3 — `api.py:204`, `:208`, `:374`                                     | ✅ works                     |
| **nlp-enrich**      | **0**                                                                | **placebo**                 |
| **translator**      | **0**                                                                | **placebo**                 |

So the issue's acceptance criterion — "`LOG_LEVEL=DEBUG docker run …` visibly changes output volume for all five services" — **fails for three services today, and would still fail for two after the `inference.py:22` line is deleted.** Deleting that line is necessary and nowhere near sufficient.

Where those services' records actually go is `print()`. **47 `print()` calls sit in the five `service/` dirs, but 41 of them are correct and must be left alone** — they belong to three standalone CLI scripts that run as their own process, where stdout *is* the output contract:

- `service/healthcheck.py` (×5 repos, 15 calls) — invoked by the Docker `HEALTHCHECK`;
- `page-classification/service/api_client.py` (9) — a CLI client;
- `nlp-enrich/service/test_api.py` (18) — a CLI smoke test.

Excluding those, the real inventory is **6 sites in 4 files**:

| Site                                | What it is                                                                                                                           |
|-------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------|
| `alto/service/text_inference.py:30` | `print("CRITICAL: 'v3' folder not found in project root — layout reordering unavailable.")`, at **import time**, in a library module |
| `alto/service/text_api.py:285`      | a request-path `WARNING` to **stderr**, about skipping the accretion                                                                 |
| `nlp/service/enrichment.py:340`     | `print("[warmup] KeyBERT model loaded.")`                                                                                            |
| `nlp/service/enrichment.py:342`     | `print(f"[warmup] KeyBERT warmup failed: {exc}. Will degrade gracefully.")`                                                          |
| `translator/service/api.py:68`      | `print(f"[INFO] Warming up translation backend …")`                                                                                  |
| `translator/service/api.py:78`      | `print("[INFO] Shutting down service...")` — the **#55 drain record**                                                                |

Four of these six are records a partner needs and cannot get:

- **`text_inference.py:30` is the sharpest.** A `CRITICAL`-severity condition — layout reordering silently unavailable for the life of the process — announced by an un-timestamped, un-named `print` at import time, **21 lines above the comment at `:51` that documents the factor-XI rule.** The repo the issue holds up as the model violates factor XI itself, in the very file that writes the rule down. The issue's framing ("alto-postprocess is the model here") is right about `basicConfig` and wrong about the file.
- **`text_api.py:285`** is described by its own docstring as guarding "the silent-wrong-data failure this whole issue is about" — and it goes to `stderr`, split from every other record, invisible to `LOG_LEVEL`.
- **`enrichment.py:342`** is a *degradation* record. As a `print` it cannot be levelled, filtered, or alerted on.
- **`translator/service/api.py:78`** is the shutdown line whose loss costs a diagnosis during a rollout — the one case #55 exists for.

llm-enrich needs no conversion. It is the only repo whose service logging is already correct end to end.

### D3 — nlp-enrich's `processing.log`: the file is a symptom, the parent is the disease

The issue asks what `processing.log` is for and assumes it is file-only. **It is not**, and the answer is mechanical rather than a matter of policy.

`api_util/api_common.sh:41` — the helper every stage script logs through:

```bash
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" | tee -a "$LOG_FILE"
}
```

`tee` writes the file **and stdout**. The shell pipeline already mirrors every record. They become unreachable one level up, in the Python parent at `service/enrichment.py:472-480`:

```python
proc = subprocess.run(cmd, cwd=..., env=..., capture_output=True, text=True)
rc = proc.returncode
tail = (proc.stdout + proc.stderr)[-4000:]
```

`capture_output=True` pipes the child's streams into the parent and **nothing relays them onward**. Only the last 4000 characters survive: on success as `stdout_tail` on the result object, on failure inside a `PipelineError` message. In a multi-stage run, early-stage diagnostics are truncated away — precisely the records wanted when stage 1 misbehaved and stage 7 is what finally raised.

Then `service/enrichment.py:532`:

```python
except Exception:
    if not _KEEP_WORKSPACES:
        shutil.rmtree(workspace, ignore_errors=True)
    raise
```

`_KEEP_WORKSPACES` (`enrichment.py:41`, from `API_KEEP_WORKSPACES`) is **off by default**, so on a failed job the workspace — and `processing.log` inside it — **is deleted**. The file exists on exactly the runs nobody needs it for, and is destroyed on the runs someone does. The same pattern guards the dry-run health path at `enrichment.py:402-413`.

So the issue's open question resolves on the evidence, without a policy decision: the records are already mirrored; the parent discards them. **Relay the child's streams and `processing.log` becomes a harmless per-job artifact** — no "keep it or delete it" call required. One caveat for whoever implements: `api_4_stats.sh:145` tees to `>&2`, so a relay must cover **stderr as well as stdout**.

The workspace-lifetime half of this remains [nlp#35](https://github.com/ufal/atrium-nlp-enrich/issues/35)'s (`API_JOBS_ROOT` is container-local, so the log is unreadable from any other replica regardless). Coordinate; do not decide it twice.

### D4 — documentation and parity gaps (as measured 2026-09-11; **fixed by S5** below except where noted)

- **`.env.example`** — nlp-enrich `:55` and translator `:76` carry `LOG_LEVEL=INFO`; **alto-postprocess's `.env.example` omits it** (`grep -c LOG_LEVEL` → 0), and page-classification and llm-enrich have no `.env.example` at all. Boundary with [\#60](https://github.com/ufal/atrium-project/issues/60) — flag, do not absorb. **S5 fixes alto's; the two missing files stay #60's, now unblocked (see D6).**
- **`service/README.md` wording drift** — alto `:400` and page-classification `:312` say "root logger level for the start path below"; nlp-enrich `:193`, translator `:44` and llm-enrich `:111` say "…for the `python -m service.api` start path (issue #61)". **S5 unifies all five.**
- **Batch CLI entrypoints** — `run.py`, `run_pipeline.py`, `main.py`, `llm_run.py`, `entrypoint.py` configure no root logger at all. The `grep -rn basicConfig` criterion passes **vacuously** for them. A scope decision, not an oversight — but it should be written down as one. **S6 makes this a structural test boundary rather than a sentence someone has to remember.**

## 🆕 Round 2 (2026-09-12) — S1–S3 landed; what remains

Re-measured every acceptance criterion directly against the pushed commits rather than trusting
the prior revision:

```
$ grep -rn "basicConfig" --include=*.py <five repos>       # only __main__ + rule comments, 0 violations
$ grep -rn "print(" <five repos>/service                    # only healthcheck.py/api_client.py/test_api.py
$ logger.*(...) call-site counts: alto 10 · pc 18 · nlp 3 · translator 2 · llm 3   # all >0
```

All three pass. **The code half of #61 is done.** Four things are not:

### D5 — nothing guards this going forward, and the obvious guards don't reach

[`60.plan.md`](../plans/60.plan.md) itself proposes the natural test: assert that importing
`service.inference` installs no root handler. It doesn't exist, and **it would be the wrong
test** — page-classification's `service.inference` needs `torch`, the fast lane omits `torch`,
and the equivalent runtime check in `tests/test_service_entrypoint.py:108` is already
`@pytest.mark.slow` for exactly that reason. A runtime import-based guard would skip in the one
repo #61 was filed against.

Ruff doesn't reach either: no rule expresses "`basicConfig` outside `__main__`", and scoping
`T201` (print) to `service/` needs enumerating almost every other file in five repos — measured
88 (translator) to 309 (alto) legitimate prints in batch CLIs, and `per-file-ignores` has no
negated glob.

**S6** adds a static (`ast`-based, no imports) guard instead: one file, byte-identical across
the hub and all five repos, enforced by para-drift like `atrium_service.py`/`healthcheck.py`,
covering all four acceptance criteria. Proven against the actual historical defects, not just
written: reintroducing D1 in a scratch copy of `inference.py` fails it; removing translator's
`logger.info` calls fails it. **S7** adds the one thing the static guard structurally cannot
prove — that a running container's output volume actually changes — as two container-log
assertions beside the existing #55/#58 probes in `docker-tool.reusable.yml`.

### D6 — the GitHub issues have no record any of this happened

#61: **0 comments**, `state: open`, `updated_at` unchanged since 2026-09-10 12:13 — despite four
repos' code landing on `test`. #58 and #60 are the same shape (2–3 comments each, still open).
The commits went straight to `test`, not through a PR that would auto-close or reference #61, so
GitHub has no trail. **S8** posts the comment.

[`60.plan.md`](../plans/60.plan.md)'s status line — "the dependency chain … has cleared — #58,
#61 and #63 all landed" — is accurate for the *code* but the *issue* is still open; worth a note
on #60 rather than editing another issue's plan file.

### D7 — the hub's shared-file set has one pre-existing orphan (not #61's)

`docs/templates/shared/test_atrium_service.py` exists in the hub (396 lines) and in **none** of
the five tool repos — a canonical file with no vendored copies and no para-drift step. Found
while confirming S6's new entry wouldn't collide with an existing one. Unrelated to #61 and not
fixed here; noted so it isn't mistaken for something this round touched.

## 🔗 Coupling to other sub-issues

- **[\#58](https://github.com/ufal/atrium-project/issues/58) — gate cleared,** as of the prior revision; unchanged.
- **[\#60](https://github.com/ufal/atrium-project/issues/60) — now genuinely unblocked, still hasn't moved.** `LOG_LEVEL` is effective in all five as of this revision, so #60 can publish it honestly for the first time — its own plan's condition is met. The two missing `.env.example` files are #60's to add, not #61's.
- **[\#55](https://github.com/ufal/atrium-project/issues/55) — resolved.** Translator's drain record is a `logger.info` call now, landed with S2.
- **[nlp#35](https://github.com/ufal/atrium-nlp-enrich/issues/35) — still owns the disk half of D3.** Unaffected by this round.

## ⏳ Open

- **Doc parity** (D4/S5) — alto's `.env.example`, both READMEs' wording. Mechanical, not yet applied.
- **No regression guard exists yet** (D5/S6/S7) — designed and proven this round, not yet landed.
- **The issue trail** (D6/S8) — #61, #58, #60 all show no comment activity despite #58 and (now) #61's code being on `test`.
- **Structured logging is still not on the table.** Unchanged from the prior revision.

---
_Digest of [ufal/atrium-project#61](https://github.com/ufal/atrium-project/issues/61). Sub-issue of #53 (factor XI). Re-measured 2026-09-12 against pushed `origin/test` commits — S1–S3 confirmed landed and re-verified independently; S4–S8 track what remains. Strategy: [`../plans/61.plan.md`](../plans/61.plan.md)._
