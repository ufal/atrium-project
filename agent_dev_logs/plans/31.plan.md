# Strategy — agent-skill ↔ test service-API alignment (#31 §12.2 · #32)

> #31 [agent-skill] · #32 [API standard] · Milestone Q3 [WP8/WP7] · **Status: 🟢 Landed on all five
> `agent-skill` branches — service/ now == test service (helper-based §4 meta-contract); one
> residual (pc `service/test_api.py` to delete). Enforcement stays on default/test.**
> Full narrative: [`agent-skill-sync.digest.md`]. Standard: `docs/agent_skill_strategy.md` §4/§5/§12.2.

## Context
The #32 OpenAPI meta-contract landed on every repo's `test` `service/`; the `agent-skill` branches
still shipped the pre-#32 service. Per §5 ("`service/` — unchanged from the default branch") and the
§12.2 forward-merge policy, the skill branches must track it. This syncs each `agent-skill`
`service/` to its `test` version (+ the runtime deps the service imports).

Decisions (locked):
1. **Overlay `test:service/`**, don't blind-checkout — preserve skill-only assets, exclude stray blobs.
2. **Meta-contract only** (per #32) — `/info`+`/health`+error codes+`MAX_UPLOAD_MB`; endpoints unchanged.
3. **Close the runtime closure** — add modules the synced service newly imports (nlp `bbox_scale`,
   `atrium_paradata`) so the skill service actually runs, not just imports.
4. Deliver as full files; maintainer lands on `agent-skill`.

## Status snapshot (origin/agent-skill)
| Repo                | agent-skill HEAD | State                                                                                             |
|---------------------|------------------|---------------------------------------------------------------------------------------------------|
| page-classification | `9e61f0c`        | ✅ helper + `/info`+`/health`; `+frontend-lindat`; ⚠️ `service/test_api.py` still present (delete) |
| translator          | `01bbe75`        | ✅ helper + `api.py`; skill-only README+frontend preserved                                         |
| alto-postprocess    | `fe44e59`        | ✅ helper + `text_api.py`/`text_inference.py`/`utils.py` + frontends                               |
| nlp-enrich          | `a85f8e5`        | ✅ helper + `api.py`; `+api_util/bbox_scale.py` + `+atrium_paradata.py` (runtime closure)          |
| llm-enrich          | `1772374`        | ✅ helper + `api.py`; skill-only frontend preserved                                                |

## Verified state
- `service/atrium_service.py` byte-identical (`md5 6557be0…`) across all five + hub.
- `compileall` clean; alto/nlp/llm TestClient-smoke green (`/info` envelope, `/health` 200, OpenAPI valid).
- Skill-only frontends/README preserved (translator, llm); pc stray PDF excluded; nlp deps present.

## Open items / next steps
1. pc: `git rm service/test_api.py` (incomplete rename — `api_client.py` supersedes it).
2. §31 W2 acceptance per branch (server.sh → client smoke on samples → assert /info+/health).
3. Skill-validate CI green + version-tag the skill branches (separate from api-contract).
4. Standing: forward-merge future `test` `service/` changes to `agent-skill` (§12.2).

## Critical files
- Add/replace everywhere: `<repo>/service/atrium_service.py` + `service/{api,text_api}.py` (+ deps).
- Runtime closure: `atrium-nlp-enrich/{api_util/bbox_scale.py, atrium_paradata.py}`.
- Preserve: `atrium-translator/service/{README.md,frontend/*}`, `atrium-llm-enrich/service/frontend/*`.
- Delete: `atrium-page-classification/service/test_api.py`.