# Strategy — agent-skill ↔ test alignment (#31 §12.2 · #32 OpenAPI · #13 accretion)

> Issue [#31](https://github.com/ufal/atrium-project/issues/31) · Labels: `enhancement`,
> `development` · Milestone: Q3 [WP8/WP7] · Type: Feature · **Status: 🟢 Enforcement live —
> §12.3 step 4 is implemented and `skill-validate` is green on all five `agent-skill` branches
> (2026-07-29), with step 4b confirmed asserting for real, not skipping. 🟡 Content alignment
> partial — nlp-enrich is done (drift 22 files → 3); the other four still need the #13 accretion
> port, `atrium_document.py`, and a version bump. The two hub items recorded as "never landed"
> were located and relocated 2026-08-05 (issue #10 R2) — see §Hub gaps.**
> Full narrative + thread trail: [`digests/31.digest.md`](../digests/31.digest.md). Normative
> standard: [`docs/agent_skill_strategy.md`](../../docs/agent_skill_strategy.md) §4 / §5 / §12.2.

## Context
The `agent-skill` branches are trimmed derivatives of the default branches (§5), kept current by
porting `service/` changes forward **by hand** — for alto-postprocess, nlp-enrich and
page-classification there is no merge base at all, so `git merge` is unavailable and `git log`
cannot answer "how stale is this branch?".

The 2026-07-24 sync landed the #32 meta-contract, then went stale when `test` wired the **#13
Document-JSON accretion model** into three services and bumped every repo's `para_config.txt`.
The 2026-07-29 pass closed the *enforcement* half of that gap: `skill-validate.reusable.yml`
step 4 now checks that the API a SKILL.md advertises is the API the service serves, and it is
green everywhere. The *content* half is done for nlp-enrich and outstanding for the rest.

Decisions locked (2026-07-29, unchanged):
1. **Manual landing, scripted diagnosis** — close drift by hand per §12.2; `skill_drift_check.py`
   and `skill_ify.py` do the diagnosis so the next sync is cheap.
2. **Port the accretion feature fully**, including the runtime modules it needs.
3. **Full contract check in CI** — step 4, both passes. ✅ done.
4. Work lands on `claude/agent-skills-test-alignment-yowvdc`, cut **from `agent-skill`**.

## Status snapshot (verified 2026-07-29)
| Repo                | `test`    | `agent-skill` | skill-validate | content drift remaining                                                |
|---------------------|-----------|---------------|----------------|------------------------------------------------------------------------|
| page-classification | `73bbd13` | `deb2187`     | ✅ #5           | 11 files · version `v1.5.1-beta`→`v1.7.0-beta`                         |
| translator          | `6a28704` | `fce1091`     | ✅ #4           | 8 files · accretion · `atrium_document` · `v0.8.1`→`v0.10.0`           |
| alto-postprocess    | `f073a3f` | `f08cd95`     | ✅ #5           | 11 files · accretion · 4 modules · `v1.0.0-beta`→`v1.4.0-beta`         |
| nlp-enrich          | `8f8df2a` | `1a8ff2c`     | ✅ #7           | **3 files** (2 idf pickles + 1 intentional) · 7 file modes · 3 orphans |
| llm-enrich          | `86433f9` | `a90aa92`     | ✅ #4           | 9 files · accretion · `atrium_document` · `v0.2.0`→`v0.4.0`            |
| hub                 | `eb5ec4a` | *n/a*         | —              | see §Hub gaps                                                          |

`atrium_document.py` is absent from **all five** skill branches; the four repos other than
nlp-enrich are still out of `atrium_paradata.py` parity.

## Done — do not re-do
- **§12.3 step 4 implemented and green on all five.** Two passes: **4a** static and
  zero-dependency (documented `GET /x` vs route decorators + mounts in `service/*.py`), which
  never skips; **4b** live (`/info` §4.1 envelope, `/health`, documented endpoints vs
  `app.openapi()`, spec validity), which import-skips on model-heavy repos but emits a CI
  warning so a skip cannot read as a pass. Confirmed asserting for real — llm-enrich logs
  `OK: /info envelope, /health, and 4 OpenAPI paths conform to §4.1`.
- **All five callers** pass `app-import` + `primary-endpoints`, matching each repo's
  default-branch `tests/test_api_contract.py`, so both branches assert one contract.
- **Route sets identical** on all five; `service/atrium_service.py` byte-identical everywhere;
  `service/api.py` byte-identical on nlp-enrich and page-classification.
- **nlp-enrich content alignment** — see §nlp-enrich below.

## Hub gaps (block §12.2 being usable)
1. ~~**`tools/skill_drift_check.py` and `tools/skill_ify.py` never landed**~~ — **RESOLVED
   2026-08-05** (issue #10, roadmap R2). They *had* landed, at
   `docs/templates/skill/`, while all ~12 citations (§12.2, `agent_skill_strategy.md:219,492,497`,
   this file, and both scripts' own usage docstrings) pointed at `tools/`. Since the hub already
   had a `tools/` directory the paths looked plausible and failed only when run. Resolved in the
   direction the citations already assumed: both scripts `git mv`'d to `tools/`, because they are
   executable maintainer tooling — unlike the rest of `docs/templates/skill/`, which is
   copy-into-your-repo template material — and `tools/ci/` + `tools/e2e/` is where the hub's other
   executable tooling already lives. Every citation is now true as written.
2. **`docs/templates/workflows/skill-validate.caller.example.yml` is still the pre-step-4
   version** — no `app-import`, no `primary-endpoints`, while all five live callers pass both.
   Anyone copying the template gets a silently degraded check.
3. **`agent_dev_logs/DEVLOG.md`** has no 2026-07-29 entry.

## nlp-enrich — done, with three leftovers
Aligned 2026-07-29 (`1a8ff2c`); drift 22 files → 3. What the pass found:
- **`api_util/api_common.sh` ran the pre-FIX-#11 preflight demanding `analyze.py`** — a file on
  no branch of the repo. Fixed by adopting test's version.
- **`config_api.txt` pinned `MODEL_NAMETAG` to `nametag3-czech-cnec2.0-240830`** vs test's
  `nametag3-multilingual-onto-260521`. That key is outside `_derive_config`'s override set, so
  the skill service ran an older NER model than the pipeline. (`OUTPUT_DIR="../../ARUB"` and the
  missing `PARADATA_DIR` looked worse but are overridden per workspace — harmless.)
- **Root `requirements.txt` omitted `yake`/`keybert`/`sentence-transformers`** while
  `keywords.py` imports all three, and `service/requirements.txt` claimed the root file provided
  them.
- **`service/requirements.txt` on `test` is the broken one** — it holds
  `pytest`/`pytest-cov`/`openapi-spec-validator`, test deps mislanded into the service file
  (same defect class #32 hit on llm-enrich). The skill branch keeps the correct
  fastapi/uvicorn/python-multipart set; this is the one intended divergence. **Fix `test`.**

Leftovers: delete the now-orphaned `api_util/manifest.py` + `api_util/nametag.py` (nothing
references them once the FIX-#11 `api_common.sh` landed) and `service/test_api.py` (§5 trims
tests); restore `100755` on the four `api_*.sh`, `api_util/summarize_nt_udp.py` and
`service/enrichment.py`; the two `ker_data/*.pickle` differ from test by one byte.

## Remaining work
1. **Land the three hub gaps** above — items 1–2 are the priority, since §12.2 currently points
   at missing tools.
2. **Accretion port** — translator, alto-postprocess, llm-enrich: carry the optional
   `document_json` / `document_record` parameter and accreted response field, plus
   `atrium_document.py` (+ alto's `document_hook.py`, `extract_JSON_2_TXT.py`, `text_util.py`).
3. **Shared re-baseline** on the four non-nlp repos — `atrium_paradata.py` parity, `test`'s
   `para_config.txt` version, `CITATION.cff`.
4. **Skill layer** — document the accretion parameter in `SKILL.md` and add the matching
   stdlib-only flag to each `scripts/atrium_<verb>.py` (translator additionally parses
   `multipart/mixed`).
5. **Frontend defects** — llm-enrich and translator branch READMEs claim a frontend "mounted at
   `/frontend`" that neither `service/api.py` mounts; alto mounts `/static`, not `/frontend`, on
   **both** branches, so the 2026-07-22 "unified `/static`→`/frontend`" never landed. Each needs
   a call: add the mount or drop the claim.
6. **alto's forked `text_util_langID.py`** (1560 ln vs test's `text_util.py` 2455) — retire the
   fork (recommended) or record the `process_json` omission as an intentional §12.2 divergence.
7. **Tooling gap** — `skill_drift_check.py`'s runtime closure follows Python imports only. It
   under-reported nlp-enrich, whose `service/enrichment.py` reaches `run_pipeline.py`,
   `api_*.sh` and `api_util/` by **subprocess**. Teach it to follow `subprocess` invocations.
8. **Still open from earlier passes** — §13 sub-issues ×5; forward-merge the §10.1/10.3/10.4
   pre-work to the default branches; version tags; live acceptance-runbook round-trips;
   W5 llm-enrich async jobs.

## Critical files
- **Hub**: `tools/skill_drift_check.py`, `tools/skill_ify.py` (landed; relocated from
  `docs/templates/skill/` 2026-08-05, issue #10 R2),
  `docs/templates/workflows/skill-validate.caller.example.yml` (stale),
  `.github/workflows/skill-validate.reusable.yml` (landed), `docs/agent_skill_strategy.md`
  §5/§12.2/§12.3 (landed).
- **Port `test` → `agent-skill`**: `atrium-translator/service/api.py`,
  `atrium-llm-enrich/service/api.py`,
  `atrium-alto-postprocess/service/{text_api,text_inference,utils}.py`.
- **Hoist onto skill branches**: `atrium_document.py` (all four),
  `atrium_document.schema.json`; `document_hook.py`, `text_util.py`, `extract_JSON_2_TXT.py`
  (alto).
- **Skill layer**: `SKILL.md`, `scripts/atrium_{keywords,translate,postprocess,classify}.py`,
  `service/README.md`, `para_config.txt` / `setup/para_config.txt`.

## Verification
1. `python3 tools/skill_drift_check.py` exits 0 (allowlist: `README.md`, `Dockerfile`,
   `docker-compose*`, `.gitignore`; plus nlp-enrich's `service/requirements.txt`).
2. Spec parity: `bash scripts/server.sh` → `curl -s :8000/openapi.json | jq -S .paths` on the
   skill branch vs the same from `test`; paths **and** accretion param/response schemas match;
   `/info.version` equals `test`'s `para_config.txt`.
3. Each repo's `test`-branch `tests/test_api_contract.py` passes against the skill branch's
   service module.
4. Client round-trip per repo on `small_data_samples/`, with and without the accretion flag;
   the returned record validates against `atrium_document.schema.json`. **nlp-enrich needs a
   live smoke run** — its `MODEL_NAMETAG` change was verified statically only.
5. One green `skill-validate` per branch (✅ all five as of 2026-07-29).
6. Clients compile + `--help` in bare `python:3.11-slim` (step 3).
