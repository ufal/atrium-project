# ATRIUM #32 (OpenAPI meta-contract) — finalized strategy for remaining actions

## State (landed on test + agent-skill)
- **Meta-contract**: all 5 services + hub aligned; `service/atrium_service.py` byte-identical
  (md5 6557be0…), para-drift-guarded; `/info` envelope + `/health` + §4.4 codes everywhere.
- **Hub CI**: `api-contract.reusable.yml` fixed (valid `workflow_call`, exit-5 tolerant, `09c690f`);
  para-drift covers the helper; `32.digest.md`/`32.plan.md` committed.
- **All gap fixes landed**: pc caller `api-contract.yml`, pc+alto `requirements-test.txt`,
  llm `service/requirements.txt` rename.
- **agent-skill branches**: all 5 synced to their test service (pc `9fd72e6` — `test_api.py`
  removed, `api_client.py` in, PDF excluded; nlp runtime deps `bbox_scale.py`+`atrium_paradata.py`
  added; translator/llm skill-only frontends preserved).
- **api-contract CI**: ✅ pc, alto, llm · ❌ nlp, translator (stale — last ran before `09c690f`).

## Further actions (priority order)
1. **Re-trigger api-contract on nlp + translator** — their last run predates the reusable fix, so
   a no-op re-push to each `test` (or "Re-run all jobs") resolves the valid reusable → green
   (nlp asserts; translator skip-tolerated). *Only open #32 CI item.*
2. **Forward-merge `test` → default branches** once green ecosystem-wide (hub `main`; pc
   `vit`/`master`; tool repos `master`/`main`) — the §12.2 manual-sync item; default-branch readers
   still see the pre-#32 API.
3. **agent-skill finalization (#31 W2/W4)** — per branch: `server.sh` → client smoke on
   `small_data_samples/` asserting `/info`+`/health`; get `skill-validate` CI green (separate check);
   version-tag each `agent-skill` branch.
4. **Close #32** — flip the `plan_repo_review.md` §5.A A-rows to ✅ and mark the issue done once 1+2
   are green.

## Standing / optional
- **§12.2 sync stays live**: future `test` `service/` changes forward-merge to `agent-skill`
  (+ their runtime-dep closure, as the nlp `bbox_scale`/`atrium_paradata` case showed).
- **Deferred #32 follow-ups (out of scope)**: response-body Pydantic typing + shared `ErrorModel`
  for a richer `/openapi.json`; fold nlp's own `_read_tool_version` onto the shared helper; route
  llm-enrich ALTO input through the TEITOK reader.