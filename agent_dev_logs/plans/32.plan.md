# Strategy — Issue #32: API services per repo should be standardized (OpenAPI meta-contract)

> Issue [#32](https://github.com/ufal/atrium-project/issues/32) · Labels: `enhancement`, `development` ·
> Milestone: Q3 [WP8/WP7] · Type: Task · Assignees: rharasim, K4TEL · **Status: 🟡 Meta-contract landed
> on all five services' `test` HEADs + the hub (2026-07-23); reference = nlp-enrich. Remaining work is
> CI plumbing — five defects with fixes prepared as patches (hub `api-contract.reusable.yml` malformed
> tail → 0 jobs; pc caller filename `api-contract.ym;`→`.yml`; pc+alto empty `requirements-test.txt`;
> module-skip→exit-5 false failure; llm-enrich `service/requirements.txt` mislanded). Then: one green
> run per repo + forward-merge to defaults.**
> Full narrative + thread trail: [`digests/32.digest.md`](../digests/32.digest.md). Normative
> standard: [`docs/agent_skill_strategy.md`](../../docs/agent_skill_strategy.md) §2.2 / §4 / §12.3.

## Context
Standardize each service repo's HTTP API to the **OpenAPI-followed meta-contract** of
`docs/agent_skill_strategy.md` §4, then enforce it in CI so it can't drift — the same federation
model that `para-drift.reusable.yml` uses for the paradata trio. This plan is the issue-level index;
§4 of the strategy doc is the source of truth for the contract itself.

Decisions locked at planning time (unchanged):
1. **Meta-contract only** — uniform `GET /info` (`service`/`version`/`endpoints`/`limits`) +
   `GET /health` (shallow + `?deep=true`), §4.4 error table (413/415/422/429/500/502/503), canonical
   `MAX_UPLOAD_MB` + `ALLOWED_ORIGINS` default `*`, version from `para_config.txt`. **Domain endpoints
   not renamed** (`/predict_*`, `/translate`, `/process`, `/enrich`, `/extract_keywords`).
2. **Build llm-enrich a `service/` first** (it had none) — all five repos share one server standard.
3. **No committed OpenAPI snapshots** — validate the live `/openapi.json` (spec-validator) + the
   `/info`/`/health`/endpoint-set assertions via per-repo `tests/test_api_contract.py`; the hub
   `api-contract.reusable.yml` runs it, and `para-drift` guards `service/atrium_service.py` parity.
4. **Enforce-by-copy** — one canonical `docs/templates/shared/atrium_service.py`, byte-identical in
   each `service/` (not a monorepo, not submodules; §5.H model).

## Status snapshot (origin/test, 2026-07-23)
| Repo | test HEAD | Meta-contract state |
|---|---|---|
| page-classification | `c616d68` | ✅ aligned — helper (byte-identical), `build_info` `/info` (`limits{max_upload_mb,max_pdf_pages}`), `/health`, contract test. ⚠️ caller filename `api-contract.ym;` (won't run) |
| translator | `abfdb5f` | ✅ aligned — `name`→`service`, `endpoints`+`limits`, `/health`, non-XML **400→422**, `MAX_UPLOAD_MB` |
| alto-postprocess | `e19edc9` | ✅ aligned — `service`+`endpoints`+`limits`, `/health`, 500→4xx + 413 guard, CORS `*`, `resolve_max_upload_mb(25)` |
| nlp-enrich | `acbba7c` | ✅ aligned (reference) — gained `endpoints` via `list_endpoints`; already had `service`/`limits`/`/health` |
| llm-enrich | `12a1618` | ✅ new `service/` — `/info`+`/health`+`/extract_keywords`(`_text`), torch-free engine, Docker `api` stage. ⚠️ `service/requirements.txt` mislanded as `service/atriumllmenrich__service__requirements.txt` |
| hub (atrium-project) | `6ba36b5` | ✅ canonical `atrium_service.py`, `api-contract.reusable.yml`, `para-drift` +`atrium_service.py` step, `plan_repo_review.md` §5.A |

Healthy across all five: `/info` reads `para_config.txt`; helper byte-identical to hub (para-drift
passes on all five, pc included); torch-free services' contract tests pass locally (alto, nlp, llm —
5/5 each); import closures intact.

## Verified state (2026-07-23)
- **Landed & byte-verified**: the hub canonical + four aligned copies of `atrium_service.py` share one
  md5; each aligned repo carries `tests/test_api_contract.py` + an api-contract caller; the reusable +
  para-drift step are on hub `test`.
- **CI not yet green — root cause found**: `api-contract` first runs on translator/nlp/alto/llm are
  `failure` with **0 jobs** because the hub `api-contract.reusable.yml` has a **malformed duplicated
  tail** (a second `name:`/`on:`/`jobs:`), so its effective `on:` lacks `workflow_call` and `jobs:` is
  empty — an *invalid callable workflow*, not a `uses:@test` timing artifact. pc's check has **never
  run** (caller filename `api-contract.ym;`). Two further traps: pc+alto `requirements-test.txt` are
  empty (no pytest/fastapi; pc `conftest.py` needs `matplotlib`), and a module-level skip returns
  `pytest` exit 5 which the bare step reads as failure.
- **Fixes prepared as patches** (`*.gapfix.patch`, verified against the `test` HEADs, not pushed): hub
  reusable (drop tail + tolerate exit 5); pc caller `git mv` + `requirements-test.txt`; alto
  `requirements-test.txt`; llm-enrich `service/requirements.txt` `git mv`.

## Open items / next steps
1. **Apply the five gap-fix patches** (the hub reusable patch is what turns api-contract from 0 jobs
   to real jobs; the rest make each repo's job green).
2. **Re-push/rerun once landed** — torch-free services (alto/nlp/llm) assert; pc/translator skip
   cleanly (exit 5, tolerated). Confirm green per repo.
3. **Forward-merge `test` → default branches** once green (hub `main`; pc `vit`/`master`; tool repos'
   `master`/`main`) — the #31 §12.2 manual-sync item.
4. **Optional (out of #32 scope):** response-schema typing + shared `ErrorModel` for richer
   `/openapi.json`; fold nlp-enrich's own `_read_tool_version` onto the shared helper; route
   llm-enrich ALTO input through the TEITOK reader.

## Critical files
- Standard: `docs/agent_skill_strategy.md` §2.2/§4/§12.3 · tracker: `docs/plan_repo_review.md` §5.A.
- Helper (canonical): `docs/templates/shared/atrium_service.py` → each repo's `service/atrium_service.py`.
- Services: `{page-classification,translator,nlp-enrich}/service/api.py`,
  `alto-postprocess/service/text_api.py`, `llm-enrich/service/api.py`.
- Enforcement: `.github/workflows/{api-contract,para-drift}.reusable.yml` (hub) + `api-contract.yml`
  callers · per-repo `tests/test_api_contract.py`.
