# Strategy — Issue #66: Integration of developed tools into Galaxy

> Issue [\#66](https://github.com/ufal/atrium-project/issues/66) · Labels: `beginning`, `a big one`, `integration` ·
> Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover · **Status: ✅ Finish — pilot (AMČR, #67); 🟡 prepared —
> requirements published on GHP (Workflows pages), access and route not yet asked.** _Written 2026-09-24; rewritten
> 2026-09-25 (round 7) after the Galaxy research; refreshed 2026-09-26 (AMČR baseline). Evidence: [`../digests/66.digest.md`](../digests/66.digest.md)._

### 2026-09-26 — AMČR baseline ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ✅ finish.** Decided by motyc (AMČR) in #67, adopted by ÚFAL. The plan stands; two rules join it:
1. **Own tool only, link `0xSpVP`.** Each Galaxy workflow (C) annotates its own tool's steps and links the SSHOMP
   workflow record it pairs with; the full AMČR chain is described once, in `0xSpVP` (motyc on #4, 2026-09-26). No
   issue numbers, dates or versions in the annotations — the `<version>` belongs in the tool XML, not the prose.
2. **Translator defaults follow the pilot:** `replace` (AMČR 2.2 validates it, not `append`; translator#46). Whether
   the wrapper can use `ct2` with a staged model instead of LINDAT waits on the 30 September discussion (translator#4).

A's questions were posted on the issue on 2026-09-25 (K4TEL); the request to Mark A. Greenwood is still to send.

## Context
WP4 asked for the tools as Galaxy *actionable workflows*: a workflow described on the SSH Open Marketplace that can
also be run. The reviewer (Mark A. Greenwood, DARIAH) marked them green. The issue's first step is information: what
the wrap-up needs, published before any Galaxy record exists. The research of 2026-09-25 found two things that shape
everything after it:

1. **The pattern already exists in ATRIUM.** DARIAH's
   [`atrium-galaxy-tools`](https://github.com/DARIAH-ERIC/atrium-galaxy-tools) wraps other ATRIUM components as local
   Galaxy tools and pairs the T4.2.1 Galaxy workflow with its SSH Open Marketplace narrative `IrpmkB`.
2. **ssh.usegalaxy.eu is usegalaxy.eu** under another name. Getting a tool there means ToolShed publication and a PR to
   `usegalaxy-eu/usegalaxy-eu-tools`, which is a different route from DARIAH's repository.

So the first question for DARIAH is not "what does ssh.usegalaxy.eu need" but "which Galaxy, and which route".

## Status snapshot
| Item                                         | State             | Note                                                                          |
|----------------------------------------------|-------------------|-------------------------------------------------------------------------------|
| Reviewer sign-off for wrapping               | ✅ **GIVEN**       | per the issue                                                                 |
| Requirements published before Galaxy records | ✅ **DONE**        | Workflows pages, a Galaxy sheet per tool (#57 round 7)                        |
| Narratives to pair with Galaxy workflows     | 🟡 **PARTLY**     | pages written; SSHOMP workflow records `13eHAZ` (rebuild) and pc planned (#4) |
| Which server, which route                    | ❓ **UNKNOWN**     | DARIAH's Galaxy (PR to `atrium-galaxy-tools`) or usegalaxy.eu (ToolShed)      |
| Access for the three accounts                | ❌ **NOT ASKED**   | contact: Mark A. Greenwood                                                    |
| Container images                             | ✅ **EXIST**       | GHCR, all five tools; models not baked in                                     |
| Test data                                    | ✅ **EXISTS**      | pc `small_data_samples/`, translator `data_samples/`                          |
| First wrappers                               | ❌ **NOT STARTED** | translator, then page classifier                                              |

## Strategic actions

### A — Ask (user, one message to Mark A. Greenwood)
1. **Which Galaxy** is "ATRIUM's Galaxy space": DARIAH's own instance (the one `atrium-galaxy-tools` deploys to), or
   ssh.usegalaxy.eu?
2. **Which route** for ÚFAL's tools: a PR adding wrappers to `atrium-galaxy-tools` (`tools/` + `atrium_tools.xml`), or
   ToolShed publication and a `usegalaxy-eu-tools` PR — or both, DARIAH's first?
3. **Containers or conda.** Our tools ship as images on GHCR (`ghcr.io/ufal/atrium-<tool>:<version>`) and are not
   conda packages. Is `<container type="docker">` with a GHCR image acceptable on the target server?
4. **Network and models.** page-classification downloads model weights from the Hugging Face Hub; the translator
   calls LINDAT's translation and UDPipe services and downloads a FastText model. Do jobs have outbound network, or
   should models be staged (the `huggingface` data table)?
5. **GPU** for page classification on large collections and for the LLM stages later — available, and how requested?
6. **Workflow metadata.** Our `.ga` files will carry an annotation, creators with ORCIDs, a licence and labelled
   inputs and outputs, from the narratives. Does DARIAH want the same for theirs, or a shared convention?
7. **Access** for the three accounts named in the issue.

### B — Where the requirements live (done in round 7)
The planned `docs_site/operations/galaxy.md` is replaced by the **Galaxy sheet** in each workflow narrative and the
field table on the Workflows overview — one place per tool rather than a separate page that repeats them:
- [`workflows/translator.md`](../../docs_site/workflows/translator.md) and
  [`workflows/page-classification.md`](../../docs_site/workflows/page-classification.md): full sheets;
- the three other narratives: datatypes and closest Galaxy tools only (stable core);
- [`workflows/index.md`](../../docs_site/workflows/index.md): narrative § → SSHOMP field → `.ga` field → Workflow
  RO-Crate, and the T4.2.1 model.

### C — First two wrappers (after A)
| Tool                | Galaxy inputs                                                                | Parameters                                                             | Galaxy outputs                                                         | Container                                           | Compute and network                                                           | Test data                    |
|---------------------|------------------------------------------------------------------------------|------------------------------------------------------------------------|------------------------------------------------------------------------|-----------------------------------------------------|-------------------------------------------------------------------------------|------------------------------|
| translator          | collection of `alto` or `xml`; `txt` XPath list; `csv` vocabulary (optional) | target and source language; backend; replace or append; XSD (optional) | translated `alto`/`xml`; `tabular` log; `json` paradata; `json` record | `ghcr.io/ufal/atrium-translator:<version>`          | CPU; LINDAT + HF Hub, or ct2 with a staged model; LLM key via `<credentials>` | `data_samples/my_documents/` |
| page-classification | collection of `png`/`jpg`                                                    | revision; single model or ensemble; top-N                              | `tabular` Top-N; `tabular` all scores; `json` paradata; `json` record  | `ghcr.io/ufal/atrium-page-classification:<version>` | CPU (GPU for large collections); HF Hub or staged weights                     | `small_data_samples/`        |

Then a two-step Galaxy workflow per tool — the narrative's steps as step annotations — linked from `13eHAZ` and the
page-classifier workflow record (#4), and linking back to them, as `IrpmkB` and T4.2.1 do.

### D — The rest, in pipeline order, when each tool's workflow settles
alto-postprocess (`alto`/`json` → `tabular`, `txt`), nlp-enrich (`tabular` + `alto` → `tei`, CoNLL-U as `tabular`),
llm-enrich (`tabular`/`tei`/`markdown`/`pdf`/`docx` → `json`). Between steps the record passes as a `json` dataset
(`--document-json` / `--document-json-out`). Closest existing tools, which also bound what is worth wrapping:
Kraken/Tesseract upstream of alto-postprocess; `stanza_nlp` beside nlp-enrich; DARIAH's
`atrium_pdf_text_extraction` + `atrium_t4_1_2_ie` and `llm_hub` beside llm-enrich.

## Verification / done-criteria
- DARIAH's answer to A recorded on the issue; access granted to the three accounts.
- Translator and page-classifier tools pass `planemo lint` / `planemo test` with the repositories' sample data.
- Each has a Galaxy workflow with annotation, creators (ORCID), licence and labelled inputs/outputs, linked both ways
  with its SSH Open Marketplace workflow record.

## Out of scope (for now)
- Re-architecting the tools for Galaxy: the wrappers call the published images.
- Wrapping the three tools still in development before their workflows settle.
