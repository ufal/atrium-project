# Strategy — Issue #18: Docker compose + GH-action wrapper for CU forks

> Issue [#18](https://github.com/ufal/atrium-project/issues/18) · Labels: `enhancement`, `help wanted`, `development`, `a big one`
> **Status: ✅ original scope delivered · ▶️ refinement & expansion roadmap proposed (2026-08-04)**

## Context

The issue's original scope is done. Seven hub reusable workflows serve **38 caller jobs** (35 cross-repo +
3 hub-local), all resolving to `@v1`; GHCR publishes provenance-stamped images; the E2E threads a real
document JSON through all five stages; every tag-gated guard has fired on a real tag. Only the two
external items in [#40](https://github.com/ufal/atrium-project/issues/40) remain from the original
done-criteria.

A full audit of all six working trees plus live GitHub state on 2026-08-04 established what comes next.
The full findings register and wave sequencing are in **[`docs/docker_gha_roadmap.md`](../../docs/docker_gha_roadmap.md)**;
current state is in **[`docs/docker_gha.md`](../../docs/docker_gha.md)**. This file is the tracking summary.

## Status snapshot — original scope

| Item                         | State            | Note                                                       |
|------------------------------|------------------|------------------------------------------------------------|
| Reusable workflows + callers | ✅ **DONE**       | 7 reusables; **38** caller jobs across 6 repos             |
| E2E all five stages          | ✅ **VERIFIED**   | Threads document JSON through all 5 published images       |
| Release path                 | ✅ **PROVEN**     | Releases cut (`v1.7.2-beta`, `v1.4.1-beta`, …)             |
| Release bundle integrity     | ⚠️ **PARTIAL**   | Closure guard exists in **1 of 5** repos (→ W4)            |
| `secrets: inherit`           | ✅ **ELIMINATED** | Zero in callers and templates; linter parses for it        |
| `@v1` pin migration          | ✅ **COMPLETE**   | All 35 cross-repo refs; see roadmap §3.4 on the trade-off  |
| Hub linter + tests wired     | ⚠️ **PARTIAL**   | Runs everywhere, but has **no tests of its own** (→ W1)    |
| `action-gh-release` SHA pins | ✅ **DONE**       | Verified against resolved annotated tags                   |
| Trivy release gate           | ✅ **LIVE**       | Fails a tag on **fixable CRITICAL**; HIGH undecided (→ W5) |
| Branch protection            | ⬜ **OPEN**       | **Zero of ~20 branches protected, in any repo** (#40)      |
| GPU self-hosted runner       | 🚧 **BLOCKED**   | W4 removes the single-supplier dependency                  |

## Refinement & expansion waves (proposed 2026-08-04)

| Wave   | Theme                        | Blast radius       | Headline                                                                                                                                                                                                               |
|--------|------------------------------|--------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **W1** | Make the guardrails real     | hub only           | The linter has no rule for `timeout-minutes`, `concurrency`, workflow-level `permissions` or the action floor — and `hub-self-check` runs the linter **from `@v1`**, so a linter change is never tested by its own PR. |
| **W2** | Live breakages               | 5 tool repos       | Two API images cannot start (`uvicorn` absent); 6 compose image refs unpullable; paradata names an unpublished tag; the E2E's secret-less path can only fail.                                                          |
| **W3** | `:edge` image channel        | hub + 5 callers    | Images publish only on `v*`, so CI can only ever test the last release, never HEAD.                                                                                                                                    |
| **W4** | Finish the DRY collapse      | hub + 5 tool repos | ~1,020 lines of `release`/`scheduled-smoke`/`gpu-inference`/`shellcheck` with no reusable; already drifted into 3 nightly semantics.                                                                                   |
| **W5** | Supply chain & image hygiene | 5 tool repos       | No lockfiles or hash pinning; 2 floating VCS refs; a ~1.2 GB unpinned build-time weight fetch; test+research deps in production images.                                                                                |
| **W6** | Convergence & new targets    | 5 repos + hub      | No `HEALTHCHECK` anywhere, though "reachable via API" is this issue's requirement; plan the `digital` target before the PDF/DOCX converter lands.                                                                      |

**Sequencing rationale:** guardrails before refactor. The 2026-07-30 round landed timeouts on 45 jobs and
concurrency on 33 workflows; two days later `all-repos-smoke.yml` shipped with neither, plus no
`permissions:` block, and CI stayed green — because no rule checks any of them. Collapsing another ~1,000
lines before the linter can hold the result repeats that at five times the blast radius.

## Decisions settled 2026-08-04

1. **Documentation-only round** — findings need owner review before five-repo diffs land.
2. **All four expansion themes in scope** — they are interdependent: `:edge` is what lets the collapsed
   workflows be validated without cutting a release.
3. **Retire `test`; protect the default branch** — `test` is byte-identical to default in all six repos, so
   it doubles CI spend per push and protects nothing.
4. **GPU lane becomes runner-agnostic**, and `slow` splits into `slow` (network, runnable on
   `ubuntu-latest` today) + `gpu` (needs CUDA) — so three currently-vacuous nightly lanes become
   substantive with no runner at all.

## Done-criteria — original issue

- ✅ Nightly E2E exercises all five stages and threads real JSON data.
- ✅ No caller passes `secrets: inherit`.
- ✅ The hub's linter and tests run in CI across all tool repos.
- ✅ Every action holding write scope resolves to an immutable SHA on the default branch.
- ✅ `test` merged to default in all six repos.
- ✅ The Trivy release gate fires on a real tag.
- ✅ All caller refs resolve to `@v1`.
- ⬜ Branch protection configured (#40).
- ⬜ `gpu`-marked tests execute on a real schedule (#40; W4 unblocks the `slow` half now).

## Carried forward — new, from the 2026-08-04 audit

1. **W1 is the gate on everything else.** Linter tests, the self-validation hole, and the missing rules.
2. **Two API images cannot start** — the highest user-visible defect currently in the ecosystem.
3. **`alto` owns the only real GPU test and has no GPU workflow**; `nlp` and `llm` have GPU workflows that
   collect zero tests.
4. **`docs/templates/workflows/update_issues.sh`** exports an elided placeholder token that kills its own
   guard and overrides the caller's environment. Not a usable credential, but confirm GitHub-native secret
   scanning is enabled on all six repos.
