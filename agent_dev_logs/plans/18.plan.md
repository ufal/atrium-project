# Strategy — Issue #18: Docker compose + GH-action wrapper for CU forks

> Issue [#18](https://github.com/ufal/atrium-project/issues/18) · Labels: `enhancement`, `help wanted`, `development`, `a big one` · **Status: 🔍 Tocheck**

## Context
The DRY CI/CD wrapper landed in commit `676a1fe`: a reusable `docker-tool.reusable.yml`
(`on: workflow_call`, the single source of truth), a caller example, shared `.coveragerc` /
`ruff.toml`, a `ci-cd-strategy.md`, and a Dependabot appendix. All four repos pass GH Actions
and the docs are ready for **rharasim** to test. The strategy was settled with motyc: no
overall runtime orchestrator is needed — the GHCR-published, self-identifying containers
(`atrium_paradata.py` already reads `ATRIUM_RUNNER_IMAGE/REPO/REF`) are reachable via API, so
this issue scopes down to **packaging + CI**, with cross-service orchestration handled
separately in `docs/plan_repo_review.md` §8.

The enumerated shared-template **bug list is now fixed on HEAD** (see snapshot); what's left is
verifying the translator pilot callers, the rharasim end-to-end run, and the proposed
pipeline-wide smoke test.

## Status snapshot
| Item                                       | State       | Note                                                                            |
|--------------------------------------------|-------------|---------------------------------------------------------------------------------|
| Reusable workflow + callers (`676a1fe`)    | ✅ **DONE**  | DRY single-source-of-truth; 4 repos green                                       |
| Strategy settled (no runtime orchestrator) | ✅ **DONE**  | motyc; containers reachable via API                                             |
| Codecov upload condition bug               | ✅ **FIXED** | `CODECOV_TOKEN` written to job `env:` — `docker-tool.reusable.yml:37-40` (cites #18) |
| `@main` vs `@test` caller pin drift        | ✅ **SUPERSEDED** | callers renamed `*.caller.example.yml` (`4340a21`); `secret-scan.caller.example.yml` replaces `security.caller.example.yml` |
| Action version floor                       | ✅ **DONE**  | checkout@v7 / setup-python@v6 / codeql-action@v4 / trivy@v0.36.0 / gitleaks@v3 (2026-07-12 bump) |
| `para-drift` rename + license parity       | ✅ **DONE**  | `para-drift.reusable.yml` diffs `para_licenses.py` + test vs `docs/templates/shared/` by default (2026-07-12) |
| translator caller P0/P1 fixes              | 🔍 **VERIFY** | `branches:[main]`, `image-name` vs `image-ref`, bool→string `pre-install-torch` — re-check against the translator repo's current callers |
| End-to-end pipeline smoke test             | 📝 **PROPOSED** | 2026-06-26 comment — no issue tracks pipeline-wide regression yet               |

---

## Strategic actions (fix-before-rollout)

### A — Shared-template bugs — ✅ all done on HEAD (2026-06-28 → 2026-07-12)
1. ~~**Codecov never uploads.**~~ Fixed by writing `CODECOV_TOKEN` into the job `env:`
   (`docker-tool.reusable.yml:37-40`, comment cites #18) — the `env.`-based gate now works.
2. ~~**Ref-pin drift.**~~ Superseded: caller templates renamed to the `.caller.example.yml`
   convention (`4340a21`); the `@main`-pinned `security.caller.example.yml` was replaced by
   `secret-scan.caller.example.yml` (gitleaks-action@v3 direct).
3. ~~**Action version floor.**~~ Bumped 2026-07-12: checkout@v7, setup-python@v6,
   codeql-action@v4, gitleaks-action@v3, trivy-action@v0.36.0, Node24 baseline.

### B — translator reference-caller fixes (it is the pilot template)
| Pri    | Fix                                                                                                                                                         |
|--------|-------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **P0** | Triggers `branches:[main]` → `branches:[test, master]` (real branches; workflows currently never fire on real activity).                                    |
| **P0** | `security.yml` passes `image-name`; the reusable only declares `image-ref` → call fails validation. Use `image-ref: ghcr.io/ufal/atrium-translator:latest`. |
| **P1** | `docker.yml` passes `pre-install-torch: true` (bool) into a `type: string` input → `pip install true`. Quote it.                                            |

### C — Hand-off to rharasim + orchestration follow-through
- Once A+B are green in the **translator** pilot, propagate the same caller shape to pc / alto
  / nlp and have rharasim validate end-to-end.
- Feed the compose work into `docs/plan_repo_review.md` §8: the
  `compose/docker-compose.pipeline.yml` chaining **pc → alto → nlp → translator** over the
  shared `atrium_data` volume (`depends_on: service_completed_successfully`) plus
  `compose/README.md` (the `/data` stage contract, image tags, GPU override).
- Phase-3 follow-on (review §7): the drift-check — landed as **`para-drift.reusable.yml`**
  (renamed 2026-07-12, license-parity steps included by default) — and ratcheting ruff to
  **blocking** in all four repos (safe — all at 0 findings).

## Verification / done-criteria
- All four callers fire on `test`/`master`, pass the reusable's input validation, and upload
  coverage. gitleaks on v3. The translator pilot is bit-clean, then propagated.
- rharasim confirms the wrapper on a real run; the compose scaffold chains the four images.

## Out of scope (other threads)
- The runtime orchestrator (explicitly *not* needed — containers reached via API).
- Secret-scan sign-off + GPU self-hosted runners (review §7 Phase 4, blocked on policy).

---

## 🚨 Latest Comment & Immediate Proposed Addition

As of **June 26, 2026**, the major gap identified is that no issue currently tracks cross-service pipeline regression.

* **End-to-End Pipeline Integration Smoke Test:** The plan proposes introducing a minimal test fixture running
a single-page document through all four pipeline stages (`ALTO → postprocess → translate → enrich → TEITOK`). 
This CI fixture is critical to catch cross-repo interface breakages early, especially given the ecosystem's 
`@test`-pinned reusable architecture.

---

## 🛠️ Update Plan: Cross-Cutting Shared Template Bugs — ✅ RESOLVED (2026-07-12)

All three template bugs are fixed on HEAD (see Status snapshot / Strategic actions A):
the Codecov token is written to job `env:`, the caller templates were standardized under the
`.caller.example.yml` naming, and the action versions were bumped to the Node 24 baseline
(checkout@v7, setup-python@v6, codeql-action@v4, gitleaks-action@v3, trivy@v0.36.0). The
2026-07-12 pass also renamed `paradata-drift` → **`para-drift.reusable.yml`** (license test step
included by default) and refreshed `docs/docker_gha.md`.

---

## 📦 Repository-Specific Actions & Priorities

### 1. atrium-translator (Pilot Template Reference)

* **P0 Trigger Fix:** Update workflows from `branches: [main]` to `branches: [test, master]` so they actually fire on 
legitimate activity.

* **P0 Security Configuration:** Resolve an input validation failure in `security.yml` by correcting the undeclared 
`image-name` input to `image-ref: ghcr.io/ufal/atrium-translator:latest`.

* **P1 Torch Dependency Cleanup:** Remove the accidental `pre-install-torch: true` string-coercion parameter since the 
translator has zero PyTorch requirements.


### 2. atrium-page-classification

* **P0 Secret Management:** Enforce strict secret scanning and push protection to completely scrub legacy token history 
found in historical configuration blocks.

* **P1 Hardware & Infrastructure Gate:** Configure GPU inference workflows once the self-hosted runner infrastructure
is ready.

* **P1 Dependency Realignment:** Align and verify the current discrepancy between `setup/requirements.txt` and 
`service/requirements.txt` regarding PyTorch versions.

  
### 3. atrium-nlp-enrich

* **P1 Upstream Stability Monitoring:** Deploy scheduled smoke tests to aggressively check the LINDAT API endpoint paths,
which represent the highest point of upstream fragility across the project.

* **P1 CodeQL Analysis:** Implement CodeQL scanning across its FastAPI endpoints to monitor subprocess-spawning command
injection risks.



### 4. atrium-alto-postprocess

* **P1 Heavyweight Vulnerability Audits:** Implement comprehensive `Trivy` container scanning due to the heavy 
combination of PyTorch, LayoutReader, and fastText binaries.

* **P1 Reachability Checks:** Set up automated network health monitors checking external dependencies like
`FreeOCR-AI/layoutreader` and Hugging Face model pull stability.

---

## 🔄 Next Steps & Orchestration Rollout

1. Finalize the `version-check.yml`, `codeql.yml`, and `pre-commit` updates across the lighter repositories to gain immediate quality signals.

2. Propagate the verified reference caller configurations to all repositories once the `atrium-translator` pilot finishes cleanly.

3. Feed completed Docker Compose updates into `docs/plan_repo_review.md` §8 to anchor the multi-stage pipeline 
orchestration mapping over the shared `atrium_data` volume.