# Strategy — Issue #18: Docker compose + GH-action wrapper for CU forks

> Issue [#18](https://github.com/ufal/atrium-project/issues/18) · Labels: `enhancement`, `help wanted`, `development`, `a big one` · **Status: ✅ T1–T4, T6–T7 closed · T8 is the last code item · no P0s**
> _Refreshed 2026-07-31 (third pass) against the live remotes. T7 closed: the hub's policy linter and its 52 tests now run in CI, in the hub and in all five tool repos, and the linter independently flags T8 in every repo._

## Context

Every tag-gated guard in the ecosystem has now fired on a real tag, which was the last substantive
question this issue held open. Two releases proved the path end to end: `atrium-translator` `v0.10.1`
exercised the version gate, the digest scan and the SARIF upload, and `atrium-page-classification`
`v1.7.1-beta` fired the **bundle closure guard** — the one guard that had never run, covering a
defect that actually shipped.

The `@v1` migration is also complete. The five `agent-skill` callers, missed by the original 26-ref
pass because it only walked default branches, are now on `@v1`. That gap was not academic: it is
exactly how a `test`-branch change reached and broke them (T6).

T7 has since closed. `workflow-lint.reusable.yml` and `hub-self-check.yml` landed, five thin callers
went into the tool repos, and all of it is green in CI — verified from step lists, not conclusions.
The linter runs everywhere now, and it earned its keep on the first pass: pointed at the five tool
repos' default branches it flags the mutable `action-gh-release@v3` on its own.

**That is the one code-level item left (T8).** The pins are committed on `test`, not on any default
branch. It is easy to believe it is already done — which is exactly why it leads the remaining
work.

## Where things actually are

Verified against the remotes. Stale local refs have misled this work more than once, so this table is
re-derived rather than remembered.

| Repo                         | default              | `test` vs default | `agent-skill` |
|------------------------------|----------------------|-------------------|---------------|
| `atrium-project`             | `main` @ `1fc652f`   | in sync           | —             |
| `atrium-alto-postprocess`    | `master` @ `b55e85d` | **2 ahead**       | `8dbaf16`     |
| `atrium-llm-enrich`          | `main` @ `88bc060`   | **2 ahead**       | `815dcbb`     |
| `atrium-nlp-enrich`          | `master` @ `73f47fb` | **2 ahead**       | `9f48128`     |
| `atrium-translator`          | `master` @ `32a15cf` | **2 ahead**       | `6176730`     |
| `atrium-page-classification` | `vit` @ `e5f65ff`    | **2 ahead**       | `0e45201`     |

`v1` → `1fc652f`, which is also `main` and `test`: hub tag and default branch are the same tree
again. The two commits on each tool repo's `test` are the `action-gh-release` SHA pin and the new
workflow-lint caller — they must merge together, since the linter flags the pin's absence.

## Status snapshot

| Item                                       | State                  | Note                                                             |
|--------------------------------------------|------------------------|------------------------------------------------------------------|
| Reusable workflows + callers               | ✅ **DONE**             | 7 reusables; **42** caller jobs across 6 repos                   |
| Strategy settled (no runtime orchestrator) | ✅ **DONE**             | motyc; containers reachable via API                              |
| E2E all five stages                        | ✅ **VERIFIED NIGHTLY** | run `30515547822`; stage 5 executes; ≈4 min                      |
| `docker.yml` permissions                   | ✅ **FIXED**            | Wave B regression; green in all 5                                |
| Release path                               | ✅ **PROVEN ×2**        | translator `v0.10.1`, page-classification `v1.7.1-beta` — T1     |
| Release bundle integrity                   | ✅ **GUARD FIRED**      | run `30602523745`: 15 modules, 10 imports resolved — T1          |
| `secrets: inherit`                         | ✅ **ELIMINATED**       | zero in callers **and** templates                                |
| Container-scan race                        | ✅ **FIXED**            | post-publish scan by digest inside `build-and-push`              |
| Lint policy                                | ✅ **UNIFIED**          | blocking in all 5                                                |
| version-guard duplication                  | ✅ **CONSOLIDATED**     | one `check_version.py`, md5 `d36004d0` in all 6 copies           |
| Dependabot                                 | ✅ **ALIGNED**          | hub covered; ecosystems grouped                                  |
| **`@v1` pin migration**                    | ✅ **COMPLETE**         | 42 caller jobs; `agent-skill` migrated 2026-07-31 — T2           |
| codeql/pre-commit consolidation            | ✅ **VERIFIED IN CI**   | green on `vit` and `master` — T3                                 |
| Trivy blocking severity                    | ✅ **SETTLED**          | tag-gated CRITICAL+fixable gate — **unfired**, needs a tag — T4  |
| Caller-example templates                   | ✅ **CORRECTED**        | docker example had reproduced the Wave B failure — T6            |
| `skill-validate` clobber                   | ✅ **RESTORED**         | verified at `v1`; all 5 `agent-skill` callers match — T6         |
| Hub self-lint (ruff/CodeQL)                | ✅ **LIVE + GREEN**     | `main` @ `1fc652f` — T7                                          |
| **Hub linter + tests wired**               | ✅ **LIVE + GREEN**     | runs `30617892015` / `30618019962`; 5 tool-repo callers too — T7 |
| **`action-gh-release` pins live**          | ⬜ **OPEN**             | on `test` only; defaults on `@v3`; **linter flags it** — T8      |
| `test` → default (5 tool repos)            | ⬜ **OPEN**             | 1 commit each; same commit as T8                                 |
| Branch protection (hub)                    | ⬜ **OPEN**             | repo-settings action on `main`/`test`                            |
| GPU self-hosted runner                     | 🚧 **BLOCKED**         | rharasim — gates every `slow` test and the E2E live bridge       |
| Secret scanning (gitleaks)                 | ✅ **DROPPED**          | GitHub-native scanning + push protection covers it               |

---

## T1 — Release path — ✅ PROVEN TWICE

Verified from step lists rather than run conclusions, because a green run can still skip everything.

**`atrium-translator` v0.10.1** (run [`30561449166`](https://github.com/ufal/atrium-translator/actions/runs/30561449166)):
version gate passed, `build-and-push` succeeded under the new `security-events` grant, Trivy digest
scan 15s, SARIF upload 7s, GHCR index carries an `attestation-manifest`. The SARIF upload is the
notable one — the exact permission chain whose absence caused the Wave B `startup_failure`.

**`atrium-page-classification` v1.7.1-beta** (run [`30602523745`](https://github.com/ufal/atrium-page-classification/actions/runs/30602523745)):
`Verify bundle is self-contained` succeeded, logging `OK — bundle is self-contained (15 modules, 10
first-party imports resolved)` — identical to the local replay. That closed the last unexercised
guard, and it is the one covering a defect that actually shipped (`ImportError` on first import).

**Remaining:** the negative test — tag once with a deliberately mismatched `CITATION.cff` and confirm
`version-guard` blocks. It would have caught `v1.0.0.-beta` and `v1.16.2`. Proven locally against
every failure mode, including the CITATION/para_config mismatch; never fired on a real tag. The 16
unit tests in T7 cover the *logic* permanently; a real tag would additionally prove the *wiring*.

## T2 — `@v1` pin migration — ✅ COMPLETE

**42 caller jobs** across six repos resolve `@v1`; zero `@test` pins remain anywhere, including the
five `agent-skill` branches migrated 2026-07-31.

Those five were the last gap and they matter more than their count suggests. The original 26-ref
migration walked default branches only, so `agent-skill` kept a `@test` pin — and a `test`-branch
change is precisely what silently broke them (T6). The lesson generalises: **a pin migration must
enumerate branches, not repos.**

`para-drift.reusable.yml` takes a `hub-ref` input (default `v1`) so `@test` callers stay
self-consistent — otherwise the pin would be half-done, taking workflow *logic* from the immutable
tag while the *files it enforces* still came from a mutable branch.

**Two channels**, documented in `docs/docker_gha.md` §1: `@v1` is what every caller pins (a moving
major tag only a maintainer advances); `@test` validates a hub change before `v1` moves.

## T3 — codeql/pre-commit reusables — ✅ DONE, verified in CI

431 lines across ten files → 300. Differences were an arbitrary weekly cron and a cache present in
2 of 5; crons are staggered, the cache is universal, and `pre-commit/action@v3.0.1` — the last
patch-pinned third-party dependency — is gone. Green on page-classification `vit` (`90deffc`) and
alto-postprocess `master` (`b55e85dc`).

Fixed in passing: page-classification's `codeql`/`pre-commit` triggered on `[test, master]` while its
other four workflows covered `[vit, test, clip, master]` — so static analysis and lint were not
running on its own default branch.

## T4 — Supply chain + Trivy policy — ✅ DECIDED

**SHA pins.** The precedent is in this repo's own history: `security.reusable.yml` documented that
`aquasecurity/trivy-action` tags ≤ 0.34.2 were force-pushed to credential-stealing malware on
2026-03-19 (GHSA-69fq-xp46-6x23, 76 of 77 tags) and recommended pinning to the release commit. Live on
`main`: `docker/build-push-action` ×3, `trivy-action` ×2, `peter-evans/create-pull-request` ×1.
`softprops/action-gh-release` ×5 is committed but **not yet on any default branch** — see T8.

Read-only and upload-only actions (`setup-buildx`, `metadata`, `login`, `codecov`) stay on tags
deliberately. Dependabot maintains SHA pins and their trailing `# v3` comments, so this hardens
without freezing.

**Trivy `exit-code`.** Settled as a *separate* gate rather than by flipping the existing step, which
keeps two concerns apart: the SARIF step still runs on every build and never blocks, so the full
CRITICAL+HIGH picture always reaches code scanning; the new step blocks. It sits **after** the SARIF
upload deliberately — if it fails, the findings are already in the Security tab.

| Narrowing                                  | Effect                                                  |
|--------------------------------------------|---------------------------------------------------------|
| `if: startsWith(github.ref, 'refs/tags/')` | pushes and PRs untouched; it stops a *release*          |
| `severity: CRITICAL`                       | HIGH stays report-only — too noisy to hold a release on |
| `ignore-unfixed: true`                     | an unpatchable CVE cannot wedge releases indefinitely   |

The weekly `security.reusable.yml` scan stays report-only. **Caveat:** the code scanning alerts were
not readable from the session that made this change, so the design fails safe rather than deriving
from a triaged list, and it cannot fire without a tag. Reverting is one value.

## T6 — Templates and the `skill-validate` clobber — ✅ RESOLVED

**Caller-example templates.** `codeql.caller.example.yml` was a byte-identical copy of the docker
example. A sweep then found worse in `docker.caller.example.yml`: an *explicit* permissions block of
only `{contents: read, packages: write}` while `docker-tool.reusable.yml` requests
`security-events: write` — so copying it reproduced the Wave B `startup_failure` verbatim. Fixed,
along with a leftover `secrets: inherit` and a missing tag-exempt `concurrency` block.

The distinction to keep: an **explicit** block omitting a scope fails at parse time; **no** block at
all inherits the repo default and is fine.

**The clobber.** Commit `39a9edd` applied one round's `security.reusable.yml` edits into
`skill-validate.reusable.yml`, destroying 348 lines and leaving `security.reusable.yml` unpinned. All
five `agent-skill` callers pass `client-script`; the replacement declared different inputs, so they
were **already broken** — latently, since nobody had pushed to those branches. Restored from
`7fa727b`; verified at `v1` and against all five callers.

## T7 — Hub self-coverage — ✅ DONE (2026-07-31)

The hub is the source of truth for 42 references and six vendored files and had **no caller workflows
at all**. Nothing in CI parsed, linted or policy-checked it — which is why two clobbers and a missed
pin were each caught only by a human reading files.

**All of it now runs**, verified from step lists rather than run conclusions:

| What                                  | Evidence                                                                                                               |
|---------------------------------------|------------------------------------------------------------------------------------------------------------------------|
| `Hub Self-Check` on `main`            | run `30617892015` — `Check workflows and callers` ✅, `Run release-gate tests` ✅, `Run canonical shared-module tests` ✅ |
| same on `test`                        | run `30617882578` ✅                                                                                                    |
| `Workflow Policy Lint` in a tool repo | atrium-translator run `30618019962`, resolving `workflow-lint.reusable.yml@v1` → `1fc652f` ✅                           |
| `codeql.yml` / `pre-commit.yml`       | green on `main`, calling the hub's reusables by local `./` path                                                        |

The linter exits 1 on any finding, so a succeeded step is a real assertion rather than a skipped one.

**Design: a reusable, not a vendored copy.** `workflow-lint.reusable.yml` checks out both the
caller's tree and the hub, mirroring `para-drift.reusable.yml`. Six copies of the script would need a
`para-drift` parity step to stop them drifting, and adding such a step for a file the repos do not
have yet fails `para-drift` in all five on their next push. One copy, no drift, no parity step.

**The capability that unlocked.** The caller/callee permission check could only resolve a callee from
the repo being linted. In a tool repo the callee lives in `atrium-project`, so it resolved nothing and
the check silently passed — running the script against `atrium-translator` reported *"0 caller/callee
permission pairs"*, which reads as clean but is really an unasked question. With `--hub-root`, each
tool repo gets **13 real permission assertions** plus its inputs checked. That is the check that
catches the Wave B `startup_failure` class, and it had no coverage outside the hub. The same resolver
handles `./`-form calls, so the hub's own callers are checked at last (hub pairs: 12 → 17).

**Tests run too** — including `docs/templates/shared/test_para_licenses.py`, 36 passing tests that had
sat in this repo invoked by nothing, predating #18 entirely.

The linter's six checks, each matching a defect this issue hit: every workflow and template parses ·
write-scoped actions SHA-pinned with a version comment **whose tag really resolves to that SHA** ·
caller/callee permissions agree (explicit under-grant fails, no block inherits) · no structural
`secrets: inherit` · no duplicate `name:` within a directory · no caller passing an input its callee
does not declare. The last two are the clobber detectors; both real incidents are caught when
replayed. Every check was proven able to fail on its own defect, and seven mutations of
`check_version.py` are each detected by the tests.

**It found T8 on its own** — pointed at all five default branches, it flags the mutable
`action-gh-release@v3` without being told to look for it.

## T8 — Land the `action-gh-release` pins — ⬜ OPEN

Each tool repo's `test` is exactly one commit ahead of its default branch, and that commit is the
`softprops/action-gh-release` SHA pin. Until it merges, all five default branches resolve that
action — which runs with `contents: write` to create releases and upload bundles — through the
mutable `@v3` tag.

Five fast-forwards. This also closes the `test` → default gap, which matters independently because
**`schedule` fires only from the default branch**, so anything schedule-driven stays dormant until it
merges.

## T5 — GPU runner (external, rharasim)

Until a `[self-hosted, gpu]` runner is registered, no `slow`-marked test executes anywhere and the
E2E `alto → nlp` boundary keeps using the committed fixture CSV instead of a live
`--start-from classify` run. Crons are removed so the ecosystem stops advertising coverage it lacks;
restore instructions are inline in each `gpu-inference.yml`.

## Done-criteria for the issue

- ✅ Nightly E2E exercises all five stages and fails loudly when it cannot.
- ✅ No workflow targets a branch that does not exist; PR checks match push checks.
- ✅ No caller passes `secrets: inherit` — nor any doc template.
- ✅ A release has been cut through the new path, twice, with every guard proven to execute (T1).
- ✅ All caller refs resolve to `@v1` — 42 caller jobs including `agent-skill` (T2).
- ✅ The hub's linter and tests run in CI, and in all five tool repos (T7).
- ⬜ Every action holding write scope resolves to an immutable SHA **on the default branch** (T8) —
  now continuously checked rather than remembered.
- ⬜ `test` is merged to default in all six repos (T8).
- ⬜ The Trivy release gate fires on a real tag (T4).
- ⬜ The version gate is proven to block a mismatch on a real tag (T1).
- ⬜ `slow`-marked tests execute somewhere on a real schedule (T5).

## Out of scope (other threads)

- The runtime orchestrator — explicitly *not* needed; containers reached via API (motyc).
- Compose-level pipeline chaining → `docs/plan_repo_review.md` §8.
- GPU runner provisioning itself → infra/rharasim; tracked here only as a blocking dependency.
- Vendoring `workflow_lint.py` / `test_check_version.py` into the tool repos. The linter does work
  there — run against `atrium-translator` it verifies 8 files and its `action-gh-release` pin — so it
  is worth considering later. It must be two-phase: files first, `para-drift` parity steps second,
  or `para-drift` fails in all five repos on their next push.
