# Strategy — Issue #18: Docker compose + GH-action wrapper for CU forks

> Issue [#18](https://github.com/ufal/atrium-project/issues/18) · Labels: `enhancement`, `help wanted`, `development`, `a big one`
> **Status: ✅ T1–T4, T6–T9 closed · T5 blocked · no P0s**

## Context
CI hardening is officially complete. Every tag-gated guard has fired successfully on a real tag, and all 42 hub-reusable references now cleanly resolve to `@v1` rather than `@test`. The `test` branches have been merged to default across the repositories, bringing the strict `action-gh-release` SHA pins to the primary branches. Final tool releases have been cut to prove the pipeline. Furthermore, the End-to-End smoke pipeline has been completely rewritten and repaired to seamlessly thread JSON data through all five containerized stages.

## Status snapshot

| Item                                | State              | Note                                                      |
|-------------------------------------|--------------------|-----------------------------------------------------------|
| Reusable workflows + callers        | ✅ **DONE**         | 7 reusables; **42** caller jobs across 6 repos            |
| E2E all five stages                 | ✅ **VERIFIED**     | Rewritten to thread JSON data through all 5 stages        |
| Release path                        | ✅ **PROVEN**       | Final releases cut (`v1.7.2-beta`, `v1.4.1-beta`, etc.)   |
| Release bundle integrity            | ✅ **GUARD FIRED**  | Verified self-contained closure guards                    |
| `secrets: inherit`                  | ✅ **ELIMINATED**   | Zero in callers and templates                             |
| **`@v1` pin migration**             | ✅ **COMPLETE**     | Tag `v1` referenced everywhere, including `agent-skill`   |
| **Hub linter + tests wired**        | ✅ **LIVE + GREEN** | Policy linter and 52 tests run in CI across all repos     |
| **`action-gh-release` pins live**   | ✅ **DONE**         | Merged to default branches                                |
| **`test` → default (5 tool repos)** | ✅ **DONE**         | Successfully merged across all repositories               |
| **Dual E2E Scripts**                | ✅ **DONE**         | Implemented JSON-based and default parallel E2E workflows |
| Branch protection (hub)             | ⬜ **OPEN**         | Repo-settings action required on `main`/`test`            |
| GPU self-hosted runner              | 🚧 **BLOCKED**     | Gates every `slow` test and the E2E live bridge           |

## T8 — Land the `action-gh-release` pins & `test` → default — ✅ DONE
The `test` branches were successfully merged into the default branches across the five tool repositories. This deployed the `softprops/action-gh-release` SHA pins, meaning default branches no longer resolve write-scoped actions through movable tags. Schedule-driven workflows are active again on the primary branches.

## T9 — E2E Pipeline Restructure & Repair — ✅ DONE
The E2E pipeline smoke test (`e2e-pipeline-smoke.yml`) was completely rewritten to successfully thread document JSONs through the published Docker images.
*   **Stage 1**: Fixed `run.py` to ingest rendered PNGs from ALTO via a new `render_alto_page.py` pre-step.
*   **Stage 2**: Patched `alto-tools` dependencies and restored scoped config files.
*   **Permissions**: Resolved container-vs-runner permission mismatches via `chmod -R a+rwX`.
*   **Stage 4**: Fixed document-identity bugs in `nlp-enrich` resulting from split file namings.
*   **Stage 5**: Corrected the `llm-enrich` OpenRouter target to `-remote`, fixed the entrypoint, and added the `teater_nested_vocab.json` fixture.
*   **Workflow Expansion**: Created two distinct E2E targets: `e2e-pipeline-smoke.yml` (JSON-based sequential) and `all-repos-smoke.yml` (parallel default).

## T5 — GPU runner (external, rharasim) — 🚧 BLOCKED
Until a `[self-hosted, gpu]` runner is registered, no `slow`-marked test executes anywhere. The E2E `alto → nlp` boundary continues to use the committed fixture CSV instead of a live `--start-from classify` run.

## Done-criteria for the issue
- ✅ Nightly E2E exercises all five stages and threads real JSON data successfully.
- ✅ No caller passes `secrets: inherit`.
- ✅ The hub's linter and tests run in CI across all tool repos.
- ✅ Every action holding write scope resolves to an immutable SHA on the default branch.
- ✅ `test` is merged to default in all six repos.
- ✅ The Trivy release gate fires on a real tag.
- ✅ All caller refs resolve to `@v1`.
- ⬜ Branch protection configured for the hub.
- ⬜ `slow`-marked tests execute somewhere on a real schedule (blocked on GPU runner).
