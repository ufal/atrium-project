# Strategy — Issue #18: Docker compose + GH-action wrapper for CU forks

> Issue [#18](https://github.com/ufal/atrium-project/issues/18) · Labels: `enhancement`, `help wanted`, `development`, `a big one` · **Status: ✅ Release path proven · pins on `@v1`**
> _Refreshed 2026-07-30 after the v0.10.1 release exercised every tag-gated guard and all 26 pins moved to `@v1`. No P0s open._

## Context

Everything the 2026-07-29 audit produced is now **merged to the default branch in all six repos** and
CI is green across the board. Both P0s from the previous revision are closed, with evidence:

- The nightly E2E ran **all five stages for real** — run
  [`30515547822`](https://github.com/ufal/atrium-project/actions/runs/30515547822) on `main`,
  05:03→05:07. `Enrich the fixture lines (OpenRouter)` executed successfully. Stage 5 had been dark
  for weeks while the run reported green.
- `test` == default in all six repos, so `schedule` (which fires only from the default branch) now
  reaches the GPU cron removals, the smoke failure-reporting, and everything else that was dormant.
- The Wave B `security-events` regression is fixed; Docker Build & Publish is green in all five.

**Both of those have now closed.** The `v0.10.1` translator release exercised every tag-gated guard
for real (T1), and all 26 reusable pins moved off the mutable `@test` branch onto the `@v1` major tag
(T2). What is left is genuinely small: one more release to exercise page-classification's bundle
guard, the codeql/pre-commit consolidation that T2 just unblocked, branch protection as a settings
action, two policy calls, and the GPU runner that has been external throughout.

## Status snapshot

| Item                                       | State                  | Note                                                                                             |
|--------------------------------------------|------------------------|--------------------------------------------------------------------------------------------------|
| Reusable workflows + callers               | ✅ **DONE**             | 5 reusables, 20 caller refs, merged to default in all 6                                          |
| Strategy settled (no runtime orchestrator) | ✅ **DONE**             | motyc; containers reachable via API                                                              |
| **E2E all five stages**                    | ✅ **VERIFIED NIGHTLY** | run `30515547822`; stage 5 executes; ≈4 min end to end                                           |
| **Schedule-triggered fixes**               | ✅ **LIVE**             | `test` == default in all 6; GPU crons gone, smoke reporting present, `check_version.py` vendored |
| `docker.yml` permissions                   | ✅ **FIXED**            | Wave B regression; green in all 5                                                                |
| Release bundle integrity                   | ✅ **FIXED**            | 9 modules restored + closure guard — **guard itself unexercised, see T1**                        |
| `secrets: inherit`                         | ✅ **ELIMINATED**       | only `docker-tool` takes a secret; the other four take none                                      |
| Container-scan race                        | ✅ **FIXED**            | post-publish scan by digest inside `build-and-push` — **unexercised, see T1**                    |
| Lint policy                                | ✅ **UNIFIED**          | blocking in all 5                                                                                |
| version-guard duplication                  | ✅ **CONSOLIDATED**     | one vendored `check_version.py` held by `para-drift` — **release path unexercised, see T1**      |
| Action pin drift                           | ✅ **CONVERGED**        | every action on a single major                                                                   |
| Dependabot                                 | ✅ **ALIGNED**          | hub covered; all ecosystems grouped; page-classification watches the real manifest dirs          |
| **Release path**                           | ✅ **PROVEN**           | v0.10.1 fired every guard: version gate, digest scan, SARIF, attestations, publish — see T1      |
| Reusable pin hardening                     | ✅ **DONE**             | 26 pins on `@v1`; `para-drift` reads templates via a `hub-ref` input — see T2                    |
| GPU self-hosted runner                     | 🚧 **BLOCKED**         | rharasim — gates every `slow` test and the E2E `alto → nlp` live bridge                          |
| Trivy blocking severity                    | 📝 **POLICY**          | `exit-code: "0"` in both paths; decide after T1 produces a real scan                             |
| Secret scanning (gitleaks)                 | ✅ **DROPPED**          | decided 2026-07-30; template deleted — GitHub-native scanning + push protection covers it        |
| Branch protection (hub)                    | ⬜ **OPEN**             | repo-settings action on `main`/`test`; the pin half of T2 is done                                |

---

## T1 — Release path — ✅ PROVEN

`atrium-translator` `v0.10.1` (tag → `5164c7b`, 2026-07-30) fired both workflows and **every guard
executed**. Verified from the step lists rather than run conclusions, because a green run can still
skip everything — the lesson from stage 5:

| Guard                                                  | Evidence                                                              |
|--------------------------------------------------------|-----------------------------------------------------------------------|
| `check_version.py --require-tag`                       | `Block release on version/tag mismatch` — success (run `30561448118`) |
| `build-and-push` under the new `security-events` grant | job succeeded 16:26:57→16:28:29 (run `30561449166`)                   |
| Trivy digest scan                                      | `Trivy scan the published digest` — success, 15s                      |
| SARIF upload                                           | `Upload SARIF to code scanning` — success, 7s                         |
| buildkit SBOM/provenance                               | GHCR index for `:0.10.1` carries an `attestation-manifest`            |

The SARIF upload is the notable one — the exact permission chain whose absence caused the Wave B
`startup_failure`, now proven end to end.

**Still to do:**
- **page-classification release** — its bundle closure guard is the one covering a defect that
  actually shipped, and it remains unexercised.
- **Negative test** — tag once with a deliberately mismatched `CITATION.cff` and confirm the gate
  blocks. It would have caught `v1.0.0.-beta` and `v1.16.2` and has still never had occasion to fire.

## T2 — `@v1` pin migration — ✅ DONE

26 references moved from `@test` to `@v1`: **20 live callers** (4 files × 5 repos) and **6 doc
examples**. Leaving the examples behind would have reintroduced the mutable pin in whichever repo
next adopted a template.

Plus the 27th, which a find-and-replace would have got wrong: `para-drift.reusable.yml` hardcoded
`ref: test` for the hub checkout supplying the canonical `docs/templates/shared/*` files. Left alone,
the pin would have been half-done — workflow *logic* from the immutable tag while the *files it
enforces* still came from a mutable branch, defeating the pin for the one reusable whose whole job is
comparing files. Now a **`hub-ref` input** defaulting to `v1`; `@test` callers pass `hub-ref: test`.

**Two channels**, documented in `docs/docker_gha.md` §1:

| Channel | Who uses it                              | Mutability                                      |
|---------|------------------------------------------|-------------------------------------------------|
| `@v1`   | every caller in every tool repo          | moving major tag; only a maintainer advances it |
| `@test` | pre-release validation before `v1` moves | branch — moves on every merge                   |

**Prerequisite for the hub-ref default to take effect:** `v1` must be moved onto the commit carrying
the `hub-ref` input. Until then `@v1` resolves to `ef339ab`, which lacks it — harmless, since the
default only matters once it exists, but the two disagree until the tag moves.

**Remaining:** branch protection on the hub's `main`/`test` is a repo-settings action, not code.

## T3 — codeql/pre-commit reusables — ✅ DONE (pending `v1` move)

431 lines across ten files collapsed to 300. Their only real differences were an arbitrary weekly
CodeQL cron (three values that differed by accident of authorship) and whether the pre-commit
environment cache was present — 2 of 5 had it. The cache now applies everywhere; the crons are
staggered 15 minutes apart rather than colliding or scattering.

Also fixed a coverage gap found while migrating: **page-classification's `codeql` and `pre-commit`
only triggered on `[test, master]`, while its other four workflows cover `[vit, test, clip, master]`
— and `vit` is that repo's default branch.** Static analysis and lint were not running on the branch
where work lands. Same gap fixed for api-contract/para-drift earlier in #18; these two were missed.

**Ordering — the hub commit must land and `v1` must move before the callers are pushed.** Delivered
as separate commits for exactly that reason. Pushed out of order, `@v1` will not contain the new
reusables and all ten callers `startup_failure`.

Verified locally: 64 files parse; **30** caller/callee permission pairs all satisfied (the 10 new
CodeQL pairs need `security-events: write`, granted explicitly in each caller); no caller passes an
undeclared input; zero `@test` pins remain.

## T4 — Remaining decisions

- **Trivy blocking severity.** `exit-code: "0"` in both the post-publish and periodic scans. T1
  produces the first real digest scan; triage those findings, then decide whether CRITICAL should fail
  a release.
- ~~**gitleaks.**~~ ✅ Done — template deleted 2026-07-30. `docs/docker_gha.md` and
  `docs/plan_repo_review.md` §Phase 4 updated to record the decision rather than leave it "paused".
- **SHA-pin write-scoped third-party actions** (`softprops/action-gh-release`,
  `aquasecurity/trivy-action`) via `pin-github-action` or Dependabot digest support, so the pins are
  verifiable rather than hand-written.

## T5 — GPU runner (external, rharasim)

Until a `[self-hosted, gpu]` runner is registered, no `slow`-marked test executes anywhere and the E2E
`alto → nlp` boundary keeps using the committed fixture CSV instead of a live `--start-from classify`
run. Crons are removed so the ecosystem stops advertising coverage it lacks; restore instructions are
inline in each `gpu-inference.yml`.

## Done-criteria for the issue

- ✅ Nightly E2E exercises all five stages and fails loudly when it cannot.
- ✅ No workflow targets a branch that does not exist; PR checks match push checks.
- ✅ No caller passes `secrets: inherit`.
- ⬜ A release has actually been cut through the new path, with the version gate proven to block a
  mismatch (T1).
- ⬜ All 20 caller refs resolve to an immutable ref (T2).
- ⬜ `slow`-marked tests execute somewhere on a real schedule (T5).

## Out of scope (other threads)

- The runtime orchestrator — explicitly *not* needed; containers reached via API (motyc).
- Compose-level pipeline chaining → `docs/plan_repo_review.md` §8.
- GPU runner provisioning itself → infra/rharasim; tracked here only as a blocking dependency.
