# Strategy — Issue #61: logs as an event stream

> Issue [\#61](https://github.com/ufal/atrium-project/issues/61) · Labels: `enhancement`, `development` · Parent: [\#53](https://github.com/ufal/atrium-project/issues/53) (factor XI)
> **Status: 🟢 Code landed on `test` in all five repos (S1–S3), verified green. 🟡 What remains is not code: doc parity, a regression guard, a scope record, and the issue's own trail — S5–S8 below. Evidence in [`../digests/61.digest.md`](../digests/61.digest.md).**
> _Rewritten against the tree as measured **2026-09-12** on `origin/test` (alto `ee51ec3` · translator `8aeac5f` · pc `9683c76` · nlp `61d9533`). The 2026-09-11 revision's S1–S3 are done; this revision adds S5–S8 and supersedes its verification/out-of-scope sections._

## Context

Factor XI asks for logs on stdout, configured once, at the entrypoint, at a level the operator sets. The issue framed this as two counter-examples and a one-line fix.

Since the issue was written, #58 landed and created the `__main__` root-logger block in four of the five services. The issue's framing no longer fits: **the configuration half is done.** `LOG_LEVEL` is now read in five places rather than one.

What the measurement shows instead is that wiring the knob was the easy half. `LOG_LEVEL` is **effective in two of five services**. In two others nothing ever reaches the logger; in the fifth a single library-module line silently overrides the entrypoint that was written to satisfy this very issue. A partner setting `LOG_LEVEL=DEBUG` today sees no change in three of five containers, and no error telling them why.

The work is still small — six `print` conversions, one deleted line, one subprocess relay — but it is in different files than the issue names, and skipping any one of the three leaves the acceptance criterion failing.

## Status snapshot (`origin/test`: project `44c4710` · alto `c5d41f7` · pc `6640e1b` · nlp `f711694` · translator `d8155b7` · llm `04540e7`)

| Repo                | Root logger configured              | `logger.*` sites | `LOG_LEVEL` effective | Work                       |
|---------------------|-------------------------------------|------------------|-----------------------|----------------------------|
| alto-postprocess    | `service/text_api.py:537` ✅         | 8                | ✅                     | **S2** (2 prints)          |
| llm-enrich          | `service/api.py:531` ✅              | 3                | ✅                     | — already correct          |
| page-classification | `service/api.py:360` ✅ but **dead** | 18               | ❌                     | **S1**                     |
| translator          | `service/api.py:335` ✅              | **0**            | ❌ placebo             | **S2** (2 prints)          |
| nlp-enrich          | `service/api.py:558` ✅              | **0**            | ❌ placebo             | **S2** (2 prints) + **S3** |

**S1–S3 landed 2026-09-12** (confirmed byte-identical to the fix delivered, and re-verified
independently against the pushed commits): `logger.*` counts are now alto 10 · pc 18 · nlp 3 ·
translator 2 · llm 3 — `LOG_LEVEL` effective in all five, zero import-time `basicConfig`, zero
stray `print()` in service modules. What remains is S4 (partly done — see below) and S5–S8.

## Strategic actions

Do them in this order. S1 is two lines and makes an existing test observable; S2 is what the acceptance criterion actually measures; S3 is the only one with design content.

### S1 — Delete the override that is killing an already-written fix (page-classification)

`service/inference.py:22` — delete `logging.basicConfig(level=logging.INFO)`, keep `logger = logging.getLogger(__name__)`.

That is the whole change. `service/api.py:360-365` already contains the correct block, already commented `(issue #61)`; it is a no-op today only because `api.py:16` imports `inference.py` at module scope and `basicConfig` does nothing once the root logger has handlers. Deleting line 22 makes the existing block live. **Do not add `force=True`** as a shortcut — it would paper over the import-time side effect rather than remove it, and leave the next library module free to do the same thing.

Replace the deleted line with alto's rule comment (`text_inference.py:51-55`), adapted. The rule belongs in the file that has to obey it, which is the entire reason alto's version works as documentation:

```python
# (12-factor XI) No basicConfig() here. This module is imported as a library by
# api.py and by tests; configuring the ROOT logger at import time is a side effect
# that silently overrides whatever the host application chose — and because
# basicConfig() is a no-op once the root logger has handlers, the first import
# wins over the real entrypoint. Emit to a named logger; let __main__ decide
# handlers and level. (issue #61)
logger = logging.getLogger(__name__)
```

Two things to check while there:

1. **Does anything lose output?** 18 `logger.*` sites in `service/` have been logging "for free" through the root config this line installed. After the fix they route through `api.py`'s `__main__` — under `python -m service.api` that is strictly better. Under `pytest` the root logger is unconfigured; `caplog`-based tests are unaffected, but confirm none of `tests/` asserts on captured stream output.
2. **Both import names.** `inference.py:15-20` is a `try/except` import block, so the module loads under two different names depending on the path. Both must reach a configured root; only `service/api.py` and `tests/` import it (the batch CLI path — `run.py`, `entrypoint.py` — does not), so the risk is contained.

**Expected observable change**: page-classification's records move from stderr to stdout, gain timestamps and logger names, and start responding to `LOG_LEVEL`. Anyone grepping container logs for the old `INFO:service.api:` prefix will need the new format — worth a line in the issue.

### S2 — Convert the six in-server `print()` calls (4 files, 4 repos)

Without this, S1 does not satisfy the acceptance criterion: two services would still have a correctly configured logger that no record enters.

**First, add the logger.** `translator/service/api.py` and `nlp/service/enrichment.py` do not `import logging` at module scope at all. Add `import logging` and `logger = logging.getLogger(__name__)` near the top of each — nothing else in those files changes.

**Then convert, at these levels:**

| File:line                           | Now                                                                         | Becomes                                                                                                                      |
|-------------------------------------|-----------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------|
| `alto/service/text_inference.py:30` | `print("CRITICAL: 'v3' folder not found …")`                                | `logger.critical(…)`                                                                                                         |
| `alto/service/text_api.py:285`      | `print(f"[document] WARNING – …", file=sys.stderr)`                         | `logger.warning(…)` — drop the `[document] WARNING –` prefix and the `file=` kwarg; the level and logger name now carry both |
| `nlp/service/enrichment.py:340`     | `print("[warmup] KeyBERT model loaded.")`                                   | `logger.info("KeyBERT model loaded.")`                                                                                       |
| `nlp/service/enrichment.py:342`     | `print(f"[warmup] KeyBERT warmup failed: {exc}. Will degrade gracefully.")` | `logger.warning("KeyBERT warmup failed: %s. Will degrade gracefully.", exc)`                                                 |
| `translator/service/api.py:68`      | `print(f"[INFO] Warming up translation backend …")`                         | `logger.info("Warming up translation backend (%s)", backend or "lindat")`                                                    |
| `translator/service/api.py:78`      | `print("[INFO] Shutting down service...")`                                  | `logger.info("Shutting down service")`                                                                                       |

Use `%s` lazy formatting, not f-strings, in the converted calls — it matches llm-enrich's existing `api.py:204,208` and skips the interpolation when the level is filtered out, which is the point of having a level.

Two of these deserve naming in the commit message, because they are the ones that justify the change:

- **`text_inference.py:30`** sits 21 lines above the comment at `:51` that documents the factor-XI rule. A `CRITICAL` condition — layout reordering unavailable for the process's whole life — announced by an un-timestamped, un-named `print` at import time, in the repo the issue holds up as the model. Fixing it makes alto's own documentation true.
- **`translator/service/api.py:78`** is #55's drain record.

**Leave the other 41 `print()` calls alone.** They are in three standalone CLI scripts whose stdout *is* their output contract, run as their own process: `service/healthcheck.py` (×5, invoked by Docker `HEALTHCHECK` — converting these breaks the health contract), `page-classification/service/api_client.py`, and `nlp-enrich/service/test_api.py`. **Say this in the PR description.** A later reader sweeping for `print` in `service/` will otherwise "finish the job" and break five healthchecks.

llm-enrich needs no changes in this step.

### S3 — Relay the subprocess's output instead of discarding it (nlp-enrich)

The issue asks what `processing.log` is for and offers three answers. The measurement settles it without a policy call: **the records are already mirrored to stdout by the shell, and the Python parent throws them away.**

`api_util/api_common.sh:41` already does `… | tee -a "$LOG_FILE"` — file *and* stdout. The loss is at `service/enrichment.py:472-480`, where `subprocess.run(..., capture_output=True)` pipes the child's streams and nothing forwards them, keeping only `(stdout + stderr)[-4000:]`.

**The change**: replace the capture-and-discard with a relay. Stream the child's output line by line to `logger.info` as it arrives, while still accumulating the tail:

- keep a bounded deque (or a running 4000-char slice) so `PipelineError`'s message and `EnrichmentResult.stdout_tail` keep their current contracts exactly — **do not change the 4000-char tail semantics in this issue**, several error paths and at least one API response field depend on them;
- merge stderr into the same stream (`stderr=subprocess.STDOUT`) or relay it separately — but **cover it either way**: `api_4_stats.sh:145` tees to `>&2`, so a stdout-only relay silently drops the stats stage's failure records;
- apply the same treatment to the dry-run health path at `enrichment.py:402-413`, which has the identical pattern;
- preserve the `timeout=30` on the health path — a relay loop must not become an unbounded read.

**`processing.log` then needs no decision.** It stays as a per-job artifact the caller can download, the event stream is complete without it, and it is documented as an artifact rather than inherited as an accident.

**Worth recording in the issue**: `enrichment.py:532` `shutil.rmtree`s the workspace on exception unless `API_KEEP_WORKSPACES` is set (off by default), so today `processing.log` survives successful runs and is **deleted on failed ones**. That is the clearest single argument that the file was never a diagnostic channel. The relay fixes the diagnosis; whether the workspace should outlive a failure is [nlp#35](https://github.com/ufal/atrium-nlp-enrich/issues/35)'s call — coordinate, do not decide it twice.

### S4 — Close the parity gaps (narrowed by S5 below)

- ~~README wording~~ / ~~alto's `.env.example`~~ → **done by S5.**
- **The two missing `.env.example` files** (page-classification, llm-enrich) stay **#60's** —
  #60 owns the env contract, and its own plan withheld `LOG_LEVEL` "until #61 is verified green
  in all five"; it now is (see S5).
- ~~Correct #60's premise~~ → **done by S8** (the issue comment states the landed state plainly;
  #60's plan text is #60's to fix, not #61's to edit).
- ~~Batch CLIs out of scope~~ → **done by S6**, recorded as an enforced test boundary rather than
  a sentence in a design doc.

### S5 — `LOG_LEVEL` doc parity (narrow: #61's own knob only)

Two edits, both mechanical, both scoped to the variable this issue makes real — not the two
`.env.example` files #60 still owns:

1. **`atrium-alto-postprocess/.env.example`** — add `LOG_LEVEL=INFO` after
   `GRACEFUL_SHUTDOWN_S=20`, with the same one-line comment form nlp-enrich and translator
   already use, adapted to alto's real start path (`python service/text_api.py`, not
   `python -m service.api`).
2. **README wording** — `atrium-alto-postprocess/service/README.md:400` and
   `atrium-page-classification/service/README.md:312` still say "root logger level for the
   start path below"; rewrite both to name the path + `(issue #61)`, matching nlp-enrich `:193`,
   translator `:44`, llm-enrich `:111`.

### S6 — A static regression guard: `tests/test_logging_contract.py`

Nothing currently enforces any of #61's acceptance criteria going forward. The obvious fixes
don't reach:

- **Ruff can't.** No rule combination covers "`basicConfig` outside `__main__`", and enabling
  `T201` (print) to catch D2 would need enumerating almost every file outside `service/` in five
  repos — measured 88 (translator) to 309 (alto) legitimate prints live in batch CLIs, and ruff's
  `per-file-ignores` has no negated glob.
- **A runtime assertion would skip in the one repo that regressed.** The natural test —
  "importing `service.inference` installs no root handler" — needs to actually import it, and
  page-classification's `service.inference` needs `torch`, which the fast lane (`pytest -m
  "not slow"`, run by the hub's `docker-tool.reusable.yml:187` on every PR) deliberately omits.
  `tests/test_service_entrypoint.py:108`'s equivalent check is already `@pytest.mark.slow` for
  exactly this reason.

So the guard is **static** — `ast`-based source analysis, no imports of the modules under test,
stdlib + pytest only. One file, byte-identical in all five repos (delivered), canonicalized at
`atrium-project/docs/templates/shared/test_logging_contract.py` and enforced by para-drift like
`atrium_service.py` and `healthcheck.py`. It discovers the entrypoint module (the `service/*.py`
file whose `__main__` block calls `uvicorn.run`) rather than hardcoding a filename, so the same
bytes cover alto's `text_api.py` and the other four's `api.py`.

Four checks, one per acceptance criterion / defect:

1. No `logging.basicConfig(` outside the discovered entrypoint's `__main__` block — D1.
2. No `print(` in `service/*.py`, excluding `healthcheck.py` / `api_client.py` / `test_api.py`
   (their stdout genuinely is a CLI contract, not service logging) — D2. This exclusion list is
   also **S6's answer to the batch-CLI scope question**: the check is scoped to `service/` by
   construction, so `run.py` / `run_pipeline.py` / `main.py` / `llm_run.py` / `entrypoint.py`
   are out of scope structurally, not by a sentence someone has to remember.
3. The entrypoint's `__main__` reads `LOG_LEVEL` — the config half.
4. At least one `logger.<level>()` call exists under `service/` — the **"not a placebo"** check.
   1 and 3 alone would have passed for translator and nlp-enrich before their fixes: a correctly
   wired `LOG_LEVEL` controlling zero call sites. This is the check built specifically to have
   caught that.

**Proven, not just written**: re-introduced D1 in a scratch copy of `pc/service/inference.py`
(`logging.basicConfig(level=logging.INFO)` before the `logger = getLogger(...)` line) and
confirmed check 1 fails with the exact violating line; removed both `logger.info` calls from a
scratch copy of `translator/service/api.py` and confirmed check 4 fails. A guard never seen to
fail is not known to work — see Verification.

**Wiring is a four-file change, not one** — miss any and the file drifts silently, exactly as
`atrium_rocrate.py`'s own para-drift comment records happening before:

- canonical file at `docs/templates/shared/test_logging_contract.py` (delivered)
- `+1` entry in `scripts/revendor_shared.sh`'s `SHARED_FILES` map (delivered)
- `+1 diff -u` step in `para-drift.reusable.yml`, modeled on the `healthcheck.py` step (delivered)
- `+1` entry in **every** `ruff.toml`'s `[format] exclude` (all six, delivered) — otherwise
  `ruff-format` reflows it to whichever repo's local `line-length` touches it next (120 in
  alto/pc/translator, 100 in nlp/llm), the exact incident `test_document_originators.py` already
  lived through per every `ruff.toml`'s own comment.

**The `@v1` tag is the one manual step.** Tool repos call
`ufal/atrium-project/.github/workflows/para-drift.reusable.yml@v1`; the hub's `v1` tag has to
move to include the new step before any tool repo enforces it. Not something this session can do.

### S7 — Container-level proof: extend `docker-tool.reusable.yml`

The static guard proves the *shape* is right; it cannot prove `LOG_LEVEL=WARNING` actually
changes what a running container emits — the second acceptance criterion, and the one no test
above touches. Two additions beside the existing issue-#55/#58 probes, same file:

1. **The control**, added to the existing default-port HEALTHCHECK step: after the container
   reports healthy, grep its logs for one line matching
   `^[0-9-]+ [0-9:,]+ INFO [^ ]+ ` (the agreed format string's shape, anchored so `INFO` must sit
   in the level field — not merely appear as a substring). Fails loudly if the format has drifted
   or nothing is logging at all, so a false pass in step 2 can't be blamed on this.
2. **The assertion**, a new step: run the same image with `LOG_LEVEL=WARNING`, wait for healthy
   (same 330s budget, no request/SIGTERM battery — not LOG_LEVEL-dependent, already covered
   above), then grep for the **absence** of that same INFO pattern.

Deliberately the WARNING direction, not DEBUG: raising the bar needs no request traffic to
observe, since startup/warmup alone produce the INFO records being suppressed. A DEBUG-only test
would have passed throughout this repo's own history — `LOG_LEVEL` was wired and controlled
nothing in two services; "more output allowed through" proves nothing when nothing was gated to
begin with.

### S8 — The issue's own trail

#61 has zero comments and is still `open`; the code landed straight on `test`, not through a PR
that would auto-reference it. Post the comment (delivered as `.md`) once S5–S7 land, stating
plainly: what shipped, what it fixed, and what's still open (the two `.env.example` files, now
#60's and now unblocked). Flag #60's stale "#58, #61 and #63 all landed" line for #60 to correct
— #61's comment states #61's own truth; #60's plan is #60's to fix.

## Verification / done-criteria

**S1–S3 (verified 2026-09-12, all against pushed `origin/test` tips):**
- [x] `grep -rn "basicConfig"` matches only `__main__` blocks and rule comments in all five repos.
- [x] `grep -rn "print("` under `service/` matches only `healthcheck.py`, `api_client.py`,
      `test_api.py` in all five repos.
- [x] `logger.*` call-site counts are non-zero in all five (alto 10 · pc 18 · nlp 3 · translator 2
      · llm 3) — `LOG_LEVEL` is no longer a placebo anywhere.
- [x] nlp-enrich's `_run_and_log` relay is live at both call sites (`dry_run`, `enrich`).

**S5–S8 (this round):**
- [ ] Alto's `.env.example` declares `LOG_LEVEL`; all five READMEs use the same wording form.
- [ ] `tests/test_logging_contract.py` passes in all five repos' fast lane
      (`pytest -q -m "not slow"`) — confirmed: alto, 416/865/501/860 passed respectively across
      pc/nlp/translator/llm with zero new failures, on top of the existing suites.
- [ ] The guard **fails** when the original D1 defect is reintroduced, and **fails** when a
      repo's only `logger.<level>()` calls are removed — both reproduced this session.
- [ ] `ruff check` and `ruff format --check` both pass on the new file at line-length 120 (alto)
      and 100 (nlp-enrich) — confirmed clean and confirmed excluded from formatting at both.
- [ ] The five vendored copies and the hub canonical are byte-identical (the same check
      para-drift will run).
- [ ] Once the hub's `v1` tag moves: the new para-drift step is green on all five, and the two
      new container-probe assertions (INFO present at default, absent at WARNING) both pass.
- [ ] The #61 comment is posted and states the landed state accurately.

## Out of scope

- **Structured/JSON logging.** Factor XI asks for an event stream on stdout, not a schema.
- **Log aggregation, retention, shipping.** The execution environment's job under factor XI.
- **Batch CLI entrypoints.** Structurally out of scope via S6's `service/`-only test boundary.
- **The 4000-char tail contract** in `enrichment.py`. A separate, API-visible change.
- **The `API_JOBS_ROOT` workspace design and workspace lifetime on failure.**
  [nlp#35](https://github.com/ufal/atrium-nlp-enrich/issues/35).
- **The two missing `.env.example` files** (page-classification, llm-enrich). #60's, now
  unblocked — see S8.
- **The hub's orphaned `docs/templates/shared/test_atrium_service.py`** — exists in the hub,
  vendored in none of the five repos. Pre-existing, unrelated to #61; noted in the digest.
- **Any commit, push, tag move, or PR.** The `@v1` move (S6) is the one step named for the user
  to perform by hand.
- **Publishing `LOG_LEVEL` in the env contract.** [\#60](https://github.com/ufal/atrium-project/issues/60) — unblocked by this issue, not part of it.
