# Strategy — Issue #51: SKOSification of internal data / DARIAH Vocabs standards

> Issue [\#51](https://github.com/ufal/atrium-project/issues/51) · Labels: `enhancement`, `development`, `beginning`, `integration` · Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover · **Status: ⏸️ Deferred — after the pilot (AMČR, #67).** _Before:_ 🟢 Internal SKOS alignment BUILT (2026-09-08). Namespace request remains ❌ not pursued — deferred to @petrpajdla's 18-month project, per @motyc.**
>
> Normative document: [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md).

### 2026-09-26 — AMČR baseline ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ⏸️ defer.** Decided by motyc (AMČR) in #67, adopted by ÚFAL. The open follow-ups (F4, F5, F7, V-5, the
harvester retirement) wait until after the pilot. Two corrections and one link:
- **V-1's date:** fixed on **2026-09-25** (llm-enrich `0aef037`), not 09-16 (K4TEL's correction on the issue,
  2026-09-25). The two status rows that say "2026-09-16" are corrected below.
- **#67 R7** wants one versioned vocabulary artifact under `para-drift`. When that is built, its format follows
  `docs/skos_strategy.md` and `atrium_vocab.schema.json`, so the SKOS alignment done here is reused, not redone.

## Context

The issue asked for a DARIAH Vocabs namespace so the harmonised AMCR+TEATER vocabulary could be
published as SKOS. @motyc's same-day answer removed that from this repo's plate — a dedicated
SKOSification project led by @petrpajdla owns it on an 18-month timeline — **and added the part
this plan now acts on**:

> "This does not prevent using SKOS as framework to align the data internally. It is definitely
> desirable."

This plan replaces its own previous version, which read that as §A *"Do nothing that anticipates a
namespace"* plus an optional, unscheduled §B. §A was right about the namespace and wrong about the
consequence: it treated "no namespace" as "no work", when the blocking dependency applied only to
**publication**. Everything @motyc affirmatively encouraged needed no namespace at all — and three
live defects were waiting in the gap. See [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md) §6.

## Status snapshot

| Item                                                      | State                            | Note                                                                                                                                                                             |
|-----------------------------------------------------------|----------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| DARIAH Vocabs namespace request                           | ❌ **NOT PURSUED**                | Unchanged. Owned by @petrpajdla's project.                                                                                                                                       |
| Source concepts keep their own IDs                        | ✅ **HELD BY CONSTRUCTION**       | ATRIUM mints no URI for any AMCR or TEATER concept, so there is nothing to migrate off when the PIDs land.                                                                       |
| SKOS as internal alignment framework                      | ✅ **BUILT**                      | `atrium_vocab.py` (hub-canonical) + `vocab_build.py --skos`.                                                                                                                     |
| `vocab_sources.py` SKOS export path                       | ✅ **BUILT**                      | 5,594 concepts / 55,188 triples, `union.skos.ttl`, drift-gated.                                                                                                                  |
| The 707 captured `skos:exactMatch` URIs                   | ✅ **EXPORTED**                   | Were harvested and read by one reviewer sheet; now in the graph and resolvable at runtime.                                                                                       |
| `entities[].pid` (the ARIADNE/GoTriple hook)              | ✅ **FILLABLE**                   | `vocab_manager.resolve_pid()`; 432 labels resolve to Getty AAT. Was written by nothing.                                                                                          |
| ATRIUM-authored label sets                                | ✅ **DECLARED ONCE**              | 64 concepts, 6 schemes, previously scattered across four repos in four forms.                                                                                                    |
| Defect V-1 (`DROP_CATEGORIES` no-ops on the OCR path)     | ✅ **FIXED 2026-09-25**           | Taken by the owner. Now filters on `UNTRUSTWORTHY_LINE_CATEGORIES`. **This changes what text reaches the model** — always the cost, now paid.                                    |
| Defect V-2 (`collect_images()` breaks on shipped samples) | ✅ **FIXED 2026-09-16**           | Filtered to directories. Fixes both the raise and the silent class-index shift.                                                                                                  |
| Defect V-5 (`teater_category` read as a theme)            | ⚠️ **NEW, MITIGATED 2026-09-16** | Found while wiring F6: the crate was minting an ATRIUM URI for an AMCR concept. It now prefers the source URI. The contract question needs an owner.                             |
| `enrichment.items[].teater_category_uri` (F6)             | ✅ **BUILT 2026-09-16**           | The 707 captured `skos:exactMatch` URIs now have somewhere to go, and reach the crate.                                                                                           |
| Prompt CONTENT / construction                             | ✅ **UNCHANGED**                  | Still protected: F6's URIs reach runtime via `concept_index()`, never via `nested_keep`. See §B.                                                                                 |
| Text the model is SHOWN                                   | ⚠️ **CHANGED 2026-09-25**        | By V-1, and only V-1: untrustworthy OCR lines are now filtered out where they previously passed through. Distinct from the row above — the prompt is the same, its input is not. |

## Strategic actions

### A — The namespace question stays closed, and is now closed *by design*

No namespace is requested. The one constant that roots ATRIUM-authored URIs
(`atrium_vocab.SKOS_BASE = "https://w3id.org/atrium/"`) covers only the ~64 concepts ATRIUM itself
authors — page categories, line categories, quality bands, coarse entity types, harmonisation
themes. Every harvested concept keeps the source's own identifier. When the SKOSification project
ships, either the `w3id` redirect is repointed (zero data changes) or one string changes and the
artifact is regenerated.

Registering the `w3id.org/atrium` redirect is **optional and blocks nothing** — these are
identifiers before they are locations. It is also not a namespace request in the sense @motyc ruled
out: it reserves a redirect, not a vocabulary, and commits ARUP/ARUB to nothing.

### B — What was deliberately NOT done

Recorded because each looked like the obvious next step and each is wrong:

* **`exact_match` was NOT added to `_settings.nested_keep`.** `get_prompt_string()` serialises the
  whole nested structure into the model's system prompt, so that one config edit would inject 707
  Getty URIs plus 5,594 source URIs into every request — against a context budget this repo tracks
  in its own reviewer sheet, and a change to model behaviour in the name of a provenance field.
  The URIs reach runtime through `concept_index()` instead.
* **AMCR's `hierarchie_vyse` was NOT emitted as `skos:broader`.** All 1,176 edges cross heslář
  boundaries; `obývání` (an activity) declares 22 `areal` "parents". It is `skos:related`.
* **TEATER's ancestor chain was NOT emitted as 12 `skos:broader` links.** Only the direct parent is
  `skos:broader`; the rest are `skos:broaderTransitive`.
* **TEATER's AAT quotes were NOT promoted to `skos:exactMatch`.** They are citations
  (`dcterms:source`), not identity claims.
* **`same_as` was NOT serialised as `owl:sameAs`.** It is a label-shape scoring heuristic.
* **No `enum` was added** to `lines[].categ`, `pages[].category` or `page_categories`.
  `validate_document()` raises, and page-classification derives its labels from the filesystem at
  runtime — an enum would convert a naming slip into a pipeline failure.

### C — Check in with @petrpajdla's project when it has a namespace

Unchanged and still not urgent. The check-in is now cheaper than it was: the migration is a
redirect or a one-line constant, not a data model change.

## Verification / done-criteria

Run and recorded in [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md) §8: registry
selftest; Turtle/JSON-LD isomorphism under `rdflib`; zero SKOS integrity violations; drift gate
clean **and** proven to still catch a tampered artifact; byte-stability; `revendor_shared.sh
--check` green across 5 repos; and the full suites — 255 (nlp-enrich), 178 (llm-enrich), 340
(page-classification), 434 (translator), 776 (alto-postprocess), 63 (hub).

## Out of scope

- Publishing anything externally, and the namespace itself.
- Vocabulary *content* governance (facets, exclusions, dedup) — owned by `atrium-nlp-enrich#6`.
- The eight follow-ups in [`../../docs/skos_strategy.md`](../../docs/skos_strategy.md) §7, including
  the V-1 fix, which needs an owner because it changes what the model reads.
