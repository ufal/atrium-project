# Strategy — Issue #17: Review workflow descriptions in SSHOMP

> Issue [\#17](https://github.com/ufal/atrium-project/issues/17) · Labels: `documentation` · Milestone: Q4 [WP3: Conclusion of Semantic Harmonization] · **Status: 🏛️ AMČR-owned (#67) · 🟡 `0xSpVP` rewrite submitted 2026-09-24 (v`106039`), awaiting moderation**
> _Refreshed 2026-09-25 (round 7). Evidence: [`../digests/17.digest.md`](../digests/17.digest.md)._

### 2026-09-26 — AMČR baseline ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: 🏛️ AMČR's own.** Decided by motyc (AMČR) in #67, adopted by ÚFAL. `0xSpVP` and this issue are AMČR's; ÚFAL's part is its own workflow records and reporting what it finds. Two edits below: A's check adds motyc's rule (no issue numbers, dates or versions in `0xSpVP`); B no longer *asks* motyc to point steps 3 and 10 at the new records — he committed to linking `j9fqxo` (steps 6 and 8) and the rest is his decision. C applies to ÚFAL's texts only.

## Context
Two kinds of SSHOMP object are in play: **tool-or-service records**, one per repository (#4), and **workflow
records**, which tell a process step by step. #17 is about the second kind, and first of all about the AMČR text
workflow `0xSpVP`, which @motyc curates. He rewrote it as an 11-step chain that links the ÚFAL tool records and
submitted it on 2026-09-24. Two further workflow records belong to ÚFAL: the translator's `13eHAZ` (to be rebuilt)
and a new page-classifier workflow — planned in #4 (Appendix B5 and C) from the hub site's Workflows pages.

## Status snapshot
| Item                                             | State           | Note                                                        |
|--------------------------------------------------|-----------------|-------------------------------------------------------------|
| Tool-or-service records (five)                   | ✅ **DONE**      | #4; `j9fqxo` submitted 09-25, the four others updated 09-25 |
| `0xSpVP` rewritten (11 steps, links the records) | ⏳ **SUBMITTED** | v`106039`, 09-24; not public yet                            |
| Licence information at workflow level            | ✅ **BY LINK**   | the linked tool records carry the licence tables            |
| `13eHAZ` rebuilt with steps                      | ⏳ **OPEN**      | #4 Appendix B5                                              |
| Page-classifier workflow record                  | ⏳ **OPEN**      | #4 Appendix C                                               |
| Box on this issue ticked                         | ⏳ **@motyc**    | after approval and the check below                          |

## Strategic actions

### A — Check `0xSpVP` once `106039` is public
- Steps 3, 5, 6, 7, 8 and 10 link `RER7Fw`, `YParYU`, `j9fqxo`, `EMhu3X`, `j9fqxo` and `CizIUW`.
- KER appears nowhere; step 8 names llm-enrich's vocabulary keywords.
- No issue numbers, dates or version details anywhere in the text (motyc's rule, #4, 2026-09-26).
- Step 6 holds the born-digital branch and the GROBID note.
- Anything missing is reported here, with the step number.

### B — Relate the workflow records to each other
When `13eHAZ` has its steps and the page-classifier workflow exists, relate each to `0xSpVP` ~~and ask @motyc to
point steps 3 and 10 at them~~ — linking back from `0xSpVP` is motyc's call (2026-09-26); he committed to `j9fqxo` only. A reader then moves from the chain to the tool's own workflow and back.

### C — One source text
The per-tool workflow text is the hub site's Workflows section (`docs_site/workflows/`, #57). A change to a step is
made there first, then in the SSHOMP record, then in the Galaxy workflow (#66).

## Verification / done-criteria
- `0xSpVP` v`106039` public, with the links and wording in A.
- `13eHAZ` and the page-classifier workflow record related to `0xSpVP`.
- @motyc ticks the second box.

## Out of scope
- Tool-or-service record content and licence tables — **#4** and **#6**.
- Galaxy workflows — **#66**.
