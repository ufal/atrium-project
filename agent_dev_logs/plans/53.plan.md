# Strategy — Issue #53: Follow best practices in all repos - `12 Factor` rules

> Issue [\#53](https://github.com/ufal/atrium-project/issues/53) · Labels: `enhancement`, `development` · Type: `Feature` · Milestone: WP4 [Pre-Closure Architecture Review]
> **Status: 🟢 Twelve factors green. Every remaining item is a CI dispatch or a comment — no factor is blocked on analysis or on code.**
> Supersedes the 2026-09-15 edition, which sequenced the closure of three ambers. All three are now green in the tree.

### 2026-09-16 pass — what changed

**§A's three "false claims" and §C's amber are all fixed**, verified by the plan's own commands:

| §A claim | Command | Result |
|---|---|---|
| the dotted `.env.example.template` was never renamed | `git ls-files docs/templates/ \| grep -c env.example.template` | `1`, undotted |
| `docs/docker_gha_roadmap.md` still says "seven canonical" | `grep -c "seven canonical" docs/docker_gha_roadmap.md` | `0` |
| `server.template.sh` cannot run from a `.yaml` repo | `grep -c "for ext in yml yaml" docs/templates/skill/server.template.sh` | `1` |
| §C: the runbook has no replica paragraph | `grep -i -c replica docs/k8s_acceptance_runbook.md` | `5` |

So factors VI, VIII and X are green, not amber, and nlp-enrich#35's last acceptance bullet closed when
`docs/templates/k8s/atrium-service.deployment.yaml` gained the per-repo constraint on 2026-09-16.

**Two findings this pass that no plan file named, both worth carrying forward:**

1. **A thirteenth concern the twelve factors do not cover: what the build INHERITS.** The release gate blocks on
   fixable CRITICALs in the published image, and `python:3.11-slim` is a floating tag nothing in this ecosystem
   bumps. That blocked two releases for the same three `perl-base` CVEs — translator `v1.0.0-beta` (09-13) and
   nlp-enrich `v0.20.2` (09-15, all three matrix targets) — and because promotion is `if: success()`, both
   published **by digest with no `:latest` tag**. Only translator was fixed; the other four repos were each one
   tag away from the same failure. The `apt-get upgrade` layer is now in all five, and
   `tests/test_dockerfile_security_layer.py` is para-drift canonical file #17, so losing it goes red **before** a
   release rather than during one. Closest factor is V (build/release/run): a release artifact whose security
   posture is decided by whenever Docker Hub last rebuilt an upstream tag is not a strictly separated build.
2. **Factor II has an upstream half we had not checked: that a declared dependency is what it says it is.** All
   five `v*.4` revisions of `ufal/vit-historical-page` serve the same `regnety_160` checkpoint, though
   `model_registry.py` declares five different base models. Switching `--best` to them would average one model
   with itself five times and still report a five-model ensemble — no error, plausible output.
   `tests/test_best_ensemble_distinct.py` now guards it and `data_scripts/unix/hf_reupload_v4_revisions.sh`
   repairs it. Pinning a dependency says nothing about whether the artifact behind the pin is correct.

## Context

The 09-09 plan asked "in what order do we fix what the audit found?" and answered it with seven
lettered actions (A–G). Between 2026-09-10 and 2026-09-14 every one of those actions became a
sub-issue and every sub-issue landed code on its repo's default branch. Action A (factor VII) is
green and proved in CI; action B (factor V) is green at `latest`; action C (factor I) closed with a
shared manifest; action D (factor IV) closed with more surfaces fixed than it named; action F's
three tidy-ups are two-thirds done.

So the question has changed. It is no longer "what do we fix?" It is **"what stops each of these
nine threads from being closed, and which of them are wrong on GitHub right now?"** — because the
code moved much faster than the record of it, and three comments posted on 09-14 assert fixes that
were never made.

## Status snapshot — hub `f675943`, all six repos at default == `test`

| Factor               | State | Sub-issue        | What stands between it and closed                                                   |
|----------------------|-------|------------------|-------------------------------------------------------------------------------------|
| I. Codebase          | 🟢    | #59              | Nothing in code. Correct three claims on the thread, then close                     |
| II. Dependencies     | 🟢    | — (rider on #62) | Closed; no issue of its own                                                         |
| III. Config          | 🟢    | #60              | The dotted `.env.example.template` and the 404 it leaves at `k8s_deployment.md:127` |
| IV. Backing services | 🟢    | #63              | Nothing in code. The digest still describes the defects in the present tense        |
| V. Build/release/run | 🟢    | #62 + alto#50    | An `image-tag: test` dispatch, never run. **alto#50 closed 2026-09-15**             |
| VI. Processes        | 🟡    | nlp#35           | One paragraph in `docs/k8s_acceptance_runbook.md`                                   |
| VII. Port binding    | 🟢    | #58              | The deliberate-breakage run the acceptance asks for                                 |
| VIII. Concurrency    | 🟡    | nlp#35           | Same paragraph as VI                                                                |
| IX. Disposability    | 🟢    | #55              | Five ARÚP/ARÚB checkboxes. **Not ours to close**                                    |
| X. Dev/prod parity   | 🟡    | #64              | `server.template.sh:70` + `docker_gha_roadmap.md:285`                               |
| XI. Logs             | 🟢    | #61              | Nothing. Closable once its digest stops saying the guard "is not yet landed"        |
| XII. Admin processes | 🟢    | —                | Closed                                                                              |

## Strategic actions

### A — Correct the three false claims before anything else
Three comments posted on 2026-09-14 state that work was done which was not. Left standing, each one
will be taken as true by the next reader and the underlying defect will never be found again.

- **#60** — `docs/templates/.env.example.template` was never renamed. Do the rename
  (`git mv`), then the 14 references resolve and `docs/k8s_deployment.md:127` stops 404-ing.
- **#59** — `docs/docker_gha_roadmap.md:59` ("All seven canonical shared files") and `:121` (D5's
  description of the old para-drift shape) were not updated. Both are one-line edits.
- **#64** — `docs/templates/skill/server.template.sh:70` still hardcodes the `.yml` pair, and
  `docs/docker_gha_roadmap.md:285` still prescribes normalising to `.yml`, which #64 explicitly
  decided against. Make the template resolve the extension at runtime and amend the roadmap.

All three are hub-only and none needs a retag except the roadmap/template pair, which is docs.

### B — Close what is closed
In order of how little is left:

1. ~~**alto#50**~~ — **done 2026-09-15.** Closing comment posted and the issue closed
   (`state_reason: completed`, 06:27). All four acceptance bullets were met; its thread had carried
   no record at all that the work landed, which is why it closed *with* a comment rather than silently.
2. **#61** — refresh the digest+plan (they still list the guard as "not yet landed" hours after it
   landed), then close. All three prose bullets verify.
3. **#63** — same shape: refresh first, because the digest's `## 🔎 Verified state` still describes
   every defect in the present tense and both files cite `verify_issue63.py`, which does not exist.
   Re-point the evidence at `tests/test_backing_service_urls.py`, then close.
4. **#59** — comment with the four corrections (agent-skill is fetched; the guard runs and reports
   `0/5 aligned`; `v1` is already retagged; the roadmap citations were not fixed), then close.

### C — Finish the three ambers
Each is a paragraph, and each has been a paragraph for four days.

- **VI/VIII (nlp#35)** — add the replica constraint to `docs/k8s_acceptance_runbook.md`. The
  manifest and `k8s_deployment.md` already say it; the runbook is the one a partner actually works
  through, and it contains zero occurrences of "replica". Optionally also the plan's §C guardrails:
  name the instance-locality in the 404 detail at `nlp/service/api.py:524,532,543` and log one line
  at boot.
- **X (#64)** — action A's third bullet. That is the whole of what is left.

### D — Run the two acceptance checks that were never run
Neither is blocked; both were simply skipped.

- **#62**: dispatch `e2e-pipeline-smoke.yml` at `image-tag: test`. The lane has **never** been
  dispatched — `workflow_dispatch` run count is 0 — so the `test` half of its acceptance has no
  evidence at all, while the `latest` half has fifteen green runs.
- **#58**: put the baked `--port` back on one repo's entrypoint, confirm the `PORT=9000` probe goes
  red, revert. The acceptance says "verify by breaking it"; nobody has.

### E — Open the two issues that have findings but no home
Both are already written up in this repo's own documents and neither has a tracking issue.

- **The E2E workspace substitution.** Every stage runs `-w /workspace/work/atrium-<tool>-main`
  (`e2e-pipeline-smoke.yml:298, 348, 370, 414`) — a fresh checkout, not the image's `/app` — so a
  source change that never reached the published image still passes. `docs/docker_gha_roadmap.md`
  §9 names it and says "It needs its own issue."
- **Skill-branch drift.** `tools/skill_drift_check.py` now runs for real and returns
  `0/5 repos aligned`: `atrium_document.schema.json` is out of parity on every `agent-skill` branch
  and translator's skill branch is a release behind. Triage whether the skill branches are meant to
  be regenerated; if so this is routine, and if not it is a real finding.

### F — Settle the relationship to issue #10
Unchanged from both previous editions, and still needs @K4TEL, who opened both. #55 demonstrated the
pattern that worked — carve a factor out as a sub-issue with a real acceptance criterion — and #53
has now run that pattern nine times to completion. Whether #53 **absorbs** #10's process or runs
**alongside** it should be stated rather than left to overlap silently.

## Verification / done-criteria

- **A**: `git ls-files docs/templates/ | grep env` returns the undotted name; the link at
  `docs/k8s_deployment.md:127` resolves on GitHub; `grep -n "seven canonical" docs/docker_gha_roadmap.md`
  returns nothing; `server.template.sh` runs from a `.yaml` repo without editing.
- **B**: each closed issue's final comment states the acceptance bullets and the evidence for each.
- **C**: `grep -i replica docs/k8s_acceptance_runbook.md` returns at least one hit.
- **D**: an E2E run at `image-tag: test` is linked on #62; a red-then-green `PORT=9000` pair is
  linked on #58.
- **E**: two issues exist and are linked from `docs/docker_gha_roadmap.md` §9 and from #59.
- **#53 itself** closes when the three ambers are green and every sub-issue is closed — except #55,
  which closes on the partner's word and not ours.

## Out of scope

- **Re-auditing the twelve factors.** Done twice (09-09, 09-14); the digest holds both with
  file:line citations. Re-derive only if the trees move substantially again.
- **Re-litigating findings tracked under #10's own lettering.**
- **Converting file-based algorithmic parameters to environment variables.** Factor III asks for
  deployment-varying config, and #60 got this boundary right — alto's ~95 `ATRIUM_TEXT_UTILS_*` /
  `ATRIUM_CLASSIFY_*` constants are declared `NOT_PUBLISHED` with a reason, which is the correct
  answer, not an omission.
- **Converging the `.yml`/`.yaml` compose split.** #64 decided to leave it; the roadmap needs to be
  amended to match that decision, not the other way round.
