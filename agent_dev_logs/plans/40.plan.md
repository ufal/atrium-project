# Strategy — Issue #40: CI/CD: Enforce Branch Protection (GH) & Enable GPU Runner (GHA)

> Issue [\#40](https://github.com/ufal/atrium-project/issues/40) · Labels: `enhancement`, `development`, `ci`, `github_actions` · Milestone: WP4 — Pre-Closure Architecture Review: schematics + documentation handover · **Status: ✅ Finish — pilot (AMČR, #67), rescoped: branch protection now, immutable releases, reusable workflows pinned by commit; GPU runner ⏸️ deferred** _(was: 🟡 GPU half reframed by @motyc; branch-protection half stalled)_
> _Refreshed 2026-09-26 (AMČR baseline). Evidence: [`../digests/40.digest.md`](../digests/40.digest.md)._

### 2026-09-26 — AMČR baseline ([#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ✅ finish — branch protection now, the GPU runner later.** Decided by motyc (AMČR) in #67 and on this
thread ([5847698000](https://github.com/ufal/atrium-project/issues/40#issuecomment-5847698000)), adopted by ÚFAL.
AMČR will build its production images from a release tag, after its own checks, so **a tag has to mean one thing,
permanently**. AMČR does not depend on this — its checks run on its side — but it makes a released tag trustworthy
for everyone. The GPU half (§B–D below) is ⏸️ deferred: *"outside what AMČR needs"* (motyc, 2026-08-02 and 09-26).

**The rescoped work, in order:**

**A′ — Branch protection, all six repositories** (A below, widened from the hub's `main`/`test`).
- [ ] The default branch and `test` in the hub and the five tool repos: PR required, the repo's own required checks,
  no force-push, no deletion. Required checks per tool repo: the `docker`, `para-drift`, `api-contract`, `pre-commit`
  and `security` callers; in the hub: `hub-self-check`, `all-repos-smoke`, `codeql`, `pre-commit`.
- [ ] Protect the tag patterns too: `v*` in the tool repos; `v1` and `doc-schema-v*` in the hub (a tag ruleset:
  restrict update and deletion to maintainers).

**E — Immutable releases in the five tool repositories** (new).
- [ ] Turn on *release immutability* in each tool repo's settings. It applies to releases published after it is on;
  existing releases stay as they are.
- [ ] **Check the release flow first, on one repository.** All five `release.yml` create the release and attach
  `service_bundle.zip` in one `softprops/action-gh-release` step (pinned `efb35369…`, v3.0.3; e.g. alto
  `release.yml:144`). An immutable release refuses assets added after publication, so confirm on the next
  translator or page-classification tag that the bundle is attached before the release is published; if it is not,
  create the release as a draft, attach, then publish.
- *Done when* (David's): **new releases in the five repositories are immutable.**

**F — Reusable workflows referenced by commit** (new).
- [ ] Replace the 35 `uses: ufal/atrium-project/.github/workflows/<name>.reusable.yml@v1` lines (seven reusables ×
  five repos) with `@<40-char SHA>  # v1`. Dependabot's `github-actions` entry, already present in all six
  `dependabot.yml`, then proposes the SHA bumps with the comment kept.
- [ ] **The second pin.** `para-drift.reusable.yml:21` and `workflow-lint.reusable.yml:37` default `hub-ref` to
  `v1` and check the hub out at it (`:40-41`, `:64-65`), so a commit-pinned `uses:` would still read the shared
  files at the movable tag. Callers pass `hub-ref:` with the same SHA, and a `workflow_lint.py` rule fails a caller
  whose `hub-ref` differs from its `uses:` SHA (so a Dependabot bump that updates only one of them turns red instead
  of drifting).
- [ ] What stays: `v1` keeps moving as the *release channel* the hub announces; nothing resolves through it at run
  time. The `doc-schema-v1` freeze tag stays as it is (immutable by the tag ruleset in A′).
- *Done when* (David's): **every `uses:` line that points to this repository names a commit.**

**Ordering:** A′ first (cheap, independent). E before the next tool releases — the fixes #67 asks for will reach
AMČR as new releases, and those should be the first immutable ones. F with the next `v1` move (the #67 schema round
is the natural one), because it changes the same 35 lines that a `v1` move would otherwise leave alone.

## Context
This issue bundles two originally-independent tasks that turned out to have very different shapes
once discussed. Branch protection is a straightforward GitHub-settings change with no external
dependency. The GPU runner task, as originally framed ("configure the external GPU runner"),
assumed a self-hosted runner reachable from UFAL's GitHub Actions — @motyc ruled that out on
governance grounds (ARUP/B data-handling boundary: GH Actions must not be wired to any ARUP/B
environment; the boundary is a Docker image built and run entirely inside ARUP/B's own fork/
Kubernetes cluster). The GPU half is therefore not "blocked on hardware" — it's "blocked on which
outside-infrastructure GPU service to use and how testing integrates with a service ATRIUM doesn't
control."

## Status snapshot
| Item                                                       | State             | Note                                                           |
|------------------------------------------------------------|-------------------|----------------------------------------------------------------|
| GPU-in-UFAL-CI ruled out                                   | ✅ **DECIDED**     | @motyc, 2026-08-02 — governance boundary, not a technical one  |
| Metacentrum identified as the outside option               | ✅ **IDENTIFIED**  | @stranak; e-INFRA.cz's GPU grid, application-gated             |
| Metacentrum application submitted                          | ⏸️ **DEFERRED**   | after the pilot (AMČR, #67)                                    |
| Alternative CERIT services (Galaxy, n8n-agents) considered | 🔄 **FLAGGED**    | @motyc, not evaluated against the actual `pytest -m slow` need |
| `pytest -m slow` actually pointed at any GPU runner        | ⏸️ **DEFERRED**   | after the pilot (AMČR, #67)                                    |
| Branch protection on hub `main`/`test`                     | ❌ **NOT STARTED** | widened to all six repositories + tag rules (A′)               |
| Immutable releases, five tool repos                        | ❌ **NOT STARTED** | new 2026-09-26 (E)                                             |
| Reusable workflows pinned by commit                        | ❌ **NOT STARTED** | new 2026-09-26 (F); 35 `@v1` lines today                       |

## Strategic actions

### A — Branch protection (independent, no blocker, do this first)
Apply GitHub branch-protection rules to `main` and `test`: require PR review, require the hub's
own CI checks (`hub-self-check`, `all-repos-smoke`, `codeql`, `pre-commit`) to pass, disallow force
pushes. This has no dependency on the GPU decision and is the cheaper of the two tasks — it should
not continue waiting on the GPU half to resolve. Owner: @stranak.

### B — Decide what "GPU testing" is actually for, before applying anywhere — ⏸️ deferred (2026-09-26)
Before applying to Metacentrum or evaluating CERIT's services, get an explicit answer to: which
`slow`-marked tests actually need a GPU (vs. just needing more wall-clock time), and does CI need
to *run* them on every push/schedule, or is an occasional manual/`workflow_dispatch` run on an
outside service enough? The original task assumed "wire CI to a runner"; @motyc's ruling makes that
the wrong default shape — a nightly/weekly manual trigger against an outside Metacentrum job is a
very different integration than a live self-hosted runner label.

### C — If Metacentrum is the answer, submit the application — ⏸️ deferred (2026-09-26)
`metavo.metacentrum.cz/en/application/form` is the concrete next step if §B concludes an outside
GPU grid is worth the integration cost. Whoever owns this should also check what CI-integration
pattern Metacentrum supports (does it expose anything GitHub Actions can dispatch to, or is this
purely a manual/offline validation path run by a person, with results posted back as evidence?).

### D — Re-scope the acceptance criteria — ⏸️ deferred (2026-09-26)
The original acceptance criteria ("workflows targeting the GPU runner successfully spin up,
execute `slow` tests, and tear down without hanging") assumed a live CI-integrated runner. If §B/C
land on a manual-trigger, outside-infrastructure model instead, the acceptance criteria need
rewriting to match — e.g., "a documented, repeatable manual procedure exists for running the
`slow` suite on Metacentrum, with the last run's result linked from this issue."

## Verification / done-criteria
- Branch protection live on the default branch and `test` in **all six** repositories, verified by attempting (and
  having rejected) a direct push without a passing PR; a push that moves a protected tag is rejected.
- **New releases in the five tool repositories are immutable** (David's *Done when*): the release page shows it,
  and an attempt to replace the bundle on the first such release fails.
- **Every `uses:` line that points to this repository names a commit** (David's *Done when*):
  `grep -rn "uses: ufal/atrium-project" .github/workflows | grep -v "@[0-9a-f]\{40\}"` returns nothing in each tool
  repo, and each caller's `hub-ref` equals its SHA (the lint rule).
- ~~Either: a self-hosted-runner-free GPU testing procedure is documented and has been run at least
  once with results posted here, or the ecosystem has made an explicit, recorded decision that GPU
  testing isn't worth pursuing at all for the remainder of the project.~~ ⏸️ The GPU half is deferred until after
  the pilot (AMČR, #67); the decision is recorded, the procedure is not needed now.

## Out of scope
- The specific `slow`-marked tests themselves and which repos own them — see each tool repo's own
  DEVLOG (alto owns the one real GPU-concurrency test per the 2026-09-07 cross-repo digest).
- Model-serving GPU questions (#26, #27) — those are about production inference sizing, not CI.
